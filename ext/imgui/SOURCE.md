Dear ImGui, docking branch, vendored from https://github.com/ocornut/imgui
at the revision recorded in UPSTREAM_REVISION. MIT licensed (LICENSE.txt).

The vendored set is the core library plus the Vulkan and Metal render
backends. The platform backends are deliberately absent: imtty implements
the platform side (input, clipboard, cursors, scale) over plt.
