Пункты 2, 3, 23, 32, 33, 36, 44 (dw-шрифты, шрифтовый фолбек, пустые клетки) — отдельный заход, в этом проходе не делаются.

1. Metal: гонка CPU/GPU на общем cellBuffer — framesInFlight=3, но буфер обновлений один; draw() ждёт только fence кадра N-3, а кадры N-1/N-2 ещё могут читать буфер, который перезаписывает buildCellUpdates(). Рваные кадры под нагрузкой. Vulkan-путь сделан правильно (per-frame буферы) — Metal надо привести к нему (render_metal.mm:290,740-752,843-847).
2. CJK/emoji из коробки — пустые клетки: без валидного dwfont широкие символы рендерятся пустотой (return 0 слот), а дефолт 18x18ja почти никогда не проходит проверку «ровно 2×». Fallback-цепочки по покрытию кодпоинтов нет вообще (render_vk.cpp:1748, font_pack.cpp:136, options.cpp:39).
3. Правило dwfont «ровно вдвое шире» ломается округлением и падает молча: точное равенство width/height/baseline двух разных шрифтов случайно; отказ проглатывается createOptional без единого лога — пользователь с явным -dwfont получает пустые ячейки без диагностики (font_freetype.cpp:215-217,259-261, font_pack.cpp:66-72).
4. Cocoa IME: двойная доставка клавиш — keyDown: шлёт key-событие в терминал до interpretKeyEvents: без проверки hasMarkedText; Enter при выборе кандидата уходит и как commit, и как \r в pty; стрелки/Escape при навигации по кандидатам — тоже (platform_cocoa.mm:179-183).
6. Тесты не исполняют прод-парсер: прод собирается ragel -G1, все тесты и фаззер — -T1 (SHITTY_COMPACT_PARSER); семантика C++ одна, но кодоген прода не исполняется нигде. Плюс python-suite ходит только через трассирующую инстанциацию ParserImpl<true> (build.py:164-196,295).

🟠 Средняя серьёзность

8. rotateRows не инвалидирует прямоугольное выделение (Rect нулевой ширины → ранний return) — IL/DL проворачивает текст под живым выделением (screen.cpp:3486).
9. fillCells — единственная мутация без инвалидации выделения: DECALN (ESC#8) и переключение 80/132 оставляют выделение висеть (screen.cpp:1922).
10. copyRectangle (DECCRA) переносит биты wrap — ложные склейки логических строк → неверные word-select/reflow (screen.cpp:2461).
11. DECSTBM/DECSLRM с невалидной областью всё равно двигают курсор в home (xterm/ghostty — полный no-op) (vterm.cpp:4759,4778).
12. Горизонтальные поля DECLRMM протекают через переключение alt-экрана (vterm.cpp:3051-3133).
13. DECSC/DECRC не сохраняют палитровые индексы (fgPalIx и др.) — stale цвет после sgrBold и в ответе DECRQSS (vterm.cpp:1135).
14. ESC внутри OSC/DCS поглощается как данные вместо прерывания строки (DEC STD 070, vte, ghostty прерывают) — \x1b]0;A\x1b[31m\x07 теряет SGR (parser.rl:5610 и др.).
15. Встроенные C0 LF внутри последовательностей игнорируют LNM (parser.cpp:376).
16. PtyOutputQueue без верхней границы — решено не чинить (по указанию).
17. Busy-spin requestFrame→timeout(0)→frame() при устойчивом false от рендера (свёрнутое окно, неудачный swapchain) — 100% CPU (application.cpp:340-364).
18. Бюджеты GC CellExtraStore не клампятся к maxExtraRef (16.7M): при 200 cols × 65k saveLines bad_alloc вылетает наружу раньше GC — падение на графемоёмком контенте (cell_extra_store.cpp:447).
19. DECFRA не ставит drawn → DECRQCRA возвращает 0 по залитой области, расходится с xterm; тесты подменяют checksum чтением снапшота и не ловят (screen.cpp:2425).
20. Vulkan: исключение между vkResetFences и vkQueueSubmit → несигналенный fence навсегда (сейчас спасает только то, что исключение убивает процесс; любой будущий retry превратит в дедлок) (render_vk.cpp:2350).
21. Без maintenance1 retired-свопчейны копятся до 8 (сотни МБ + WaitIdle-хитч посреди ресайза) (render_vk.cpp:1339).
22. В кодовой базе пять UTF-8-декодеров; decodeUtf8One в plt (вчерашний коммит) пропускает overlong/суррогаты/F5-F7 — путь IME-commit может доставить не-скаляры в pty (platform_wayland.cpp:387).
23. Индийские кластеры с матрами (Mc→Wide) уводятся в CJK-шрифт → тофу (grapheme.cpp:482).
24. Мышь: off-by-one pixel→cell — первый пиксель каждой ячейки (кроме первой) относится к предыдущему столбцу; юнит-тесты подобраны мимо границ (mouse_frontend.cpp:33).
27. Фаззер op 219 (advanceAnimation(force=false)) сравнивает wall-clock между ригами — ложный «determinism bug» на 500-мс границе.
28. plt-тесты не подключены к CI вообще (import_build берёт только libplt.a, flake-checks гоняют только группу shitty).
29. Headless-поллер no-op — решено не делать (исключено ранее вместе с plt-переделкой headless).

🟡 Низкая серьёзность

32. Переполнение глиф-кэша → молча пустая клетка, даже без fallback-рамки (render_vk.cpp:1698).
33. Double-width lead без continuation-ячейки читает не тот атлас (render_vk.cpp:2100).
34. «Двойное sRGB» при fallback-форматах поверхности, colorspace игнорируется (render_vk.cpp:1438).
35. VS15 ужимает шесть EAW-Wide баз (〰〽㊗㊙🈂🈷) до 1 клетки с обрезкой.
36. CoreText/FreeType паритет: разная обработка missing glyph, на macOS dwfont вообще не валидируется, метрики округляются по-разному.
37. latestSerial откатывается назад при автоповторе (repeat() реиграет старый serial).
38. Seat removal не чистит preedit-состояние (в отличие от textInputLeft).
39. textInputDone кэширует textInputWindow через sink-колбэки — латентный UAF, если sink когда-нибудь начнёт удалять окна.
40. Checksum (DECRQCRA): кодепоинты графемных кластеров учитываются в total, но не в trimmed — несимметрично.
41. Fast-path writeAsciiLines теряет Continuation semantic-маркер на очищаемых строках (расходится со slow-path).
42. DECRC без сохранённого курсора — no-op (xterm делает home+reset).
43. Отрицательный pitch FreeType адресуется неверно (недостижимо с FT_LOAD_RENDER).

Недоделки

44. Нет шрифтового fallback по покрытию (главная причина пустых ячеек), нет синтетического bold/italic, path-резолвер не умеет стили/индексы фейсов, DW-шрифт всегда regular.
45. IME не доведён в самом терминале: InputRouter::preedit — заглушка, композиция не рендерится.
46. VK_ERROR_SURFACE_LOST не обрабатывается — смерть процесса.
47. DECDHL в reference-рендере — кроп вместо удвоения.
48. Underline на DECDHL рисуется в обеих половинах.
49. Курсор на continuation-ячейке невидим.
50. Бордер не реагирует на OSC 11 (смену цвета фона).
51. DECAC frame colors — пустые тела-заглушки.
52. DECDMAC — фиксированные ответы-заглушки.
53. S8C1T-кодирование ответов не реализовано (в xfail замаскировано как «ожидаемое падение», по сути — нереализованная фича).
54. differential.py ничего не сравнивает между терминалами (только регэкспы формы) и молча деградирует без xterm/foot/kitty.
55. Fuzzing-конфигурация — не трогать (по указанию).
56. Захардкоженные Unicode-таблицы (VS15/VS16/viramas) без генератора, комментарий о «стабильности с 9.0» неверен; требование utf8proc ≥2.9 нигде не зафиксировано.
57. Мёртвый код: dirtySpan, Damage::operator=(&&) в screen.cpp; дубли hex-хелперов в test_mode.cpp.

Улучшения (перф)

59. fullDamage на каждое изменение выделения — драг мыши = полный перерендер каждый кадр; дамажить объединение старой/новой области (render_vk.cpp:2533).
60. Reflow материализует весь scrollback дважды (сотни МБ временных векторов на каждый шаг drag-resize) — стримить построчно (screen.cpp:1258).
61. Word-snap каждый кадр без лимита — O(N²)-подобная нагрузка на гигантских обёрнутых строках; кэш по contentRevision (screen.cpp:1590).
62. OSC 52/URI копятся побайтно в ragel — bulk-поглощение ASCII ускорит мегабайтные base64.
63. ~1.07 MiB на экземпляр парсера (scratch 1 MiB встроен в объект) — сделать лениво.
64. Цветной атлас наследует сетку mask-атласа: бюджет 16 МБ превращается в 64 МБ при первом emoji.
65. hasBlinkingText — полный скан сетки по таймеру; поддерживать счётчик.
66. Общая FT_Library вместо пяти; в color-пути глифы грузятся до трёх раз.
67. frame_pri->expose(); frame_alt->expose(); ×15 в vterm — вынести в хелпер.
