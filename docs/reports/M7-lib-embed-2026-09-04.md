# M7. `lib/embed` — фасад лёг без единого конфликта и не собрался по двум символам

**Дата:** 2026-09-04 · **Задача:** `M7`, волна 7 плана `docs/plans/2026-08-29-upstream-merge.md`, блок «Перенумерация шагов волн 6–7» · **Ветка:** `wave/upstream-merge-w7` · **Предмержевая голова:** `2ce870b0` · **Коммит мержа:** `9698bf2f`, родители `2ce870b0` + `60562f22` · **Вердикт:** `DONE с одним незакрытым критерием` — шесть критериев из семи закрыты выводом команд; шестой (`lib/embed` собирается) **не закрыт**, разобран до символа и передан задаче А отдельным разделом.

---

## 1. Коротко

Апстримный `60562f22` «Add lib/embed: a C facade over the VT core» слился **автоматически, ноль конфликтов**, и лёг построчно тем же диффом, что у апстрима: 1274 содержательные строки, `diff` пуст, статистика с обеих сторон `13 files changed, 1451 insertions(+), 9 deletions(-)`.

Собраться фасад при этом не может, и **причина не в фасаде**. Неопределённые символы `lib/shitty` есть у самого `lib/vterm/vterm.cpp` — ровно два, `Composer::contentInsets()` и `Composer::resize()`. Это делает наш `lib/vterm` не самодостаточным, а значит `libshitty_vt_core` — архив, который апстрим собирает из `glob(lib/vterm/*.cpp)` плюс фасад, — на нашем форке не линкуется независимо от того, что написано в `shitty_vt.cpp`.

Шаг **не стал** это чинить: закрытие перехода границы — архитектурное решение владельца форка, и бриф прямо предписывал остановиться и написать. Апстримные цели оставлены дословно и накрыты флагом `embed_facade_links = False` (§4.5), а `tst/test_embed_example.py` получил пропуск **по наличию артефакта** (§4.6). Дерево остаётся таким же зелёным, каким было: **228 узлов**, **`OK: 966`**, питоновский набор с теми же двадцатью красными, пять гвардов зелены, сломанная цель по-прежнему одна — `main_fuzz` на линковке, ошибок компиляции ноль.

**И самое важное, что этот шаг узнал.** `example`, собранный руками в обход графа, проходит **все 37 апстримных тестов на нашем ядре** (§8.6). Значит расхождение целиком линковочное: фасад и наши инварианты `A1`/`A8` не спорят, а адаптация к нашей сигнатуре — пять строк.

---

## 2. Что принёс апстрим — и чем это больше, чем сказано в плане

План описывает `M7` как «`lib/embed`». Фактически коммит трогает 13 файлов, и `lib/embed` — только пять из них.

| Файл | Строк | Что |
|---|---|---|
| `lib/embed/shitty_vt.cpp` | +595 | реализация фасада |
| `lib/embed/shitty_vt.h` | +117 | публичный C-заголовок, восемь функций |
| `lib/embed/link_shared.py` | +40 | линковка `.so` с version script |
| `lib/embed/merge_archives.py` | +26 | склейка трёх архивов в один |
| `lib/embed/shitty_vt.map` | +6 | наружу видны только `shitty_vt_*` |
| `bin/example/main.c` | +171 | драйвер библиотеки на чистом C |
| `tst/test_embed_example.py` | +343 | 37 тестовых методов |
| `build.py` | +107/−1 | цели `libshitty_vt_core`, `example`, `plt_headless`, `libstd_pic`, группы `a` и `so` |
| `ext/plt/build.py` | +14/−3 | `-Dplatforms=headless` → `libplt_headless.a` без wayland/cocoa |
| `ext/libstd/build.py` | +6/−1 | `-fPIC` → `libstd_pic.a` |
| `lib/vterm/vterm.h` | +15 | `VtermState` растёт с одного поля до тринадцати |
| `lib/vterm/vterm.cpp` | +12 | `VtermImpl::state()` заполняет эти поля |
| `.github/workflows/ci.yml` | +2/−2 | `./build test` → `./build test a so` в двух джобах |

**Цель у `lib/embed` есть, и её принёс сам апстрим.** Бриф допускал, что цели не окажется и её заведёт `T6.1` — заводить нечего.

### 2.1. `VtermState` — единственное содержательное изменение ядра

Апстрим превращает `VtermState` из однополевой структуры (`synchronizedOutput`) в снимок режимов на тринадцать полей: `mouseTracking`, `mouseEncoding`, `alternateScreen`, `bracketedPaste`, `applicationCursorKeys`, `applicationKeypad`, `focusEvents`, `autoWrap`, `originMode`, `insertMode`, `showCursor`, `screenReverse`. Мотив назван в сообщении коммита: фасад обязан отдавать режимы, не залезая в тестовый API.

Все двенадцать источников (`mouseTrk.mode`, `altScreenBufferMode`, `bracketedPasteMode`, `cursorKeyMode`, `keypadMode`, `mouseTrk.focusEventMode`, `autoWrapMode`, `originMode`, `insertMode`, `showCursorMode`, `screenReverseVideo`) у нас уже есть под теми же именами — конфликта не возникло, `st` собрался.

---

## 3. Конфликты: ни одного

```
$ git merge --no-ff --no-commit 60562f22
Auto-merging .github/workflows/ci.yml
Auto-merging build.py
Auto-merging lib/vterm/vterm.cpp
Auto-merging lib/vterm/vterm.h
Automatic merge went well; stopped before committing as requested
```

Четыре файла слились трёхсторонним слиянием, остальные девять — добавления. Разрешать было нечего, и раздел «чья сторона взята и почему» в этом шаге пуст **по факту, а не по недосмотру**: ни в одном хунке стороны не пересеклись.

Почему так вышло, хотя `M6c`–`M6e` стоили конфликтов: `60562f22` пишет в новый каталог и в четыре точки, где наш форк за 537 коммитов не менял ничего — `production_path_flags`, конец блока программ в `build.py`, `deps`/`env` питоновских групп, `VtermState`. Единственное место, где апстрим и мы правили один и тот же объект, — `make_python_test_groups`, и там апстрим добавляет элемент в список и ключ в словарь, а наши правки волн 3–6 лежали в других строках той же функции.

### 3.1. Сверка с апстримным диффом — 0 расхождений

Приём `T5.6`: сравнение против **merge-base**, а не против `origin/master`.

```
$ git diff --cached HEAD  -U0 | grep -E '^[+-][^+-]' > ours.txt        # 1274 строки
$ git diff 60562f22~1 60562f22 -U0 | grep -E '^[+-][^+-]' > upstream.txt # 1274 строки
$ diff upstream.txt ours.txt  ;  EXIT=0
```

Пусто. Множество файлов снято командой `git ls-tree -r --name-only 60562f22 lib/embed`, а не из таблицы плана.

Это **сам мерж**. Дальше шаг добавил к нему правки §4.5 и §4.6. Итоговая сверка всего шага, с нормализацией отступов (апстримный блок целиком уехал на четыре пробела внутрь `if`) — `1274` строки апстрима против `1325` наших:

```
только у апстрима — 2 строки, обе заменены условной формой:
  +"SHITTY_EMBED_EXAMPLE_BINARY": "$(B)/example",
  +deps=[test_target, pretty_test_target, toml_dump, example],

только у нас — 53 строки, из них 45 комментарий и 8 кода:
  +embed_facade_links = False
  +if embed_facade_links:
  +else:
  +example = None
  +deps=[test_target, pretty_test_target, toml_dump, *([example] if example is not None else [])],
  +**({"SHITTY_EMBED_EXAMPLE_BINARY": "$(B)/example"} if example is not None else {}),
  +EXAMPLE_PRESENT = EXAMPLE.is_file() and os.access(EXAMPLE, os.X_OK)
  +@unittest.skipUnless(EXAMPLE_PRESENT, f"the embedding example is not built: {EXAMPLE}")
```

**Восемь строк кода — весь наш след в этом мерже**, и каждая названа. Ни одной содержательной апстримной строки шаг не потерял и не переписал: все 1274 на месте, часть — с другим отступом.

---

## 4. Адаптация фасада к нашей сигнатуре `Vterm::create`

Раздел, ради которого шаг и был отдан исполнителю.

### 4.1. Что именно не сходится

Две ошибки компиляции, обе в `lib/embed/shitty_vt.cpp`:

```
shitty_vt.cpp:336:22: error: no member named 'windowResized' in 'Vterm'
shitty_vt.cpp:480:160: error: too few arguments to function call, expected 11, have 9
```

**Первая — механическая.** Апстримный `Vterm::windowResized()` у нас заменён на `paneResized(const VtGeometry&)` (`lib/vterm/vterm.h:277`), и замена документирована прямо там: «Upstream's `windowResized()` is what this replaced: the window's grid is not this pane's» (`A8`). Фасад держит одну панель во всё окно, поэтому подстановка однозначна.

**Вторая — не механическая.** Наши одиннадцать параметров против апстримных девяти:

```
upstream: create(owner,            geometry, config, extras, smallObjects, scheduler, host,           pty, traceFactory)
наш:      create(owner, composer&, windowGeometry, config, extras, smallObjects, scheduler, host, geometry, pty, traceFactory)
```

Из двух лишних параметров один безобиден: `geometry` — прямоугольник **панели**, отдельный от `windowGeometry` (окна). Для фасада это одна и та же величина, и передать её вторым разом ничего не стоит.

Второй — `Composer&` — упирается в то, что `Composer` живёт в `lib/shitty`.

### 4.2. Почему это не чинится внутри `lib/embed`

**Измерено, а не выведено.** Скомпилировал наш `lib/vterm/vterm.cpp` с флагами embed-цели и посмотрел, чего ему не хватает:

```
$ c++ ... -fPIC -c lib/vterm/vterm.cpp -o vterm.o
$ nm -u vterm.o | c++filt | grep -E 'Composer|SessionSet'
Composer::contentInsets() const
Composer::resize(unsigned short, unsigned short)
```

**Два символа, и это весь разрыв.** Пятое обращение, `composer.sessions->cellCapacityExcept(this)` (`vterm.cpp:3316`, `A11`), неопределённым не становится — вызов виртуальный и идёт через vtable.

Четыре места в `vterm.cpp`:

| Строка | Обращение | Зачем |
|---|---|---|
| `6562` | `gridColumns(width, composer.contentInsets(), …)` | `CSI 18t` — колонки по пикселям окна |
| `6569` | `gridRows(height, composer.contentInsets(), …)` | `CSI 19t` |
| `6598` | `composer.resize(…)` рядом с `window->requestResize(…)` | `CSI 4t`/`CSI 8t` |
| `6640` | `const Insets insets = composer.contentInsets();` | `CSI 8t` — размер в клетках в пиксели |
| `3315` | `composer.sessions != nullptr` | `A11`, разрешение отдаёт `T6.1` |

Строка `6598` — **та самая строка без владельца**, которую волна 6 перенесла в волну 7: борд, раздел «Открытое, перенесённое в волну 7», «решение отдало её `M6d`, тот опроверг это в своём же отчёте, дальше передавали `T5.1` и `T5.5`, не досталась никому».

### 4.3. Что стоит адаптация «в лоб» — тоже измерено

Проверил вариант «фасад строит свой `Composer`, как это делает наш `lib/vterm/vt_headless.cpp:232-253`»: `#include <lib/shitty/composer.h>`, `pool->make<Composer>(pool)`, `windowResized()` → `paneResized()`, одиннадцать аргументов.

**Компилируется без единой ошибки.** Но цена видна сразу:

```
$ nm -u composer.o | c++filt        (40 неопределённых)
Brand::generic · Brand::identifierCString · Options::uriSchemeAllowed
createGlyphCache · createInputRouter · InputBindings::create
createCoreTextFontResolver · createFontconfigResolver
createPathFontResolver · createEmbeddedFontResolver
createCoreTextFontRenderer · createFreeTypeFontRenderer
```

Конструктор `Composer` (`lib/shitty/composer.cpp:40-108`) зовёт их **безусловно**, без единого `if`. То есть «C-фасад над VT-ядром» пришлось бы слинковать с фонт-стеком, роутером ввода, таблицей биндингов и кешем глифов; `font_coretext` — Objective-C++ и требует CoreText, а на Linux в `libshitty_vt.so` уехали бы fontconfig и freetype. Это ровно противоположно смыслу `lib/embed`, поэтому правка была откачена и в дерево не попала.

### 4.4. Выход, названный не этим отчётом

Разрешение в `lib/vterm/check_includes.py` для пары `vterm.cpp` + `composer.h` формулирует его буквально:

> What closes both is a single change — the window's insets reach the core through `VtHost` rather than through a `Composer&` — and nothing forbids it. Not owned by a task.

Практически это: отдача инсетов окна через `VtHost` (тип уже есть — `VtInsets` в `lib/vterm/vt_geometry.h`, `Insets` из `composer.h` ядру больше не нужен) плюс переезд `gridColumns`/`gridRows`/`gridPixelWidth`/`gridPixelHeight` из `lib/shitty/grid_geometry.h` в `lib/vterm`. Побочно это закрывает **четыре из шести** нарушений `vterm_boundary` и снимает `Composer&` из `Vterm::create`, оставляя десять параметров вместо одиннадцати. `composer.sessions` (`A11`) остаётся и уходит к `T6.1`.

**Это архитектурная правка форка, а не мержа.** Решение принимает владелец форка; исполнитель `M7` остановился здесь и написал, как предписывал бриф.

### 4.5. Что сделал шаг вместо адаптации

Апстримный блок целей в `build.py` оставлен **дословно** и накрыт флагом:

```python
embed_facade_links = False

if embed_facade_links:
    …апстримный блок, без единого изменения…
else:
    example = None
```

Плюс два места `make_python_test_groups`, где апстрим упоминает `example`, стали условными — `*([example] if example is not None else [])` в `deps` и `**({…} if example is not None else {})` в `env`. Переключение обратно — одно слово `True`; больше в `build.py` трогать нечего, и это записано в самом комментарии рядом с флагом вместе с обоими недостающими символами и ценой линковки `composer.cpp`.

Комментарий рядом с флагом перечисляет **что именно вернётся** при `True` — цели, оба места в `deps`/`env` питоновских групп и пропуск в тестовом файле, — чтобы задаче А не пришлось восстанавливать это по диффу.

Чего это стоит и чего не стоит:

- **не стоит ни строки апстримного кода в `lib/embed`** — пять файлов фасада и `bin/example/main.c` лежат ровно такими, какими их прислал апстрим (§3.1, `diff` пуст);
- **не стоит ни одной сломанной цели** — `libshitty_vt_core` и `example` не существуют, а не падают; сломан по-прежнему один `main_fuzz`, на линковке (§8.5);
- **стоит одиннадцать строк в `tst/test_embed_example.py`** — пропуск по наличию артефакта, §4.6.

### 4.6. Пропуск по наличию артефакта, а не по чему-нибудь ещё

Без бинарника файл давал **82 ошибки**: `EXAMPLE` берётся из окружения и запускается без проверки. Восемьдесят два постоянных красных сделали бы эталон питоновского набора нечитаемым для волн 7 и 8 — а `R6-test` уже показала, что место, чей наблюдатель заранее красный, не проверяется **вовсе**.

Это не выключение теста и не нарушение §5 плана: тест, которому нужен собранный артефакт, обязан объявлять это предусловием, а не падать. У апстрима гварда нет по единственной причине — у него `example` собирается всегда.

```python
EXAMPLE_PRESENT = EXAMPLE.is_file() and os.access(EXAMPLE, os.X_OK)

@unittest.skipUnless(EXAMPLE_PRESENT, f"the embedding example is not built: {EXAMPLE}")
class EmbedExampleTest(unittest.TestCase):
```

Признак — **сам артефакт**: не платформа, не переменная окружения, не «мы в форке». Пропуск по платформе — та самая ошибка, которую `test_gpu_smoke.py` в этом репозитории уже отменял двумя коммитами. И снимать этот пропуск потом не придётся: первая же сборка, выпускающая бинарник, гасит его сама.

**Что гвард не прячет — показано четырьмя состояниями, а не рассуждением** (§8.6): артефакта нет → 37 пропущено; файл есть, но не исполняемый → 37 пропущено; артефакт есть и **сломан** → 37 исполнены, 82 ошибки; артефакт есть и рабочий → 37 исполнены, **все зелёные**.

---

## 5. Чего в `lib/embed` ещё нет и что придёт в `M8`

`M7` **не приносит `lib/embed` целиком**, вопреки формулировке плана.

Фасад после `M7` публикует восемь функций: `shitty_vt_new`, `shitty_vt_free`, `shitty_vt_feed`, `shitty_vt_resize`, `shitty_vt_take_replies`, `shitty_vt_each_cell`, `shitty_vt_cursor_state`, `shitty_vt_modes`. `shitty_vt_memory_usage` и `shitty_vt_set_save_lines` приходят коммитом `7e2a3c15`, который **вне диапазона волны 7**.

Диапазон волны 7 — `60562f22..44d61bfc`. Проверено:

```
$ git log --oneline 60562f22..44d61bfc -- lib/embed tst/test_embed_example.py bin/example
(пусто)
```

То есть `M7b` к `lib/embed` не прикасается вовсе, а между `44d61bfc` и `origin/master` лежат **двадцать четыре** коммита, которые его меняют: скроллбэк через фасад (`e02782b0`, `162593bb`, `af165af9`), бюджет скроллбэка (`7e2a3c15`), alternate scroll (`2c468876`), кодирование ввода (`d744af00`), превью композиции (`ea1de5ea`, `82dbb44a`), источник цвета клетки (`bddb1f70`), `build tgz` (`59b6dcd1`) и далее.

**Отдельным пунктом для `T6.1`:** если `T6.1` зафиксирует список экспортов по состоянию `M7`, волна 8 его сломает, а `tst/test_embed_example.py` вырастет. Фиксировать поверхность фасада на `M7` не следует.

---

## 6. Гварды

### 6.1. Состояние

Все прогоны — **прямым исполнением программы гварда**, текст вынут из `build.py` разбором AST и исполнен вне `./build` (`CLAUDE.md`: кеш адресуется содержимым и подставляет готовый штамп).

```
border_pixels_guard    GUARD_EXIT=0
mouse_geometry_guard   GUARD_EXIT=0
pane_grid_guard        GUARD_EXIT=0
darwin_call_guard      GUARD_EXIT=0
vterm_boundary         EXIT=0   (шесть разрешённых переходов, список не вырос)
```

Снято трижды: на предмержевой голове, сразу после мержа и на итоговом дереве — все три раза одинаково. Пробы ниже ставились на смерженном дереве; правки §4.5 и §4.6 живут в `build.py` и `tst/`, которых сканирование не касается.

### 6.2. Пять проб кодом

Каждая проба — строка **кода**, приписанная в конец файла; откат подтверждён `git diff --quiet` по этому файлу.

| № | Гвард | Файл | Проба | Результат |
|---|---|---|---|---|
| 1 | `border_pixels` | `lib/vterm/vterm.cpp` — **изменён этим шагом** | `u16 m7Probe() { return composer.borderPixels(); }` | `EXIT=1`, `lib/vterm/vterm.cpp:9972` |
| 2 | `mouse_geometry` | тот же | `void m7Probe2() { mouseGeometry(composer); }` | `EXIT=1`, `lib/vterm/vterm.cpp:9972` |
| 3 | `pane_grid` | `lib/shitty/render_reference.cpp` | `u32 m7Probe3() { return composer.geometry.columns + composer.geometry.rows; }` | `EXIT=1`, две строки `render_reference.cpp:1162` |
| 4 | `darwin_call` | `lib/vterm/vterm.cpp` — **изменён этим шагом** | `void m7Probe4() { createCsdTabsUi(composer); }` | `EXIT=1`, `lib/vterm/vterm.cpp:9972` |
| 5 | контроль | тот же | те же три вызова **в комментарии** | `EXIT=0` у всех четырёх — ловушка 3 воспроизведена |

Оговорка по `pane_grid`, та же, что у `M6d` и `M6e`: гвард читает только файлы, чьё имя начинается на `render` (`build.py:1760`), а ни одного такого файла шаг не изменил. Поэтому он доказан на своём обычном файле, а не на изменённом.

### 6.3. Две слепые зоны, которые завёл этот мерж

Обе — не разрешения, а область сканирования, и обе доказаны пробой кодом.

| Проба | Файл | Результат |
|---|---|---|
| 6 | `lib/embed/shitty_vt.cpp` — **новый каталог** | все четыре гварда `EXIT=0` |
| 7 | `bin/example/main.c` — **новый файл, суффикс `.c`** | все четыре гварда `EXIT=0` |

Проба одна и та же и содержит поводы для трёх гвардов сразу: `u16 m7Probe5() { mouseGeometry(composer); createCsdTabsUi(composer); return composer.borderPixels(); }`. В `lib/vterm/vterm.cpp` та же строка роняет три гварда (пробы 1, 2, 4); в этих двух файлах — ни одного.

Причины разные:

1. **`lib/embed` не входит в `guard_scan_roots`** — `build.py:1430`: `("lib/shitty", "lib/vterm", "ext/plt", "bin")`. Новый продуктовый каталог не охраняется ничем.
2. **`.c` не входит в `guard_scan_suffixes`** — `build.py:1431`: `(".cpp", ".h", ".mm")`. `bin` в корнях есть, а `bin/example/main.c` невидим.

Расширять **разрешения** запрещено (`T2.1`), но здесь речь не о разрешениях: это корни и суффиксы, то есть правка ровно того класса, которым `T2.1` и занималась (`Р3` плана: «гварды обязаны переехать вместе с кодом»). Ни одна задача волны 7 этого пока не держит.

---

## 7. Таблица критериев

| № | Критерий | Результат |
|---|---|---|
| 1 | `./build st --clear` зелёная, 228 узлов | **228**, `EXIT=0` — §8.1 |
| 2 | `unit_tests` ≥ `OK: 966`, `EXIT=0` | **`OK: 966`**, `EXIT=0`, 966 имён побайтово равны предмержевым — §8.2 |
| 3 | совпадение диффов, расхождения отнесены | **0 расхождений** — §3.1 |
| 4 | питоновский набор поимённо, тем же режимом | `Ran 6437`, красных **20**, `diff` с предмержевым списком пуст; `+37` посчитаны и пропущены — §8.4 |
| 5 | пять гвардов, четыре доказаны пробой кодом, `vterm_boundary` не вырос | `EXIT=0` у всех пяти, семь проб, шесть разрешённых переходов — §6 |
| 6 | `lib/embed` собирается | **не закрыт**, и закрыт поимённо: две ошибки, файл, строка, причина — §4.1; чем это кончается, если разрыв закрыть, — §8.6 |
| 7 | список сломанных целей не вырос | `main_fuzz` и только он, ошибок компиляции ноль — §8.5 |

---

## 8. Выводы команд

### 8.1. Сборка

```
$ ./build st --clear -j 10        (предмержевая голова 2ce870b0)  ; EXIT=0
[LD] {228/228} $(B)/st

$ ./build st --clear -j 10        (смерженное дерево)             ; EXIT=0
[LD] {228/228} $(B)/st

$ ./build st --clear -j 10        (итоговое дерево шага)          ; EXIT=0
[LD] {228/228} $(B)/st

$ python3 tst/pretty_binary_branding.py .build/pt                 ; EXIT=0
$ python3 tst/production_surface.py  (с тремя переменными)        ; EXIT=0
Ran 5 tests in 0.095s / OK
```

### 8.2. `unit_tests`

```
$ ./build unit_tests pty_test_helper -j 10                        ; EXIT=0
$ SHITTY_PTY_TEST_HELPER="$PWD/.build/pty_test_helper" \
      ./.build/unit_tests --threads=1 < /dev/null                 ; EXIT=0
OK: 966

$ diff base-ut-names.txt merged-ut-names.txt                      ; EXIT=0   (966 имён)
```

### 8.3. Гварды

Приведены в §6.1–6.3.

```
$ python3 lib/vterm/check_includes.py lib/vterm <stamp>            ; EXIT=0
```

### 8.4. Питоновский набор

Одним процессом (`--group=0 --group-count=1`), с полным окружением из `build.py:1283`, оба прогона в одном режиме.

```
до мержа:    Ran 6400 tests in 122.991s
             FAILED (failures=6, errors=14, skipped=17, expected failures=549)
после мержа: Ran 6437 tests in 117.854s
             FAILED (failures=6, errors=96, skipped=17, expected failures=549)

$ comm -13 base-red.txt after-red.txt | grep -vc test_embed_example   → 0
$ comm -23 base-red.txt after-red.txt | wc -l                         → 0
```

Двадцать предмержевых красных — известные средовые (`fontconfig`/`freetype`/`harfbuzz` выключены `optional_pkg()` на macOS). **Вне `test_embed_example.py` набор не шелохнулся: ни одного нового красного, ни одного исчезнувшего.**

Восемьдесят два, а не тридцать семь: у методов файла есть `subTest`.

Итоговое дерево, после пропуска по наличию артефакта (§4.6):

```
итог:        Ran 6437 tests in 120.854s
             FAILED (failures=6, errors=14, skipped=54, expected failures=549)

$ diff base-red.txt final-red.txt   → пусто (20 строк, посимвольно те же)
```

`6400 → 6437` — это тридцать семь тестов, которые апстрим принёс тем же коммитом; они **посчитаны и пропущены**, а не выключены. `skipped 17 → 54` — те же тридцать семь. Красных снова **двадцать, побайтово те же самые**: эталон волны читаем.

### 8.5. Сломанные цели

Сначала — на дереве сразу после мержа, до §4.5, со всеми четырнадцатью целями:

```
$ ./build -k st pt st_memprofile st_test pt_test main_fuzz st_test_prod_parser \
           pt_test_prod_parser pty_test_helper unit_tests toml_dump parser_perf \
           core_perf example -j 10                                ; EXIT=1
FAIL $(B)/obj/st_memprofile/lib/shitty/heap_profile.cpp.o
FAIL $(B)/obj/libshitty_vt_core/lib/embed/shitty_vt.cpp.o
FAIL $(B)/main_fuzz
build: 5 node(s) failed, 3 requested target(s) broken
```

`st_memprofile` падает на `fatal error: 'gperftools/heap-profiler.h' file not found` — это средовое, gperftools на машине нет, к мержу отношения не имеет, и на борде эта цель до сих пор не значилась.

Затем — на дереве шага (§4.5), `st_memprofile` исключён как средовой:

```
$ ./build -k st pt st_test pt_test main_fuzz st_test_prod_parser \
           pt_test_prod_parser pty_test_helper unit_tests toml_dump \
           parser_perf core_perf -j 10                            ; EXIT=1
FAIL $(B)/main_fuzz: … linker command failed
build: 1 node(s) failed, 1 requested target(s) broken
```

Ошибок компиляции — **ноль**: единственное вхождение ` error: ` во всём логе это `clang++: error: linker command failed with exit code 1`, то есть линковка `main_fuzz`. Список сломанных целей не вырос.

### 8.6. Пропуск не прячет отказов — четыре состояния

Чтобы проверить это по-настоящему, `example` был собран **руками, минуя граф**: флаг временно поднят, фасад временно адаптирован (пять правок из §4.3), а недостающие два символа добраны линковкой с полной `libshitty`:

```
deps=[libshitty_vt_core, libshitty, libstd, plt, plt_headless, libstd_pic]
$ ./build example -j 10        ; EXIT=0
[LD] {174/174} $(B)/example
```

Собралось и слинковалось. Бинарник сохранён вне дерева, все три временные правки откачены, `git diff --quiet` по `lib/embed/shitty_vt.cpp` чист.

| Состояние `SHITTY_EMBED_EXAMPLE_BINARY` | Результат |
|---|---|
| путь не существует | `Ran 37`, `OK (skipped=37)` |
| файл существует, но не исполняемый | `Ran 37`, `OK (skipped=37)` |
| артефакт есть и **сломан** (`#!/bin/sh` + `exit 1`) | `Ran 37`, **`FAILED (errors=82)`** |
| артефакт есть и рабочий | `Ran 37`, **`OK`** |

Третья строка — прямой ответ на «пропуск, срабатывающий всегда, неотличим от удаления файла»: при существующем артефакте набор исполняется и краснеет ровно так же, как краснел без гварда.

**Четвёртая строка — отдельная находка, и она больше вопроса про гвард.** С собранным фасадом **все 37 тестов апстрима проходят на нашем ядре**. То есть расхождение `M7` целиком линковочное: сам фасад, наш `Vterm` и наши инварианты `A1`/`A8` друг другу не противоречат, и адаптация под нашу сигнатуру — это пять строк, а не переписывание.

---

## Обнаружено

1. **Наш `lib/vterm` не самодостаточен, и цена этого — ровно два символа.** `Composer::contentInsets()` и `Composer::resize()`. До `M7` это было видно только как две строки в разрешении `check_includes.py`; `M7` превращает их в невозможность собрать библиотеку. Раньше это не мешало ничему, потому что все потребители ядра (`st`, `core_perf`, `main_fuzz`, `unit_tests`) линкуют `libshitty` целиком.

2. **Расхождение при этом только линковочное.** Собранный руками `example` проходит все 37 апстримных тестов на нашем ядре (§8.6). Фасад, наш `Vterm` и инварианты `A1`/`A8` друг другу не противоречат — стоило проверить, потому что «сигнатура на два параметра шире» звучит как несовместимость, а оказалось пятью строками.

3. **Адаптация через `Composer` компилируется — и втягивает сорок символов.** Это не довод, а измерение (§4.3): `nm -u composer.o` даёт фонт-стек, `input_router`, `glyph_cache`, `Brand`, `Options`. Вариант «пусть фасад строит свой `Composer`» закрыт этим числом.

4. **`lib/embed` и `bin/example/main.c` не видит ни один сканирующий гвард** — новый корень и незнакомый суффикс. Доказано пробами 6 и 7 (§6.3). Проба «все четыре гварда зелёные над явным нарушением в новом продуктовом каталоге» — самая наглядная за весь мерж, и её стоит повторить в приёмке `T6.1`.

5. **`st_memprofile` сломан и не был назван.** Не мержем: `gperftools` отсутствует в среде. Список сломанных целей на борде («одна — `main_fuzz`») неполон.

6. **`tst/test_embed_example.py` не умел пропускаться.** `EXAMPLE` берётся из окружения и запускается без проверки существования; без бинарника это не skip, а 82 ошибки — 37 методов, часть с `subTest`. Закрыто §4.6.

7. **Объём шага недооценён втрое, и причина — метод, а не невнимательность.** «Пять новых файлов, +784» верно для `git diff --stat … -- lib/embed`; коммит трогает **13 файлов на 1451 строку**, включая цели сборки, CI, `bin/example/main.c`, 37 тестов и рост `VtermState` с одного поля до тринадцати. Это ровно та ошибка, от которой волна 6 научилась лечиться: множество файлов снимать командой по коммиту (`git show --stat`, `git ls-tree`), а не по одному ожидаемому каталогу.

8. **`VtermState` у нас байт в байт апстримный, и второго источника правды он не заводит.** `git diff 60562f22 HEAD -- lib/vterm/vterm.h` по этой структуре пуст. Режимы читаются из приватных полей `VtermImpl` тремя проекциями — `TerminalUpdate` (`vterm.cpp:2541`), `VtermState` (`:2863`) и тестовая `VtermTestState` (`:2882`), — и все три апстримные, ни одна не наша. Дубля, который волна 6 закрывала бы, здесь нет.

9. **`.gitignore` не знает про `example`.** Апстрим завёл цель, которая публикует симлинк `example` в корне репозитория, и правила для него не добавил — все остальные программы (`/st`, `/pt`, `/core_perf`, …) в списке есть. Пока флаг `False`, это ничего не значит; первая же сборка после его подъёма оставит в корне неотслеживаемый бинарник. Строку не добавлял: сегодня она ничего не чинит, и это работа того, кто поднимет флаг.

---

## Исходные данные для задачи А

Всё, что ей нужно знать, поимённо и с номерами строк на состоянии этого шага.

**Что закрыть — два символа, четыре места, все в `lib/vterm/vterm.cpp`:**

| Строка | Обращение | Что вместо |
|---|---|---|
| `6562` | `gridColumns(width, composer.contentInsets(), windowGeometry_.cellPixelWidth)` | инсеты окна через `VtHost`; `gridColumns` переезжает в `lib/vterm` |
| `6569` | `gridRows(height, composer.contentInsets(), windowGeometry_.cellPixelHeight)` | то же |
| `6598` | `composer.resize(min(pixelWidth, UINT16_MAX), min(pixelHeight, UINT16_MAX))` стоит **рядом** с `window->requestResize(pixelWidth, pixelHeight)` двумя строками выше | решить, дублирует ли он `requestResize`; это **строка без владельца**, перенесённая волной 6 |
| `6640` | `const Insets insets = composer.contentInsets();` | `VtInsets` из `lib/vterm/vt_geometry.h`; `Insets` из `composer.h` ядру больше не нужен |

`composer.sessions` на `vterm.cpp:3315` **не входит** в задачу А: он не даёт неопределённого символа (вызов виртуальный) и по разрешению принадлежит `T6.1`.

**Что переезжает:** `gridColumns`, `gridRows`, `gridPixelWidth`, `gridPixelHeight` из `lib/shitty/grid_geometry.h`. Файл создан внутри волны 6, апстримного оригинала у него нет (`T5.2` измерила: `74 0` в `git diff --numstat` против merge-base).

**Что тогда становится возможным:**

- `Vterm::create` теряет `Composer&` и становится десятипараметровой (`lib/vterm/vterm.h:318`);
- `lib/embed/shitty_vt.cpp` адаптируется **пятью правками**, все проверены и все компилируются (§4.3, §8.6): `#include <lib/shitty/composer.h>` не нужен вовсе, `windowResized()` → `paneResized(vt->geometry)`, второй `VtGeometry` для панели, и всё;
- `embed_facade_links = True` возвращает цели, `example` в `deps`/`env` питоновских групп и снимает пропуск в `tst/test_embed_example.py` сам — список в комментарии у флага (`build.py:1065`);
- нарушений границы включений становится **два вместо шести**: закрываются `vterm.cpp` → `composer.h`, `grid_geometry.h` и `mouse_frontend.h` → `composer.h` (тот же `Insets`); остаются `vt_headless.cpp` (три ключа, один факт) и `vterm.cpp` → `session.h`;
- 37 тестов апстрима идут зелёными — проверено, а не предположено (§8.6).

**Чего задача А стоит:** `Vterm::create` — контракт, на который в волне 6 заложились шесть задач. Это её главный риск и причина, по которой `M7` её не делал.

---

## Что осталось `T6.1`

- **Не фиксировать поверхность фасада на состоянии `M7`.** Двадцать четыре коммита между `44d61bfc` и `origin/master` меняют `lib/embed`; `shitty_vt_memory_usage` и `shitty_vt_set_save_lines` приходят в `M8` (`7e2a3c15`), и `tst/test_embed_example.py` вырастет вместе с ними.
- **`composer.sessions` (`vterm.cpp:3315`, `A11`)** — так сказано в самом разрешении `check_includes.py`.
- **Область сканирования гвардов:** `lib/embed` вне `guard_scan_roots` (`build.py:1430`), `.c` вне `guard_scan_suffixes` (`build.py:1431`). Правка того же класса, что делала `T2.1`: корни и суффиксы, не разрешения. Пробы 6 и 7 из §6.3 годятся как приёмка.
- `M7b` (`44d61bfc`, порт Metal) к `lib/embed` не прикасается — проверено `git log` по путям, так что своей частью фасада `T6.1` не занимается вовсе.
