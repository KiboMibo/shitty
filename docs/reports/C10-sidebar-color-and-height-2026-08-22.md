# C10 — цвет сайдбара в конфиг, и сайдбар во всю высоту окна

Ветка `feat/C10-sidebar-color-and-height`, база `master` = `99fdd8d9` (`git rev-parse`).

**Статус: готово.** 11 мутаций внесено, 11 убито, ноль выживших.

## Разведка: три риска задачи 2 уже имеют ответ в коде

| вопрос | ответ, найденный чтением |
|---|---|
| скруглённые углы | радиус ставится на **фрейм-вью** (`platform_cocoa.mm`, `view.superview.layer`) с `masksToBounds = YES`. Сайдбар лежит внутри `contentView`, то есть **внутри маски** — квадратным углом вылезти не может по построению |
| порядок вью и хит-тест | титлбар-контейнер — **брат** `contentView` во фрейм-вью и лежит **выше** него. Сайдбар внутри `contentView`, значит титлбар над ним автоматически, без единой строки про порядок |
| прыжок панели при показе/скрытии оформления | верхний резерв ставится **один раз**, когда режим включается, и наведением не трогается (`ui_csd_tabs.mm`: «no per-hover state to keep»). Значит список стоит на месте, что бы ни делала полоса |
| сетка терминала | верхний резерв — терминала, а не панели. **Не трогаю его вовсе**: меняется только рамка вью сайдбара и отступ его собственного списка |

И главная находка, меняющая цену задачи: **`sidebarTabsRowAt()` — обычная C++-функция**
(`ui_sidebar_tabs.mm:581`), общая для рисования и для кликов именно затем, чтобы они «не могли
разойтись», и у неё **уже есть тесты** (`ui_sidebar_tabs_ut.cpp:227-248`). То есть геометрия списка
проверяема и предъявляема мутацией — вопреки моему ожиданию, что задача 2 целиком Cocoa.

## 1. `sidebarColor`

Имя по конвенции соседей, разбор по образцу `paneDividerColor` (`options.cpp:1125`, `convColor`).

**Умолчание — сегодняшний вывод, а не константа.** Считать его в `options.cpp` целыми числами
нельзя: панель выводится `NSColor blendedColorWithFraction:` в sRGB, и своя арифметика разошлась бы
с ней на байт-другой — то есть умолчание изменилось бы. Поэтому у опции есть признак «задана», и
при незаданной путь остаётся **буквально тем же**, что сегодня. Это проверяемо, а не убедительно.

### Что цвет делает с остальными шестью оттенками

**Оттенки выводятся из заданного цвета** — умолчательный выбор командира, и он верен: панель,
чей фон задан вручную, а подсветка активной строки выведена из `bg`, может разойтись до
нечитаемости.

Механика, сохраняющая умолчание точным. Сегодня все семь — точки на луче `bg → fg`:
панель `0.06`, разделитель `0.30`, линейка `0.16`, активная `0.20`, наведение `0.12`,
текст `0.62`, тусклый текст `0.42`.

Заданный цвет `C` становится началом того же луча, а доли пересчитываются как `(k − 0.06) / 0.94`.
При `C = mix(bg, fg, 0.06)` это тождество: `mix(C, fg, 0.2553) = mix(bg, fg, 0.30)` и так далее для
всех шести. То есть одна формула, а не два набора констант, и непрерывность в точке умолчания —
арифметическая, а не подогнанная.

### Прозрачность заданного цвета — и почему «залить с альфой» снова нельзя

Причина та же, что в `S10`: слой сайдбара лежит поверх `CAMetalLayer`, а тот под полосой уже
нарисовал `opts->bg` с альфой `a`. Сплошная заливка сложилась бы в `2a − a²`.

Сегодня это решено тем, что панель — шестипроцентный оттенок, и она рисуется наложением `fg` с
альфой `0.06`. Для произвольного `C` такого «шести процентов» нет, но обобщение есть:

**рисовать самым тонким слоем, который всё ещё даёт запрошенный цвет поверх фона терминала.**

Для каждого канала нужно `C'·α + bg·(1−α) = C`. Наименьшее допустимое `α` — то, при котором `C'`
упирается в границу диапазона:

```
α = max по каналам |C − bg| / (C > bg ? 255 − bg : bg)
C' = bg + (C − bg) / α
```

Свойства, которые делают это правильным ответом, а не трюком:

- при `C = mix(bg, fg, 0.06)` и `fg` в углу диапазона выходит ровно `α = 0.06`, `C' = fg` —
  **сегодняшняя реализация как частный случай**;
- цвет, близкий к фону, остаётся таким же прозрачным, как терминал;
- цвет, далёкий от фона, **неизбежно кроет плотнее** — это свойство просьбы, а не реализации, и
  оно будет сказано в документации опции, а не только здесь.

Функция — чистая арифметика над `Color`, значит **проверяема тестом и предъявляема мутацией**.
Дом — новый заголовок `lib/shitty/tint_coat.h`: не `render_blend.h` (там премножение рендерера) и
не `ui_window_tint.h` (тот требует AppKit и из набора недостижим).

## 2. Сайдбар во всю высоту

Сегодня (`ui_sidebar_tabs.mm:728-733`) рамка вью укорочена на верхний резерв, и `contentView` не
перевёрнут, поэтому зазор остаётся **сверху** — это и видно.

Правка: **рамка во всю высоту, содержимое списка — по-прежнему ниже резерва.**

Так, а не «список тоже наверх», по причине, записанной в самом файле: под `autoHideChrome` окно
получает `FullSizeContentView`, и верхние строки панели оказались бы **под кнопками окна**. Панель
должна дотянуться до верхней кромки — про строки пользователь не просил, а строка под светофором
неудобна и не нажимается.

Отступ уезжает в `sidebarTabsRowAt()` четвёртым аргументом, чтобы рисование и клики продолжали
читать **одно** число: разъехаться они могут ровно там, где начнут считать его порознь. При нулевом
резерве всё до последнего пикселя как сегодня — и это то, что проверит мутация.

## Тесты

Проверяемо и будет предъявлено мутацией:

- разбор `sidebarColor`: умолчание, значение, отказ на мусор;
- арифметика тонкого слоя: тождество в точке умолчания, границы, недостижимый цвет;
- геометрия списка с отступом: при нулевом — как сегодня, при ненулевом — сдвиг ровно на него,
  и согласие рисования с кликом.

Непроверяемо из стенда и уйдёт в `manual-checks-wave10.md` **формулировками, способными
покраснеть**: не «сайдбар до верха», а «верхняя кромка панели и верхняя кромка окна совпадают,
а полоса заголовка лежит поверх»; не «цвет применился», а «панель заданного цвета, и оттенки
согласованы с ним, а не с фоном».

## Что вышло на деле

### `sidebarColor`

Разбор по образцу `paneDividerColor`. Отличие одно, и оно решило форму опции: **разрешать в
умолчание здесь нечего**. Панель по умолчанию — шесть процентов переднего плана в фоне, и смешивает
их AppKit в sRGB через `NSColor`; целочисленный пересчёт в `options.cpp` разошёлся бы с ним на
байт-другой, то есть умолчание изменилось бы. Поэтому у опции есть признак `sidebarColorSet`, а
неспрошенный сайдбар идёт **буквально прежним путём**.

Оттенки едут за цветом. Луч не переписан вторым набором констант, а **переоснован**: все семь —
точки на одном луче `bg → fg`, панель стоит на `0.06`, поэтому при переносе начала луча в панель
оттенок, стоявший на `k`, встаёт на `(k − 0.06) / 0.94`. В точке умолчания это тождество —
`mix(panel, fg, 0.2553)` есть `mix(bg, fg, 0.30)`. Ветка «опция не задана» при этом оставлена
прежней: тождество точно в вещественной арифметике, а `NSColor` смешивает во float, и умолчание,
разошедшееся на байт, всё равно было бы изменившимся.

### Прозрачность заданного цвета

Обобщение того, что `S10` делал руками, и оно оказалось не трюком, а правилом.

Слоя, дающего ровно альфу окна, **не существует**: подложка (фон терминала под полосой) уже даёт
`a`, любая добавка только прибавляет, и `2a − a²` — это 0.75 там, где тело окна 0.5. Поэтому
вопрос не «как совпасть», а «как мало можно добавить». Ответ точен: толкнуть цвет слоя к краю
диапазона, и нужная альфа падает пропорционально.

`thinnestCoat()` (`lib/shitty/tint_coat.h`). На теме проекта для панели по умолчанию выходит
**альфа 14/255** там, где ручной оттенок давал 15/255, и обе дают **один видимый байт**. То есть
умолчание прошло через новую функцию, а не мимо неё, и осталось тем же на глаз — это число названо
здесь, а не оставлено на обнаружение.

Обратный ход `coatOverOpaque()` — **независимая инверсия**, поэтому тест проверяет, а не
пересказывает. Утверждается **равенство, не допуск**: измерено отдельной единицей трансляции до
написания теста — на 2 424 832 тройках худшая ошибка **нуля**.

Цена, которую надо знать: цвет, далёкий от фона, тонким слоем недостижим, и панель неизбежно кроет
плотнее. Это свойство просьбы, а не реализации; сказано в тексте опции, в примере конфига и в
пункте `V14`, где у человека есть проверка обеих сторон.

### Высота

Рамка вью больше не укорачивается на верхний резерв. Зазор оставался именно сверху — `contentView`
не перевёрнут, укороченная рамка теряет верх, — и это то, что видел пользователь.

**Верхний резерв не тронут**, и это несущая половина. Он терминала, `contentInsets()` держит по
нему сетку, и обнуление увело бы текст под титлбар. `V2` однажды получила указание его обнулить,
проверила и отказалась — она была права, и здесь то же самое.

Список по-прежнему начинается ниже резерва: под `autoHideChrome` окно получает
`FullSizeContentView`, и верхняя строка оказалась бы под кнопками окна. Отступ уехал **четвёртым
аргументом в `sidebarTabsRowAt()`** — в ту самую функцию, которая существует затем, чтобы рисование
и клики не могли разойтись. Два вызывающих, каждый со своим вычитанием, — это ровно способ
разойтись.

### Три риска, названные командиром, — чем закрыт каждый

| риск | ответ, и он в коде, а не в намерении |
|---|---|
| сетка терминала поедет | резерв не тронут вовсе; меняются только рамка панели и отступ её собственного списка |
| порядок вью и хит-тест | титлбар-контейнер — **брат** `contentView` во фрейм-вью и лежит выше него. Порядок не задаётся ни одной строкой: он уже такой |
| скруглённые углы | радиус с `masksToBounds` стоит на **фрейм-вью** (`platform_cocoa.mm`), панель лежит внутри маски и вылезти не может |
| прыжок при показе/скрытии полосы | верхний резерв ставится один раз при включении режима и наведением не трогается (`ui_csd_tabs.mm`: «no per-hover state to keep») — значит список стоит |

Первые три из четырёх я не реализовывала: они уже верны по построению. Это записано, потому что
«ничего не пришлось делать» и «не посмотрел» выглядят в отчёте одинаково.

## Мутации: 11 внесено, 11 убито, 0 выжило

Базис печатает число тестов и число чёрноящичных прежде первой мутации; во всех прогонах
`TOTAL = 952` (суита `Pty::` исключена, см. отчёт T10) и `passed = 51`, **ни разу не потеряно ни
одного теста**. Откат — `git checkout -- <точный путь>`. Тесты закоммичены до мутаций.

| # | мутация | на чём краснеет |
|---|---|---|
| `MA` | слой всегда полной толщины | `coat.alpha >= 10 && coat.alpha <= 20` |
| `MB` | толщину задаёт ближайший канал, а не дальний | `got == want` (обратный ход) |
| `MC` | цвет слоя не толкается к краю | `got == want` |
| `MD` | запас считается без направления | `got == want` |
| `ME` | цвет, равный подложке, всё равно красится | `thinnestCoat(bg, bg).alpha == 0` |
| `MF` | признак «задана» никогда не ставится | `TheSidebarColourComesFromTheConfigAndTheCommandLine` + отказ на мусор |
| `MG` | значение цвета игнорируется | те же два |
| `MH` | мусор принимается | те же два |
| `MI` | список игнорирует отступ | `sidebarTabsRowAt(tall, inset - 0.5, 3, inset) == -1` |
| `MJ` | отступ забыт в нижней отсечке | `sidebarTabsRowAt(clipped, inset + top + row*2, 3, inset) == -1` |
| `MK` | отступ применён дважды | сверка с ответом без отступа |

## Числа

Сборка и измерение — разными командами, код возврата взят у сборки.

| прогон | на `99fdd8d9` | после |
|---|---|---|
| `./build -j 8 st st_test pt unit_tests pty_test_helper` | `exit 0` | **`exit 0`** |
| `unit_tests --threads=1` | `OK: 951` | **`OK: 959`** |
| `MallocScribble=1 … -Pty:: --threads=1` | `OK: 944, SKIP: 7` | **`OK: 952, SKIP: 7`** |
| `pytest tst/test_config.py tst/test_tabs.py -q` | `51 passed` | **`51 passed`** |

Восемь новых тестов: 4 арифметики слоя, 3 разбора опции, 1 геометрии списка. `951 + 8 = 959`.

Четыре из восьми — арифметика слоя — стоят **вне** `#if defined(__APPLE__)`, под которым лежит
остальной набор сайдбара: это чистые вычисления, а тринадцатый слепой прибор плана ослеп ровно по
условию сборки. Три теста опции живут в `Options`, который тоже собирается везде. Один
геометрический — внутри darwin-условия, потому что сама функция в `.mm`; это досталось по
наследству, а не заведено мной.

## Человеку — `V13`–`V16`

В `docs/plans/state/manual-checks-wave10.md`, и каждый сформулирован так, чтобы **мог покраснеть**:

- `V13` — не «цвет применился», а «**оттенки согласованы с ним**»: панель коричневая при синеватой
  подсветке активной строки — второй, тихий дефект, на котором формулировка «панель заданного
  цвета» была бы зелёной;
- `V14` — цвет **и** прозрачность вместе, судить по стыку; плюс обратная сторона: белая панель
  **обязана** стать плотнее, и если не стала — цвет до неё не доехал;
- `V15` — не «панель до верха», а «**верхняя кромка панели и верхняя кромка окна совпадают, а
  полоса заголовка лежит поверх**», плюс оба состояния оформления и проверка, что список не
  прыгает и не лезет под кнопки окна;
- `V16` — скруглённый угол quick-окна; ожидается зелёным без правок, заведён затем, чтобы
  «проверено по коду» не осталось утверждением по чтению.
