# F9: видимый разделитель панелей и разбор `cmd+shift+d`

- **Дата начала:** 2026-08-21
- **Ветка:** `feat/F9-divider`, дерево `/tmp/f9`, базис `c44d4e90`
- **Источник:** наблюдение пользователя живьём — «`cmd+d` делит, но разделителя не видно;
  толщину и цвет хочу в конфиг. `cmd+shift+d` не работает».
- **Базис лида на этой голове:** чистый `OK: 922`, под заливкой (`-Pty::`) `OK: 915, SKIP: 7`.
  Снимаю своё число сам.

## Статус

В работе.

## Шаг 1: диагностика `cmd+shift+d` — измерением

Зонд на существующем стенде (`session_ut.cpp`, собран, прогнан, **откачен**). Он шлёт оба
чорда так, как их шлёт платформа — через цепочку обработчиков, то есть **через
`InputBindings`**, а не публикацией слушателям, — и печатает, что вышло.

```
F9PROBE cmd+d 'd'          -> panes=2
F9PROBE cmd+shift+d 'd'    -> panes=3
F9PROBE cmd+shift+d 'D'    -> panes=3      <- не разделил: было 3, осталось 3
F9PROBE   pane 0: x=0 y=0 w=40 h=24
F9PROBE   pane 1: x=40 y=0 w=40 h=12
F9PROBE   pane 2: x=40 y=12 w=40 h=12
F9PROBE   shell 0: cols=40 rows=24
F9PROBE   shell 1: cols=40 rows=12
F9PROBE   shell 2: cols=40 rows=12
```

### Что этим установлено

1. **Отказа нет.** `PaneTree::split()` направление вообще не различает (`pane_layout.cpp`,
   оно только сохраняется в узле), `place()` симметричен по осям. При совпавшей привязке
   горизонтальный сплит **происходит и правильный**: панели 1 и 2 сложены по вертикали
   (`y=0 h=12` и `y=12 h=12`), и **`SIGWINCH` дошёл до обоих шеллов** — `rows=12` у каждого.
   Гипотеза «дерево отвергает деление по высоте» закрыта измерением.
2. **Привязка совпадает с базовым кодом `'d'` и не совпадает с `'D'`.** Третья строка
   зонда — нажатие с базовым кодом `'D'` — не создало четвёртой панели. Если фронтенд
   отдаёт под Shift **сдвинутый** код, чорд молча не делает ничего.
3. **Ни один тест не гоняет чорды сплита через таблицу привязок.** Юнит-тесты зовут
   `harness.splitHorizontal()`, а это `publish(composer.splitHorizontalListeners)` —
   мимо `InputBindings`. В чёрноящичном наборе (`tst/harness.py`) хелпера для чордов
   сплита нет вовсе. То есть **мёртвая привязка проходит все 922 зелёных теста**, и
   именно такой формы дефект пользователь и наблюдает.

### Чего измерением установить не удалось, и почему

Какой именно базовый код отдаёт настоящий Cocoa под `cmd+shift+d`. По чтению кода он
должен быть `'d'`: коррекция в `platform_cocoa.mm` срабатывает как раз при взведённом
`InputShift` и берёт **несдвинутый** символ клавиши через `UCKeyTranslate` с нулевым
состоянием модификаторов. Но проверить это можно только на живом окне с настоящим
`NSEvent`, а ввод в чужие приложения я не автоматизирую.

**Поэтому обе оставшиеся версии остаются открытыми, и я закрываю обе:**

- **версия «привязка мертва»** — закрывается второй формой привязки, ровно тем приёмом,
  который в этой же таблице двумя строками ниже уже применён к `PrevTab`/`NextTab` с
  комментарием «фронтенды расходятся в том, сохраняет ли базовый код сдвинутой скобки
  сдвиг»;
- **версия «работает и не видно»** — закрывается пунктом 2 этой задачи.

Так что «чем оказался `cmd+shift+d`» честный ответ такой: **на уровне модели он
работает — это измерено; наблюдаемый отказ объясняется либо базовым кодом под Shift,
либо невидимостью шва, и различить их без живого окна я не могу.** Закрыты оба пути.

## План работ

### Пункт 1 — разбор `cmd+shift+d`, диагностика раньше починки

Проверять буду измерением, а не глазами. Что уже проверено лидом по коду и мною не
перепроверяется: чорд привязан (`input_bindings.cpp:71`), `session.cpp:1209` зовёт
`splitFocused(SplitDirection::Horizontal)`, `splitFocused()` направление не различает.

Значит развилка ниже, и гипотез две:

1. **Отказ** — `PaneTree::split()` отвергает деление по высоте.
2. **Невидимость** — панели создаются, но зазор нулевой и полосы нет, так что
   горизонтальный сплит читается как одна панель с продолжающимся текстом.

Различаю измерением: сколько панелей в дереве, какие прямоугольники они получили, дошёл
ли новый размер до обоих шеллов (`PtySize`, то есть `SIGWINCH`). Если гипотеза 2 —
говорю прямо, и пункт закрывается пунктом 2.

### Пункт 2 — разделитель виден, толщина и цвет в конфиг

Опций разделителя сегодня нет; зазор во всех шести продуктовых вызовах `layout()`
передаётся литеральным нулём.

- **толщина в пикселях** — она же зазор раскладки;
- **цвет** — по образцу разбора цветов в `options.cpp` (`bg`, `fg`, `cr`);
- **отрисовка** — найти слой, который зазор заполняет; если тянет за собой чужие файлы,
  спросить лида до правки.

**Умолчания заданы лидом:** толщина **1** пиксель, цвет производный от палитры темы, а не
константа. Сегодняшний вид не должен меняться сильнее чем на этот пиксель — апстрим чужой.

**Зона захвата шва** (найдено `R8-test`) — полглифа в каждую сторону, от толщины не
зависит. Отдельно разобрать и написать, как соотносится с новой толщиной: единица не
должна сделать шов неухватываемым, десятка — не должна удваивать зону против ожидания.

**`A10` / открытый вопрос №6:** нулевой зазор был умолчанием, объявленным
пересматриваемым. Эта задача его и пересматривает — отметить, что вопрос закрыт.

## Правила прогона

- Сборка и измерение — разными командами; код возврата беру у сборки, а не у конвейера.
- Число тестов сверяю с ожидаемым до объявления успеха.
- Тесты коммитятся до мутаций; мутация откатывается по конкретному пути файла.
- Каждый тест предъявляется мутацией.
