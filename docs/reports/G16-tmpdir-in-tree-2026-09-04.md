# `G16`: `TMPDIR` внутри дерева роняет `ui_sidebar_tabs_ut.cpp:528`

Ветка `fix/G16-tmpdir-in-tree` от `master` (`28d1b5e0`). Тронут один файл:
`lib/shitty/ui_sidebar_tabs_ut.cpp`. `build` и `build.py` не менялись —
обоснование в §3.

Порядок разделов здесь соответствует порядку работы: §1–§3 написаны и
закоммичены **до** правки, §4–§6 — после.

---

## 1. Что охраняет утверждение на строке 528

```cpp
// No repository above the temp root at all. This is the only
// answer the row may render as "no git", and it has to be
// reachable or the row would say it about everything.
STD_INSIST(!sidebarTabsBranch(StringView(root), out));
```

`sidebarTabsBranch()` (`lib/shitty/ui_sidebar_tabs.mm:413`) идёт вверх от
каталога до первого `.git`, разбирая по дороге и каталог, и файл-ссылку
worktree, и относительную ссылку подмодуля. У функции **два разных `false`**, и
они намеренно разные (комментарий на `.mm:289-294`):

- `sidebarTabsDirectory()` возвращает `false` = «каталога не добыть» — строка
  рисует пустоту;
- `sidebarTabsBranch()` возвращает `false` = «каталог есть, репозитория над ним
  нет» — и **только это** строка рисует как `no git` (`.mm:709`).

Утверждение на 528 — единственное место набора, где второй `false` достигается
**самим обходом**: циклом, который дошёл до `/` и сдался (`.mm:462-464`).
Соседние `!sidebarTabsBranch(".")`, `("lib/shitty")` и `()` до обхода не
доходят вовсе — они отсекаются гвардом относительного пути на `.mm:418`, и
комментарий над ними это прямо говорит. Значит:

- убрать 528 — снять единственное покрытие ветви «обход дошёл до корня и
  вернул `false`»;
- вместе с ней — единственное свидетельство, что обход вообще **завершается**:
  без `if (here.length() <= 1) return false;` цикл на `/` крутится вечно.

Охрана настоящая. Ослаблять её нельзя.

**Но её посылка — про машину, а не про код.** Утверждение говорит «над этим
корнем нет репозитория», а корень выдаёт `makeTempDir()`
(`ui_sidebar_tabs_ut.cpp:183-187`), который берёт `TMPDIR` **на веру**:

```cpp
const char* const directory = getenv("TMPDIR");
dir << StringView(directory != nullptr ? directory : "/tmp") << StringView(u8"/ui_sidebar_tabs_ut.XXXXXX");
STD_INSIST(mkdtemp(dir.cStr()) != nullptr);
```

Условия, при которых утверждение осмысленно, тест не устанавливает и не
проверяет. Когда они не выполняются, `sidebarTabsBranch()` отвечает **верно**
(репозиторий над корнем действительно есть), а падает — тест.

## 2. Что даёт сборка и зачем

`build:2145`:

```python
env["TMPDIR"] = str(work)
```

где `work = self.tmp / node.uid` (`build:2112`), `self.tmp = cache_root/"tmp"`
(`build:1996`), а `cache_root` — либо `context.build_root`, либо
`source_root / args.cache_dir` (`build:2600-2604`). То есть `TMPDIR` **всегда**
внутри дерева исходников, по построению: флага, который вынес бы его наружу,
нет.

`work` — это песочница узла, и `TMPDIR`, указывающий на неё, даёт четыре вещи:

1. **Уборка.** Содержимое `work` стирается в `finally` после узла
   (`_discard_contents`, `build:2136`). Временный файл инструмента исчезает
   вместе с узлом, а не копится в системном `/tmp`.
2. **Изоляция.** Каталог адресуется `node.uid`, под flock'ом
   (`build:2115-2118`). Параллельные узлы не видят временных файлов друг друга,
   и одинаковые имена не сталкиваются.
3. **Одна файловая система с CAS.** Объявленные выходы уносятся в кеш
   `os.link()`'ом (`_store_path`), с копией через `shutil.copy2` только по
   `EXDEV`. `work` рядом с `cas/` — это условие, при котором быстрый путь
   работает.
4. **Герметичность.** Ни один узел не зависит от того, что за `TMPDIR` у
   хозяина машины и что в нём лежит.

Мусор в кеш при этом не попадает: `_store()` обходит **только** `node.outputs`,
всё остальное в `work` пропадает. То есть «`TMPDIR` внутри дерева» — не
небрежность, а несущая конструкция раннера.

## 3. Почему чинится тест, а не сборка

Три довода, по возрастанию веса.

**Довод 1 — размер поражения.** Правка сборки трогает `TMPDIR` **каждого** узла
графа из 3254 узлов (`./build test` в этом дереве) ради одного утверждения в одном тесте. Вынести `TMPDIR`
наружу — значит либо потерять все четыре свойства из §2, либо завести второй
временный каталог со своей уборкой: новая сущность в раннере, у которой одна
причина существовать — чужое допущение.

**Довод 2 — «внутри дерева» здесь нормальный режим, а не авария.** Репозиторий
сам ставит `TMPDIR` внутрь рабочего каталога в двух своих обвязках:
`tst/realworld/generate.py:189` (`TMPDIR={fixture}/tmp`), там же `:262`, и
`tst/xterm_vttests/adapter.py:176`. Тест, который ломается от такого `TMPDIR`,
ломается от штатного приёма этого же репозитория.

**Довод 3, решающий — посылка неверна независимо от сборки.** `TMPDIR` внутри
репозитория — обычная конфигурация разработчика: `TMPDIR=$HOME/tmp` при
версионируемом `$HOME` (dotfiles-репозиторий) даёт ровно тот же отказ, и
сборку здесь винить не в чем. Починка сборки **зеленит сегодняшний отказ и
оставляет мину**: тест продолжит утверждать про машину то, чего он о ней не
знает, и выстрелит у первого же человека с таким `TMPDIR` — а выглядеть это
будет как дефект панели.

Отсюда решение:

> Чинится **тест** — но не снятием утверждения, а **установлением его
> посылки**. `makeTempDir()` обязан выдать корень, над которым репозитория
> действительно нет, и громко сказать, если такого корня не нашлось, — вместо
> того чтобы утверждать неправду про тот каталог, который случайно назван в
> `TMPDIR`.

Проверка «есть ли репозиторий над кандидатом» пишется **независимо**, на
`stat()`, а не через `sidebarTabsBranch()`. Иначе получается круг: код,
который тест собирается судить, выбирал бы себе площадку — и мутация «всегда
возвращать `false`» проходила бы мимо этого утверждения.

---

## 4. Правка

`lib/shitty/ui_sidebar_tabs_ut.cpp`, коммит `39165510`. Два изменения и один
комментарий:

1. `outsideAnyRepository(StringView)` — новая функция в безымянном
   пространстве файла. Идёт вверх от абсолютного каталога, на каждом уровне
   делает `stat("<уровень>/.git")`, возвращает `true`, если дошла до `/` и
   ничего не нашла. Написана на `stat()`, **не** через `sidebarTabsBranch()`:
   иначе площадку теста выбирал бы тот самый код, который тест судит, и
   `sidebarTabsBranch()`, застрявший на `false`, обеспечивал бы себе алиби.
2. `makeTempDir()` перебирает кандидатов — `$TMPDIR`, затем `/tmp` — и берёт
   **первого**, над которым репозитория нет. Если не подошёл ни один,
   `STD_INSIST(false)`: «здесь этот тест поставить не на что» — честный отказ,
   в отличие от утверждения неправды про машину.
3. Комментарий над строкой 528 (теперь 588) говорит две вещи, которых там не
   было: что это единственное утверждение, доходящее до ветви отказа обхода, и
   что отсутствие репозитория над корнем — работа `makeTempDir()` и проверено
   там, а не предположено про `TMPDIR`.

`ui_sidebar_tabs.mm` не тронут: код был прав до правки и остался прав.

## 5. Чем доказано, что охрана цела

Четыре пробы. Первые две — кодом, в `.mm` и в самом тесте; вторые две —
окружением, без правки кода. Все прогоны — `TMPDIR` **внутри дерева**, то есть
в тех самых условиях, где раньше падало.

### Проба A. Ответ «нет репозитория» сделан недостижимым

`lib/shitty/ui_sidebar_tabs.mm:462`, ветвь «обход дошёл до `/` и сдался»,
заменена на выдачу ветки:

```c
if (here.length() <= 1) {
    static const StringView probe(u8"main");
    out.append(probe.data(), probe.length());
    return true;
}
```

```
$ TMPDIR="$PWD/.build/tmp/g16probe" SHITTY_PTY_TEST_HELPER=… ./.build/unit_tests --threads=1 < /dev/null
EXIT=1
!sidebarTabsBranch(StringView(root), out) failed, at lib/shitty/ui_sidebar_tabs_ut.cpp:592
OK: 960, ERR: 1
```

Утверждение красное. То есть после правки оно по-прежнему ловит ровно то, ради
чего написано: недостижимость единственного ответа, который строка рисует как
`no git`. Проба откачена (`git checkout lib/shitty/ui_sidebar_tabs.mm`).

### Проба B. Новая проверка снята — и падение возвращается

`outsideAnyRepository()` укорочена до `return true` сразу после проверки
абсолютности, то есть `makeTempDir()` снова берёт `TMPDIR` на веру:

```
$ TMPDIR="$PWD/.build/tmp/g16probe" … ./.build/unit_tests --threads=1 < /dev/null
EXIT=1
!sidebarTabsBranch(StringView(root), out) failed, at lib/shitty/ui_sidebar_tabs_ut.cpp:593
OK: 960, ERR: 1
```

Проверка несущая, а не декоративная: без неё тест падает тем же отказом, что
и до задачи. Проба откачена.

### Проба C1. Пригодный `TMPDIR` по-прежнему используется, а не обходится

`TMPDIR=/tmp/g16-ro-outside` (вне репозитория, права `500`). Если бы правка
просто перестала читать `TMPDIR` и всегда брала `/tmp`, тест прошёл бы:

```
$ TMPDIR=/tmp/g16-ro-outside … ./.build/unit_tests --threads=1 < /dev/null
EXIT=35
mkdtemp(dir.cStr()) != nullptr failed, at lib/shitty/ui_sidebar_tabs_ut.cpp:240
```

`mkdtemp` упал **в названном каталоге** — значит кандидат `TMPDIR` был выбран.
(Остальные 34 отказа — другие наборы, которым нечитаемый `TMPDIR` тоже
некстати; к этому файлу они не относятся.)

### Проба C2. Откат срабатывает по репозиторию, а не по правам

`TMPDIR=$PWD/.build/tmp/g16-ro-inside`, права те же `500`, но каталог **внутри
дерева**:

```
$ TMPDIR="$PWD/.build/tmp/g16-ro-inside" … ./.build/unit_tests --threads=1 < /dev/null
EXIT=34
$ grep -c "ui_sidebar_tabs_ut.cpp:" лог
0
+ SidebarTabsUi::TheBranchIsFoundFromASubdirectoryAndThroughEveryKindOfDotGit
```

Наш тест зелёный, хотя каталог нечитаем: до `mkdtemp` дело не дошло, кандидат
отвергнут проверкой на репозиторий, взят `/tmp`. Пара C1/C2 разделяет две
причины отката — «внутри репозитория» и «не пишется» — и показывает, что
работает первая.

## 6. Пять гвардов, каждый сканирующий доказан пробой кодом

Гварды исполнены **прямым запуском программы**, минуя `./build` и его штамп из
CAS (`CLAUDE.md`, «Гварды и кеш сборки»): четыре сканирующих вытащены из
`build.py` через `ast`, `vterm_boundary` — это `lib/vterm/check_includes.py`.
Пробы поставлены **кодом**: `blanked()` вычищает тела комментариев, и
проба-комментарий зеленила бы все четыре разом.

Пробу ставил в тот файл, который соответствующий гвард действительно читает
(`CLAUDE.md`, «Гварды читают не все файлы»).

| Гвард | Эталон | Файл пробы | Проба |
|---|---|---|---|
| `border_pixels` | `EXIT=0` | `lib/shitty/ui_sidebar_tabs_ut.cpp` (мой файл) | `borderPixels()` → `Unallowed uses: …ui_sidebar_tabs_ut.cpp:249`, `EXIT=1` |
| `mouse_geometry` | `EXIT=0` | `lib/shitty/ui_sidebar_tabs.mm` — гвард пропускает `_ut.cpp` | `mouseGeometry(composer)` → `…ui_sidebar_tabs.mm:331`, `EXIT=1` |
| `pane_grid` | `EXIT=0` | `lib/shitty/render_reference.cpp` — гвард читает только `render*` | `composer.columns` → `…render_reference.cpp:17`, `EXIT=1` |
| `darwin_call` | `EXIT=0` | `lib/shitty/ui_sidebar_tabs_ut.cpp`, **вне** `#if defined(__APPLE__)` | `createSidebarTabsUi(...)` → `…ui_sidebar_tabs_ut.cpp:42`, `EXIT=1` |
| `vterm_boundary` | `EXIT=0` | `lib/vterm/mouse_frontend.cpp` | `#include "composer.h"` → `mouse_frontend.cpp:8`, `EXIT=1` |

Каждая проба откачена `git checkout`, после отката все пять снова `EXIT=0`.
`git status` чист.

## 7. Критерии

| № | Критерий | Вывод | |
|---|---|---|---|
| 1 | Решение обосновано | §1–§3, закоммичены до правки (`c6baff7d`) | ✅ |
| 2 | `./build test` доходит до конца | `-k`: `1854` узлов исполнены, `build: 24 node(s) failed` — **все 24** это `python-tests*/group-NN`, несущие 20 известных красных питоновских тестов (§7.2 ниже). Ни один другой узел не красный. В первом прогоне без `-k` граф дошёл до `{1160/3254}`, пройдя все 20 узлов `unit_tests` | ✅ с оговоркой |
| 3 | `OK: 961` при `TMPDIR` внутри дерева | `TMPDIR="$PWD/.build/tmp/g16probe" … ./.build/unit_tests --threads=1 < /dev/null` → `OK: 961`, `EXIT=0`. До правки на том же бинарнике — `OK: 960, ERR: 1` | ✅ |
| 4 | Охрана не снята | §5: пробы A, B, C1, C2 | ✅ |
| 5 | `./build st --clear` | `{228/228} $(B)/st`, `EXIT=0` | ✅ |
| 6 | Полный питоновский набор | `Ran 6399 tests in 125.045s`, `FAILED (failures=6, errors=14, skipped=17, expected failures=549)`, `EXIT=1` — 20 красных, поимённо совпадают с эталоном `master` | ✅ |
| 7 | Пять гвардов | §6 | ✅ |

### 7.1. Критерий 2, дословный вывод

```
$ ./build test -k
…
[UT] {241/3254} $(B)/unit-tests/group-00.stamp        (все 20 групп, exit 0)
…
build: 24 node(s) failed, 24 requested target(s) broken
EXIT=1
```

Двадцать узлов `unit_tests` в первом, некешированном прогоне:

```
$ grep -oE "test command finished .*unit_tests --group=[0-9]+" лог
группа 0 exit 0; группа 1 exit 0; … группа 19 exit 0
```

### 7.2. Почему `./build test` красный и почему это не `G16`

Двадцать четыре красных узла — это `python-tests/group-{03,04,05,06,07,08,10,12,13,16,17,18}`
и те же двенадцать в `python-tests-prod-parser`. Все они несут те самые
**20 красных питоновских тестов**, которые красны и на `master`:
`test_bitmap_font_render` ×9, `test_synthesized_symbols` ×2, `test_soft_render` ×2,
`test_font_resolver` ×2, `test_contour_input_generator` ×2, `test_italic_overhang` ×1,
`test_ghostty_key_encoding_tail` ×1, `test_color_font_render` ×1 — поимённо тот же
список, что в эталоне `T5.1`/`T5.4`.

Отдельный прогон `master` не ставился, и вот почему это не срезанный угол:
правка целиком в `lib/shitty/ui_sidebar_tabs_ut.cpp`, а этот файл входит
**только** в `unit_tests` (`build.py:1044`, `build.glob("$(S)/lib/shitty/*_ut.cpp")`);
питоновский набор гоняет `st_test`, `pt_test` и `toml_dump`, ни один из которых
`*_ut.cpp` не компилирует. Продуктовые байты у меня и у `master` те же.

---

## Обнаружено

**1. Следующий за `unit_tests` блокер `./build test` — не блокер, а известный
питоновский эталон.** После этой правки граф `test` проходит **до конца**:
единственные красные узлы — двадцать четыре `python-tests*/group-NN`, несущие
те 20 тестов, которые красны и на `master` (шрифтовой рендер и два
генератора). То есть цель `test` целиком зелёной на этой машине не бывает и не
бывала; «красный CI» после `G16` читать как регрессию нельзя. Стоит решить
отдельно, чем это должно быть — списком `xfail`, как у соседних наборов, или
признанной средой; сегодня это 24 узла, о которых надо помнить наизусть.

**2. `wraptest` мигает под параллельной нагрузкой.** В первом прогоне
`./build test` узел `wraptest_04_cr_works_at_margin` упал с
`RuntimeError: test child tty has no path` — это `lib/shitty/test_mode.cpp:2728`,
`ttyname(io[1])` вернул `nullptr`. Тот же узел в одиночку — `PASS` за `0.287s`,
и в полном прогоне с `-k` он зелёный. Признак исчерпания псевдотерминалов при
большом `-j`, а не дефекта случая. `test_mode.cpp` принадлежит `T5.2`, я его не
трогал; если решать — то там, и «мигает раз в N прогонов» стоит записать до
того, как это увидят как случайную красноту CI.

**3. Ещё четыре теста читают `TMPDIR` так же доверчиво.**
`quick_companion_ut.cpp:29,40`, `quick_frame_store_ut.cpp:29`,
`application_ut.cpp:46`, `options_ut.cpp:29`. Сегодня ни один из них не падает:
никто, кроме сайдбара, не утверждает ничего про то, что лежит **над** временным
корнем — им нужен только доступный на запись каталог. Так что чинить там нечего,
но класс отказа теперь назван: утверждение про окружение временного корня
обязано это окружение устанавливать. `application_ut.cpp` принадлежит `T5.2` и
не тронут.

**4. `./build <цель>` оставляет в корне дерева симлинк с именем цели.**
После `./build wraptest_04_cr_works_at_margin` в корне появился неотслеживаемый
`wraptest_04_cr_works_at_margin -> .build/tst/wraptest/…stamp`. Публикация
целей (`context.publish`) делает это для любой одиночной цели, и такой файл
легко уехать в коммит вместе с правкой. Удалён; стоит либо игнорировать этот
шаблон, либо публиковать только в `.build`.

**5. `--clear` стирает кеш целиком, а не кеш цели.** `build:2605-2607` сносит
`cas`, `uid`, `tmp`, `grb` у `cache_root`, то есть проверка одной цели через
`--clear` обнуляет работу всех остальных. У `./build` есть `--cache-dir`
(`build:2600-2602`), задающий отдельный корень кеша под `source_root`, — им
такую проверку можно ставить, ничего не теряя. В `CLAUDE.md` записан только
`--clear`; про его цену там не сказано.
