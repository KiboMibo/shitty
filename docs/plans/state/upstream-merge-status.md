# Статус: поэтапный мерж 57 коммитов апстрима

- **План:** `docs/plans/2026-08-29-upstream-merge.md`
- **Оценка-разведка:** `docs/research/2026-08-29-upstream-merge-assessment.md`
- **Архитектура:** `docs/architecture/2026-08-18-panes-and-window-chrome.md` — инварианты `A1`–`A11`
- **Репозиторий:** форк `KiboMibo/shitty` (remote `fork`), апстрим `pg83/shitty` (remote `origin`) · рабочая ветка `master`
- **Начато:** 2026-08-30
- **Обновлено:** 2026-08-30, волна 1 закрыта
- **Общий статус:** волны 1–3 закрыты и запушены, волна 4 на ревью, `behind` 57 → 45

## Предыстория

Исполняется **после** плана `pane-frame-stall`, закрытого полностью 2026-08-30. Его борд — `docs/plans/state/pane-frame-stall-status.md`, итоговый отчёт — `docs/reports/commander-pane-frame-stall-2026-08-30.md`. Оттуда унаследованы находки про среду, которые внесены в брифы всех агентов этого плана (см. ниже).

Состояние на старте: `master` **ahead 606, behind 57** относительно `origin/master`. Точка расхождения — `8b28e8a9`.

## Решения пользователя

| Вопрос | Решение |
|---|---|
| Объём | Весь апстрим, 8 волн, поэтапно по группам коммитов (стратегия «б» из §5 оценки) |
| `lib/embed` | Просто втащить и собирать, своей работы поверх не делать |
| Режим | Автономно до волны 6, задача `T5.1` — там точка остановки за решением человека |
| Порядок | После фикса `pane-frame-stall` и после починки гварда |

## Волны

| Волна | Задачи | Статус | Ветка волны | Смержено в master |
|---|---|---|---|---|
| 1 | `T0.1`, `T0.3` | **закрыта** | `wave/upstream-merge-w1` | да |
| 2 | `M1` | **закрыта** | `wave/upstream-merge-w2` | да |
| 3 | `M2`, `M3`, `T2.1` | **закрыта** | `wave/upstream-merge-w3` | да |
| 3-ревью | `R3-test` | не начата | — | — |
| 4 | `M4`, `T3` (слита из `T3.1`–`T3.3`), `T3.4` отменена | реализация закрыта, идёт ревью | `wave/upstream-merge-w4` | — |
| 4-ревью | `R4-test`, `R4-arch`, `R4-qa` | идёт | — | — |
| 5 | `M5`, `T4.1`–`T4.3` | не начата | — | — |
| 5-ревью | `R5-test` | не начата | — | — |
| 6 | `M6`, `T5.1`–`T5.11` | не начата | — | **точка остановки на `T5.1`** |
| 6-ревью | `R6-*`, `F6` | не начата | — | — |
| 7 | `M7`, `T6.1`, `M8`, `T6.2`–`T6.4` | не начата | — | — |
| 7-ревью | `R7-test` | не начата | — | — |
| 8 | `T7.1`–`T7.3` | не начата | — | — |

## Задачи

| ID | Что | Статус | Ветка | Отчёт | Вердикт |
|---|---|---|---|---|---|
| `T0.1` | Эталонная зелёная линия на текущем master | готово | `wave/upstream-merge-w1` | `docs/reports/T0.1-baseline-2026-08-30.md` | эталон снят, мигающих нет по пяти прогонам |
| `T0.3` | Чек-лист инвариантов `A1`–`A11` | готово | `wave/upstream-merge-w1` | `docs/plans/reviews/upstream-merge-invariants.md` | 11 из 11 проверяются; мерж целится в пять |
| `M1` | `git merge 2b16d5bd` — дешёвый апстрим | идёт | `wave/upstream-merge-w2` | — | — |

Статусы: `ждёт` · `идёт` · `готово` · `блокирована` · `отменена`

## Решения командира

| Когда | Что решено | На основании |
|---|---|---|
| 2026-08-30 | `T0.2` из плана (вычистить операционный мусор из индекса) **не ставится задачей** — выполнено командиром ещё в прошлом плане, коммит `36466b21` | Гигиена репозитория, территория командира |
| 2026-08-30 | Гвард `border_pixels_guard` починен **до** начала мержа, отдельной задачей `G1` | Он был красным с `6628ba59`; чинить ослепший гвард посреди переезда `lib/shitty` → `lib/vterm` было бы кратно дороже. Риск №2 оценки мержа прямо про это |
| 2026-08-30 | Командирский worktree `scratchpad/commander` на `master` заведён и используется для борда и планов | Дважды за прошлый план коммит командира сел на ветку задачи, потому что агент переключал общее дерево |

## Унаследованные знания о среде — во все брифы этого плана

Куплены дорого в прошлом плане, каждое стоило отдельного разбора:

| Что | Почему важно |
|---|---|
| Сборка **штатным Apple clang**, `CC`/`CXX` не выставлять | Homebrew LLVM не установлен, `brew --prefix llvm` даёт несуществующий путь, сборка падает с `FileNotFoundError` |
| Полное окружение питоновских тестов: `SHITTY_TEST_BINARY`, **`SHITTY_PRETTY_TEST_BINARY`** (не `PRETTY_TEST_BINARY`), `SHITTY_TOML_DUMP_BINARY`, `SHITTY_TEST_FONTCONFIG`, `SHITTY_TEST_PLATFORM`, `SHITTY_TEST_VERSION` | Неполное окружение даёт ложные отказы. На этом обожглись четверо, включая командира; из-за этого регрессия дошла до `master` |
| Питоновские тесты гоняются **из корня репозитория**, как это делает `build.py` при `cwd="$(S)"`: `python3 tst/run_unittest_group.py --group=N --group-count=M`, либо `python3 -m unittest discover tst`, либо `python3 tst/test_pty.py` — **все три работают** | `discover("tst")` сам кладёт `tst` в `sys.path`. Прежняя формулировка «только из `tst/`» была **неверна** и уводила от штатного пути — проверено `T0.1` всеми тремя способами |
| `pty_test_helper` **не собирается** целью `unit_tests` | Без отдельной сборки пять `Pty::*` ложно падают с «Error: execvp» |
| **НЕ запускать `python3 dev/style.py`** | clang-format 23.1.0 против ожидаемого 21; прогон переформатирует ~130 чужих файлов |
| **Вместо `tr` писать `sed`/`awk`/`python3`.** Не «не используй `tr`», а **чем заменить**: `tr -d 'X'` → `sed 's/X//g'`, `tr '\n' ' '` → `awk 1 ORS=' '` | На алиасе `task add project:rt` обожглись **пятеро**, включая командира дважды. Оболочка инструмента держит алиас из снимка окружения, сделанного до правки профиля, поэтому правка `~/.zshrc` его не снимает. Формулировка изменена по предложению `G2`: он читал запрет и всё равно набрал `tr` — значит запрет не работает, работает готовая замена |
| Параллельные задачи — **каждая в своём worktree** | Иначе переключают ветку под соседом; `pkill` по шаблонам в общем дереве запрещён |
| **Пустой вывод `./build <guard>` не доказывает прогон** | `.build/` адресуется содержимым, штампы возвращаются из `.build/cas/` без исполнения. Гвард подтверждается тем, что **краснеет на нарушении** |
| Комментарии в коде — **без номеров строк** | Протухают за одну волну |

## Отложенные находки

| Откуда | Находка | Уровень | Решение |
|---|---|---|---|
| `G1` | Механизм разрешений гвардов: количество не помнит, **какие** обращения узаконены; считается подстрока без границ слова; проза комментариев идёт в зачёт | важная | Предложено заменить количество на множество нормализованных текстов строк. Не реализовано — отдельное изменение с риском для трёх гвардов. **Встретится в `T2.1` и `T6.1`** |
| `R3-sec` прошлого плана | `Z2`: отказ открыть pty гасит весь терминал через `exit(1)` вместо отказа одной сессии | важная | Унаследованное; `pty.h` трогает задача `T5.7` — там и решить |

## Точки остановки и вопросы человеку

| Когда | Вопрос | Ответ |
|---|---|---|
| 2026-08-29 | Объём и режим мержа | Весь апстрим, автономно до `T5.1` |
| — | **`T5.1`: origin и четырёхсторонние инсеты в `VtGeometry`** | ожидается; решение углубляет расхождение с апстримом |
| — | Мерж `wave/upstream-merge-*` в `master` | ожидается по завершении плана |
| 2026-08-30 | Пуш в `fork` | **разрешён и выполнен**: `6628ba59..11f58f0b`, 105 коммитов, перемоткой вперёд. Волна 4 и далее пушатся по мере закрытия |

## Волна 1 закрыта — эталон и чек-лист

| Задача | Результат | Отчёт |
|---|---|---|
| `T0.1` | Эталон снят на `0c303cd6`, коммит `6956db3e` | `docs/reports/T0.1-baseline-2026-08-30.md` |
| `T0.3` | 11 из 11 инвариантов имеют машинную проверку; мерж целится в пять из них | `docs/plans/reviews/upstream-merge-invariants.md` |

**Эталон, с которым будут сверяться восемь волн:**

| Что | Число |
|---|---|
| `unit_tests` | **955 OK, 0 ERR** |
| Полный питоновский набор | **6391 тест: `failures=6 errors=14 skipped=17 expected failures=549`** |
| Четыре гварда | зелёные, каждый **доказанно краснеет** на искусственном нарушении |
| Холодная сборка с пустым кешем | 50 с группа по умолчанию (227 узлов) + 36 с тестовые бинарники (130) |
| Питоновский набор | 26–27 с |

**Мигающих тестов нет** — не по одному прогону, а по **пяти**: списки падений побайтово идентичны, `diff` пуст на всех парах.

### Три находки `T0.1`, все важные

**1. Критерий `T2.1` из плана был недостаточен — правка внесена.** Три сканирующих гварда из четырёх **на пустом множестве файлов возвращают `exit 0`**: самозащита `if not tracked` есть только у `darwin_call_guard`. Значит `T2.1` можно выполнить формально — заставить гвард покраснеть на пробе в `lib/shitty`, ничего не зная про `lib/vterm`, — и три инварианта останутся без охраны при зелёной сборке. В критерии добавлена самозащита и способ её проверить: временно убрать `lib/shitty` из корней, все три обязаны покраснеть.

Это третий уровень одной и той же проблемы: сперва гвард ослеп (`G1`), потом выяснилось, что ослепнуть он может тремя способами (`T0.3`), теперь — что критерий, ловящий слепоту, сам её не ловит.

**2. Девять тестов `FontResolverTest` прогоняются дважды.** `tst/test_font_fallback.py` импортирует **класс**, а не модуль, и discovery собирает его второй раз. Оба экземпляра печатаются под одним id, поэтому «одно падение в двух группах» читается как мигание — ровно то, что восемь волн придётся отличать от регрессии. Правка механическая, но вне рамок мержа.

**3. Тринадцать из четырнадцати `errors` — один класс.** Коммит `62cef373` выключил на darwin freetype/harfbuzz; соседние тесты честно пропускаются через `@unittest.skipUnless(FONTCONFIG_AVAILABLE, …)`, а в `test_bitmap_font_render.py` нет ни одного `skip`. Если закрыть той же охраной, `errors` упадёт с 14 до 1. Диагноз выведен из кода и даты коммита, бисектом не подтверждён.

### Мелочи, которые сэкономят время следующим

- `unit_tests` печатает `terminal: /dev/fd/88:1: syntax error` — это **зелёный** тест кормит битый TOML. `grep -i error` по логу даёт ложное срабатывание.
- `test_build_metadata` завязан на `test_group_count = 20` и покраснеет первым, если мерж тронет разбиение.
- Питоновский набор правильнее гонять **из корня** через `tst/run_unittest_group.py` — так делает `build.py` при `cwd="$(S)"`, раннер сам кладёт `tst` в `sys.path`, и получается единый разбор по всем 20 группам.

## Дополнение `T0.1` — и промах командира, стоивший четырёх секунд

**Эталон перепроверен на зависимость от порядка.** Первоначальная проверка «мигающих нет по пяти прогонам» была слабее, чем выглядела: `select_group()` раскладывает тесты как `test_index % group_count`, поэтому при одном разбиении состав и порядок каждого шарда одинаковы во всех прогонах. Тест, портящий состояние соседу по процессу, пережил бы её не поморщившись — а это ровно тот класс, который через волну выглядит регрессией мержа.

| Разбиение | Тестов в процессе | Результат | Время |
|---|---|---|---|
| 20 групп, параллелизм 10 | ~320 | 6391 / 6 / 14 / 17 / 549 | 26–27 с |
| 7 групп, параллелизм 7 | ~913 | то же | 26 с |
| **1 группа, весь набор одним процессом** | **6391** | то же | **114 с** |

Поимённые списки во всех трёх побайтово идентичны. **Утечки состояния между тестами на эталоне нет** — семь прогонов, три разбиения. Оговорка сузилась, но не снята: календарное время, память и сеть семью прогонами в один час не ловятся.

**Промах командира: два агента в одном рабочем дереве.** `M1` запущен в главное дерево, где ещё работал `T0.1`. Его последний прогон закончился в 14:01:37, файлы `M1` появились там же в 14:01:41 — **разрыв четыре секунды**. Однопроцессный прогон длится 114 с; стартуй он на две минуты позже, эталон был бы снят с наполовину подменённого дерева и записан как эталонный. Ветка при этом переехала с `w1` на `w2` под работающим агентом.

Не пострадало ничего — исполнитель проверил время модификации каждого из семи прогонов, все легли на дерево `0c303cd6`, и коммитил явным путём (`git commit -o <файл>`), чтобы не подмести чужое. Но повезло на четыре секунды, а впереди семь волн.

**Правило, которое я же и записал в этот борд, я же и нарушил.** Причина не в незнании: главное дерево удобно, а заводить worktree — лишний шаг. Отсюда следствие для оставшихся волн: **worktree заводится до запуска агента, а не после того, как выяснилось, что он нужен.**

**Ожидаемая дельта `M1` — плюс семь тестов, поимённо** (`-fullscreen`, лигатурные фиксы, гиперссылка на Super). Критерий приёмки волны 2 по числам — **6398**, а не 6391. Список в отчёте `T0.1`, раздел 7; передан исполнителю.

## Волна 2 закрыта, и промах командира повторился

**`M1` сдан:** мерж `2b16d5bd` (девять апстримных коммитов), два конфликта, оба разрешены объединением. Смержено в `master` коммитом `b05f788b`, **`behind` 57 → 48**. Отчёт — `docs/reports/M1-cheap-upstream-2026-08-30.md`.

| Проверка | Результат |
|---|---|
| `unit_tests` | 955 OK — эталон, пять прогонов |
| Полный набор | **6398** / `failures=6 errors=14 skipped=17` — те же двадцать красных **поимённо** |
| Семь новых тестов апстрима | все зелёные, подтверждено **по логам** в двух разбиениях |
| Протокол тест-режима | **136 команд до, 136 после**, `diff` списков пуст |
| `test_pane_alt_screen`, `test_panes_protocol` | зелёные — фикс и протокол прошлого плана мерж пережили |

**Доказательство, которого не было в брифе и которое забрано в метод:** дифф нашего мержа к предмержевой голове совпал **построчно** с диффом апстрима к его собственной базе. Значит разрешение конфликтов прибавило ровно апстримное и не убавило нашего. Проверено командиром независимо на коммите `8f8bf84d`. Работает и на переименованиях — понадобится в волне 3, где их 55.

**Второе, тоже в метод: проверка свежести бинарников, не зависящая от системы сборки.** Искать в `.build/st_test` строку, появляющуюся **только после разрешения конфликта** (у `M1` — `" fullscreen="` рядом с нашими `" auto_hide_chrome="` и `" panes="`). «Сборка ничего не пересобрала» этого не доказывает: кеш адресуется содержимым и подставляет артефакт по хешу.

**Третье: смена `group_count` вместо повтора прогона.** Повтор с тем же разбиением не меняет ни состава шардов, ни порядка, то есть теста, портящего состояние соседу по процессу, не ловит. Стоит 24 секунды.

### Промах командира, второй за час, та же причина

`W3` запущен в дерево, где `M1` ещё дописывал отчёт. Дерево переключено на ветку волны 3, начат `git merge da8d6ba3`, и `git add` от `M1` **лёг в чужой мерж-индекс** — при коммите `M2` его отчёт уехал бы внутрь чужого мерж-коммита.

Исполнитель откатил только свой файл (`git restore --staged --worktree` по имени), не тронув `UU build.py` и `A lib/vterm/check_includes.py`, и сообщил вместо того, чтобы разбираться в чужом конфликте. Командир проверил после: состояние `W3` цело, ущерба ноль. Дополнение отчёта забрано на `master` отдельным коммитом `4c31fa8d`.

**Это второй случай подряд.** Первый — `M1` в дерево работающего `T0.1`, разошлись на четыре секунды. Правило «каждой задаче свой worktree» записано в этом борде моей же рукой; нарушено дважды за час, оба раза потому, что главное дерево под рукой, а worktree — лишний шаг.

**Следствие, обязательное к исполнению до конца плана:** ни один агент не запускается в главное дерево, пока предыдущий не отчитался **и** не подтверждено, что он ничего не пишет. Мерж-шаги не исключение — они держат дерево дольше всех.

**И главное, что выяснилось при разборе: в этот раз спасло совпадение, а не процесс.** Исполнитель обнаружил чужой мерж только потому, что его `git commit` **упал** с `fatal: Exiting because of an unresolved conflict`. Не будь конфликта в `build.py`, коммит прошёл бы молча и его отчёт уехал бы внутрь чужого мерж-коммита **без единого сигнала** — ни в его выводе, ни в моём, ни в истории это не выглядело бы аномалией.

То есть наблюдаемость этого отказа нулевая: он виден, только если чужая работа случайно застряла в состоянии, ломающем посторонний коммит. Правило «свой worktree» остаётся первой линией, но оно про дисциплину, а дисциплину я нарушил дважды за час.

**Третий случай, 2026-08-30, волна 4 — уже не запуск агента, а мой собственный прогон.** Запустил полный питоновский набор в дереве, где `M4` по моему же поручению пересобирал бинарники. Получил 148–328 ошибок на группу против четырнадцати в эталоне. Все до единой — `FileNotFoundError: .build/st_test`: символьная ссылка исчезает на время пересборки.

Ущерба нет, испорчено только измерение. Но природа промаха хуже двух первых: там я не знал, что дерево занято, здесь **знал и сам его занял**. Правило записано против запуска агентов, а нарушено собственной командой — то есть сформулировано уже, чем нужно.

**Уточнение правила:** занятое дерево закрыто для всего, что пишет или читает `.build/` — включая проверки командира. Свои прогоны вести в отдельном worktree либо после отчёта исполнителя.

**Дешёвая страховка в каждый бриф, предложена исполнителем:** перед любым `git add` — `git rev-parse --abbrev-ref HEAD` и `ls .git/MERGE_HEAD`. Две команды, и они ловят именно тот случай, который прошёл бы тихо: ветка уехала под ногами, либо в дереве идёт чужой мерж.

## Волна 3 закрыта — ядро в `lib/vterm`, гварды с полом

Смержено в `master` (`72ed70eb`), **`behind` 48 → 46**. Отчёт — `docs/reports/W3-vterm-move-2026-08-30.md`.

**54 файла ядра переехали.** Решение `Р2` соблюдено, и вопрос даже не встал: ни один из переехавших не нёс правок форка, все прошли чистыми переименованиями. Работы потребовало обратное — **десять наших файлов включали переехавшие заголовки**, и апстримная замена до них не дотянулась.

**`T2.1` — пять доказательств, все прямым исполнением программы гварда, минуя `./build`:**

| Что доказано | Как |
|---|---|
| Каждый краснеет на нарушении **в `lib/vterm`** | четыре отдельные пробы, в каждой остальные три зелёные |
| Самозащита | обеднение корней роняет **все четыре** — проверено командиром независимо |
| Словарь `pane_grid` | не пробой, а прогоном по чистому `origin/master`: старый словарь `rc=0` и пустой вывод над 24 нарушениями, новый — **47 строк** в трёх рендерерах |
| `border_pixels` различает вызов и поле | на `origin/master` было 37 ложных в 8 файлах, стало 0 — и зелёный там настоящий, не от пустоты |
| Разрешения не расширены | **сужены**: `composer.h` 5→2, ключ `grid_geometry.h` удалён |

Последняя строка — вторая находка `G1` с другой стороны: три из пяти срабатываний `composer.h` и единственное срабатывание `grid_geometry.h` были **комментариями**, посчитанными старым подстрочным сопоставлением. Вернуть прежние числа было бы ошибкой.

**Самозащита сделана сильнее, чем требовал критерий.** Не «файлов больше нуля», а «то, что гвард обязан прочитать, он прочитал»: каждый ключ разрешения обязан оказаться среди просканированных, а `pane_grid` — найти все три рендер-бэкенда **по имени** (одного `render*`-файла мало, этому удовлетворяет `render_blend.h`).

**Известный сигнал на будущее:** когда `mouse_frontend` уедет в `lib/vterm` (задача `T5.1`), `mouse_geometry_guard` покраснеет с текстом «re-key the allowance». Это **сигнал, а не регрессия** — на дереве `origin/master` он уже так и делает.

### Новая ловушка сборки — того же класса, что штампы из CAS, но на компиляции

**`./build` не инвалидирует узлы при смене `build.includes`.** Единственная компиляционная правка `M2` (путь включений) дала пустой вывод и `exit 0`, **не исполнив ничего**, даже с `-v`. Контроль исполнителя: временный `-DW3_PROBE=1` в `cppflags` пересобрал 73 узла — значит `cppflags` в ключ узла входят, а `includes` нет.

Настоящая проверка такого шага — `./build st --clear`. Командир подтвердил независимо: **223 узла**.

В эталоне `T0.1` этой ловушки нет, и она опаснее штампов: там пустой вывод не доказывал прогон гварда, здесь — не доказывает **компиляцию**.

### Ещё три находки

- Новая цель `vterm_boundary` оставляет в корне симлинк, которого нет в `.gitignore`. Затянулся в коммит через `git add -A`, пойман сверкой `numstat`, снят с индекса, игнор добавлен. **Учесть в `T6.4`.**
- Апстримный `grapheme_source` в `build.py` указывает на `lib/shitty/grapheme.cpp`, которого там больше нет; переменная нигде не используется. **Кандидат в `T6.1`**, трогать не стал.
- Названная явно остаточная щель `pane_grid`: рендерер, связавший `const auto& g = composer.geometry;`, гвардом не виден. Словарь квалифицирован **сознательно**, чтобы не запретить законное чтение геометрии панели из `TerminalUpdate`.

## Волна 4 — реализация закрыта, идёт ревью

Коммиты: `f8b9b009` (мерж), `d13c9656` (остаток перевода), `9205decb` (сортировка включений), `e22249d1` (отчёт). Отчёт — `docs/reports/M4-options-split-2026-08-30.md`.

**Главный результат — решение, а не код: все 19 наших опций остались в `Options`, в `Options::vt` не уехало ничего.** Метод: не по названию, а перечислением обращений `opts->…` в файлах, которые становятся ядром. Вышло ровно апстримное множество `VtConfig` плюс `palette` — ни одного нашего имени.

**Ключевой разбор — `sidebarWidth`**, единственная опция с настоящим вопросом: её значение втекает в инсеты, а инсеты в волне 6 уезжают в `VtGeometry`. Различены **опция и её следствие**: в ядро едет посчитанный инсет, `Composer` остаётся в `lib/shitty`. Иначе `T5.1` получила бы второй источник правды ровно там, где решение и без того тяжёлое. Отдано `R4-arch` отдельным вопросом.

**Проверено командиром на освободившемся дереве:** `./build st --clear` — **224 узла** (223 в волне 3 плюс новый `vt_config.cpp`, сходится), `unit_tests` **955 OK**, полный набор **6398 / 6 / 14 / 17 / 549**, пять гвардов зелёные.

### `T3.4` отменена — дефект плана, а не пропуск работы

План приписал ей свойства другой строки. `FONT_STATE` — девять полей, все `composer.*` геометрия, **`Options` не читает вовсе**, расщепление её не задевает; наш обработчик байт в байт совпадает с апстримным. «20 полей плюс `fullscreen=`» описывает строку `OPTIONS`, которую свёл `M1`. Утверждение отдано `R4-test` на независимую проверку — на нём основано решение не делать работу.

### Сортировка включений: инструмент был под рукой всё это время

Отдельный шаг перед волной 5 **не нужен**. `reorder_includes()` в `dev/style.py` — **чистый Python и до clang-format не доходит**; запрет на `dev/style.py` касается только `format_sources()`. Вызов в обход `main()`:

```sh
python3 -c 'import sys, pathlib; sys.path.insert(0, "dev"); import style
for p in sys.argv[1:]:
    style.reorder_includes(pathlib.Path(p))' <файлы>
```

Конвенция (`style.py`): четыре группы — парный заголовок, `"…"`, `<lib/…>`, `<std/…>`, прочее **последней**; внутри группы ключ `(len(path), path)`, длина по пути **без** кавычек и скобок.

**Исполнитель проверился на этом вместо того, чтобы поверить себе:** прогнал функцию по пяти блокам, собранным руками при разрешении конфликтов, — **четыре из пяти оказались не в том порядке**. Две ловушки: `<plt/…>` идёт **после** `<std/…>`, и длина считается по пути, а не по написанию с кавычками. Для компиляции безвредно, но каждая — лишний хунк следующему мержу.

**Правило для `M5`–`M8`:** руками конвенцию не воспроизводить, сразу звать функцию. Прогон по всему дереву стоит секунды и сборки не требует.

## Волна 4 закрыта — три жёлтых вердикта, красных нет

Смержено в `master` (`0c42e1d8`), **`behind` 46 → 45**.

| Проверяющий | Вердикт | Главное |
|---|---|---|
| `R4-test` | `PASS_WITH_FINDINGS` | 19 мутаций, **4 выжили**; главное утверждение подтверждено арифметикой множеств, а не пересказом |
| `R4-arch` | соответствует с замечаниями | граница верна, переносить в волнах 5–6 не придётся ничего; **план разошёлся с историей апстрима** |
| `R4-qa` | готово с замечаниями | всё перемерено на пустом дереве без кеша; **`render_vk.cpp` локально не компилируется вовсе** |

**Утверждение волны проверено тремя независимыми способами.** Исполнитель рассуждал «кто читает»; `R4-test` сделал арифметику множеств полей против апстрима и получил ответ в форме **нуля**; `R4-arch` проверил по конечной точке — в апстримном `vterm.cpp` ноль упоминаний `Options`.

**Довод `R4-arch`, которого не было ни у кого:** ошибка в этой границе **громкая**. После растворения `VtState` ядро физически не видит `Options`, значит опция не на той стороне — ошибка компиляции, а не тихо неверное поведение. Слабый критерий здесь безопасен по причине, а не по везению.

### Четыре выжившие мутации — где сидела бы тихая ошибка

Три из четырёх — в тех самых шестнадцати строках, которые мерж не мог дотянуть: цвет фона сайдбара, тинт титульной полосы, ветка по умолчанию в эталонном рендерере. Все три про **цвет**, то есть проверяются только глазами. Ошибись исполнитель на них — дерево осталось бы зелёным.

Внесены отдельным пунктом в чек-лист ручной приёмки `T7.2`.

### Иллюзия покрытия: `options_ut` покрывает 10 опций из 19

Девять наших — `sidebarWidth`, `quickGeometry`, `quickCompanion`, `configPath`, `quickCornerRadius`, `quickRememberFrame`, `autoHideChrome`, `panes`, `quickFullscreenHotkey` — не упомянуты **ни в юнит-тестах, ни в `tst/` вовсе**. Плюс четыре теста слепы к подмене `quick`↔`transparentTitlebar`: они задают обе опции разом либо ни одной.

Критерий «20 тестов зелёные» формально выполнен и говорит заметно меньше, чем кажется. **Дефект не этой волны** — так было и до неё. Отложено: дописывать тесты значит менять объём плана мержа.

### Сборка форка под Linux сломана с 20 августа

`render_vk.cpp` компилируется **только под Linux** (`build.py:706`), поэтому на этой машине не собирается ни при каких условиях — и все зелёные сборки, наши и прежние, его не покрывали.

CI форка стал виден только после разрешения на пуш: **все прогоны красные**. Причина одна и унаследованная — `render_vk.cpp:2282`, коммит `8325f312` от 2026-08-20 («F6: the Vulkan backend says why it refuses a split»): литералы переданы в `raiseError` напрямую, без `StringView`, линковщик не находит инстанциаций. У апстрима такой строки нет.

**Следствие для волны 5:** там задача «портировать Vulkan на span shaper» — работа **наша**, апстрим её не делал, и проверить локально нечем. CI обязан быть зелёным **до** старта волны 5, иначе свою поломку не отличить от унаследованной.

Починка отдана задаче `G2` (вне плана, дефект унаследованный).

## `G2` — починка Linux, и то, что за ней обнаружилось

Коммиты на `master`: `360e9351` (правка), `17d640c9` (отчёт). Отчёт — `docs/reports/G2-linux-build-2026-08-30.md`.

**Правка минимальная и по конвенции:** литералы обёрнуты в `StringView(...)`, как в остальных тридцати вызовах файла. Гипотеза подтверждена **тремя способами**: по исходникам `ext/libstd`; изолированным TU без Vulkan, где `nm -u` показывает ровно те же массивные символы; и на самом файле — оба символа ушли из объектника, новых не появилось.

**Мест того же класса в дереве больше нет** — скан 644 файлов с вырезанием комментариев и разбором по сбалансированным скобкам, сканер проверен негативным контролем: до правки 1 попадание, после 0.

**Причина ошибки названа:** доккомментарий над `raiseError` в `lib/vterm/fatal.h` **учил сломанной форме** и был единственным местом в дереве, которое этому учит. Коммит `8325f312` почти наверняка списан с него. Комментарий исправлен.

### Главное: гейт `Build Linux` гасит весь CI, включая macOS

`needs: build` стоит у Alpine, Fedora, Sandboxed 0..4/5, Coverage, ASan, UBSan, Nix Linux **и у `build-darwin`**, а от него — все шесть Darwin-джобов и Nix Darwin. В красных прогонах все они показывают `0s`.

**Значит с 2026-08-20 не проверялся ни рантайм Linux, ни macOS.** Зелёного CI на форке не было **никогда**. За этой стеной может стоять что угодно, и одна зелёная линковка этого не снимает.

По строке «Build Linux красный» этого не видно — надо смотреть на `0s` у остальных джобов.

### `render_vk.cpp` на этой машине компилировать **можно**

Вопреки тому, что считалось: нужен только `git clone --depth 1 KhronosGroup/Vulkan-Headers` в скретчпад (в систему ничего не ставится). `-DHAVE_VULKAN_WAYLAND=1` — обычный `-D`, `render_spv.h` генерируется штатным `./build render_spv` (`glslangValidator` и `spirv-cross` установлены). После этого файл компилируется целиком, exit 0, без предупреждений. Линковка невозможна — нет `libvulkan`.

**Оговорка, важная для волны 5:** `-fsyntax-only` этот класс дефектов **не ловит** — он живёт в компоновщике. Ловит `-c` плюс `nm -u | c++filt`.

Самое дешёвое: дефект воспроизводится **вообще без Vulkan** пятистрочным файлом — `raiseError` и `stl::output` платформо-нейтральны. Это готовый гвард на весь класс; узел не заведён, потому что это `build.py`.

### `platform_wayland.cpp` и `font_freetype.cpp` — покрыты, но покрытие сегодня нулевое

Оба компилируются в CI Linux чисто. У wayland есть **39 интеграционных тестов** (только Linux); у freetype — косвенное покрытие через реальную загрузку и метрики глифов. Всё это живёт за тем же гейтом и не запускалось десять дней.

Регрессию **компиляции** красный CI поймает уже сейчас; регрессию линковки или поведения — нет. `font_freetype.cpp` устроен как `render_vk.cpp`: заглушка держит фабрику линкуемой, та же ловушка невидимости на macOS.

### Открытый вопрос, оставленный человеку

`raiseError` от повторения дефекта не защищён. Лечится шестью строками в `fatal.h` — перегрузкой, сводящей `const u8 (&)[N]` к `StringView`. Но обёртка `StringView(...)` — сложившаяся конвенция всех тридцати вызовов, и снятие требования есть **решение об API общего заголовка**, а не починка сборки. Не сделано намеренно.

## Гейт снят — и за ним оказалось не пусто

Первый зелёный `Build Linux` за десять дней (прогон `33313649107`, коммит `17d640c9`). Сразу за ним впервые с 2026-08-20 запустились все остальные джобы, и картина такая:

| Джоба | Итог |
|---|---|
| `Build Linux` | **зелёная** |
| `Nix Linux` | **зелёная** |
| `Tests Alpine` | красная |
| `Tests Fedora` | красная |
| `Tests UBSan` | красная |
| `Coverage` | красная |
| **`Build Darwin`** | **красная** |
| `Tests Darwin 0..4/5`, `Nix Darwin`, `Sandboxed 0..4/5` | не запускались — стоят за `Build Darwin` |

**`Build Darwin` красная при том, что локально macOS собирается.** Локально: `./build st --clear` 224 узла, `unit_tests` 955 OK, питоновский набор 6398 при известных двадцати красных — всё зелёное. В CI другая тулчейн (Nix), и это первый её запуск за десять дней: **мы не знаем, сломана она давно или недавно**, потому что до сегодняшнего дня она не выполнялась ни разу.

### Что это меняет в плане

**Волну 5 начинать нельзя, пока не разобраны эти красные.** Причина не в осторожности, а в измеримости: там задача «портировать Vulkan на span shaper» — работа **наша**, апстрим её не делал, и проверить её можно только через CI. Если CI красный по унаследованным причинам, свою поломку от чужой не отличить.

**Порядок работ меняется:**

1. Разобрать красные CI поимённо: что унаследовано, что от наших волн, что от апстрима.
2. Унаследованное чинить отдельными задачами вне плана мержа, как `G1` и `G2`.
3. Только потом волна 5.

Это не сдвиг сроков ради чистоты, а условие того, чтобы оставшиеся четыре волны вообще были проверяемы.

### Про `Build Darwin` отдельно

Локальная зелёная сборка и красная `Build Darwin` не противоречат друг другу: тулчейни разные. Локально — штатный Apple clang; в CI — Nix. Коммит `62cef373` («macOS: сборка без библиотек Homebrew по умолчанию») трогал ровно эту границу, и он же выключил на darwin freetype/harfbuzz — то есть в этой области у форка уже есть история расхождений с апстримом.

Диагноз до чтения логов не ставлю.

## Возобновление 2026-09-03 — красные CI разобраны поимённо

Сессия прервалась на диагностике `Build Darwin`. Расхождений борда с git не найдено: волна 4 смержена (`0c42e1d8`), `master` = `ae802cfc`, несмерженных веток нет.

**Апстрим за это время вырос на 41 коммит.** `behind` был 45, стал **86**: один коммит 30 августа, пять 31-го, четырнадцать 1 сентября, двадцать один 2-го. Волны 5–8 плана нарезаны по истории **до** `1d3ee13d`; всё, что пришло после, планом не покрыто и потребует девятой волны либо расширения восьмой. Решение отложено до конца волны 8 — резать план по движущейся цели дороже, чем один раз в конце.

### Три причины на шесть красных джоб, и все три наши

| Джобы | Ошибка | Откуда | Задача |
|---|---|---|---|
| `Build Darwin` | `Undefined symbols … simdutf::binary_to_base64` | наш `62cef373`: `build.py:177` гасит на darwin **все** необязательные pkg-config-зависимости, а `base64.cpp:13` выбирает реализацию по `__has_include` | `G3` |
| `Tests Alpine`, `Tests Fedora`, `Tests UBSan`, `Tests ASan` | `implicit declaration of function 'cfmakeraw'` | наш `T5` плана `pane-frame-stall`: `tst/pty_test_helper.c:127`, макрос тестируемых возможностей не выставлен | `G4` |
| `Tests UBSan`, `Tests ASan` | `call to 'forward' is ambiguous`, `obj_pool.h:51` из `pty_ut.cpp` | наш `T5`, контракт `К3` | `G4` |

`Coverage` не разбиралась отдельно — стоит за той же компиляцией. Двенадцать джоб (`Nix Darwin`, `Tests Darwin 0–4/5`, `Sandboxed 0–4/5`) **пропущены**, а не красные: их гейт — `Build Darwin`.

**Апстримного среди причин нет ни одной.** Ожидание было обратным: гейт `Build Linux` стоял красным с 20 августа, за десять дней в форк втекло много чужого. Втекшее оказалось ни при чём — красное принесли обе наши волны, и обе невидимы на macOS с Apple clang.

**Вывод для метода:** локальная зелёная сборка на одной платформе не является свидетельством. Оба дефекта `G4` — про то, что Apple clang принимает то, чего не принимают gcc и clang с санитайзерами; дефект `G3` — про то, что наличие заголовка и наличие библиотеки на линковке решаются в разных местах и локально совпали случайно. В брифах `G3` и `G4` поэтому потребован вывод **до** и **после** на чужом компиляторе, а не пересборка на своём.

### Решение командира: волна 5 не начинается

Волна 5 портирует Vulkan — работа, которой апстрим не делал, и проверить её можно **только** через CI. При красном CI неизвестного происхождения своя поломка неотличима от унаследованной. Сначала `G3` и `G4`, затем зелёный прогон, затем волна 5.

`G3` и `G4` запущены параллельно 2026-09-03 в отдельных worktree (`wt-G3`, `wt-G4`), границы владения разведены: `build.py` отдан `G3` целиком, `G4` явно запрещено его трогать.

### `G3` и `G4` закрыты — обе проверены командиром независимо

Смержено в `master`: `aa09d3ff` (`G3`), `10ac9dde` (`G4`). Отчёты — `docs/reports/G3-darwin-link-2026-09-03.md`, `docs/reports/G4-linux-compile-2026-09-03.md`.

| | `G3` | `G4` |
|---|---|---|
| Что изменено | `build.py`, `lib/vterm/base64.cpp` | `tst/pty_test_helper.c`, `ext/libstd/std/mem/{obj_pool,embed}.h` |
| Воспроизведение CI | include-каталог simdutf подан отдельно от линковки; ошибка совпала с логом **дословно**, включая `libshitty_prod.a[49](base64.cpp.o)` | docker, alpine/musl/gcc 15 и debian/glibc/gcc 14+clang 19, каждый до и после под `git stash`; на debian совпала строка и **колонка** |
| Проверка командиром | `./build st --clear` = 224, `unit_tests` = `OK: 955` | то же |

**`G3`: суть правки — свести два признака в один.** Define `HAVE_SIMDUTF` повешен на **сам объект зависимости**, который несёт `-lsimdutf`; раннер выкидывает выключенную зависимость и из компиляции (`build:1274`), и из линковки (`build:1306`, `1322`). Состояние «define есть, библиотеки нет» после этого невыразимо. Проверено обеими сторонами переключателя: `nm` и `otool -L` пусты при выключенной, символ и `libsimdutf.35.dylib` — при включённой.

Отказ от более короткого варианта (расширить `optional_pkg` на darwin) осознанный и записан в отчёте: он озеленил бы CI, но оставил бы `__has_include` на месте и вернул бы линковку Homebrew-dylib — ровно то, что убирал `62cef373`.

**`G4`: гипотеза брифа была неверна, и это выяснено измерением.** Я предположил «дело в версии компилятора». Нет: локальный Apple clang 21 — тот же фронтенд, что в санитайзерных джобах, `-std=c++26` принимает. Разница в **одной строке объявления стандартной библиотеки**:

| запись параметра в «библиотечном» `forward` | тот же Apple clang |
|---|---|
| классовый трейт `typename remove_reference<T>::type&` (libstdc++) | `error: call to 'forward' is ambiguous` |
| builtin `__remove_reference_t(T)&` (libc++) | компилируется молча |

ADL находит `std::forward` в обоих случаях — молчит **разрешение перегрузки**, а не поиск имени. Отсюда правило: «тот же компилятор» не означает «та же диагностика», пока не совпала и стандартная библиотека.

### Три вещи, которые обязаны попасть в брифы и в `CLAUDE.md`

**1. `./build st` не строит тесты.** Ни один из 224 узлов не компилирует `*_ut.cpp`; `unit_tests` — отдельная цель, а `pty_test_helper` не тянется даже ею (нужно `./build unit_tests pty_test_helper`). Оба дефекта `G4` сидели именно в тестовом коде — вот почему зелёная локальная сборка их не видела **ни разу**.

**2. `unit_tests` без `SHITTY_PTY_TEST_HELPER` даёт `OK: 950, ERR: 5`.** Полный прогон — `SHITTY_PTY_TEST_HELPER="$PWD/.build/pty_test_helper" ./.build/unit_tests --threads=1` → `OK: 955`. Командир снова получил ложные числа из-за неполного окружения и снова заподозрил исполнителя вместо инструмента. **Третий раз за два плана один и тот же класс ошибки**; первые два — `SHITTY_PRETTY_TEST_BINARY` в прошлом плане.

**3. Тот же класс дефекта не вычерпан ни у одного из двух.**
- `G3`: пять мест в `ext/libstd/**` с признаком «`__has_include` + необязательная зависимость» (openssl/s2n/mbedtls, c-ares, rapidhash/xxhash, liburing ×2, cpptrace). Одно не теоретическое: широкий `-I/opt/homebrew/include` роняет сборку на `ssl_socket.cpp:27`, `mbedtls/ctr_drbg.h`. И `build.py:267-269` прямым текстом описывает мину: TLS/DNS-библиотеки намеренно не в `libstd_backends`, «nothing here references them» — как только что-то сошлётся, будет тот же `Undefined symbols` и снова в одной среде из двух.
- `G4`: девять неквалифицированных `forward<...>` ещё в шести заголовках `libstd` — `h_map.h:62,69`, `map.h:33,47,76`, `refcount.h:36`, `obj_list.h:32`, `small_obj_allocator.h:31`, `qsort.h:130`. Каждый сработает, когда в шаблон впервые придёт аргумент из `std`.

Обе группы — кандидаты в одну задачу `G5` одним проходом, **после** зелёного CI: сейчас важнее увидеть, что чинилось именно то, что падало.

### Второй прогон за гейтом: отказов оказалось больше, чем было видно

Прогон `33692909258` на `3c6b8440`. `Build Darwin`, `Nix Darwin`, `Build Linux`, `Nix Linux` — зелёные. Пять тестовых джоб красные, и состав отказов **изменился**: `Tests Fedora` прошла брендирование и упёрлась в следующий, который за ним прятался.

**Так работает всякий гейт: он не копит отказы, а показывает по одному.** Сначала `Build Linux` десять дней скрывал всё; потом брендирование скрывало конфиг; сейчас видны четыре. Оценивать «сколько осталось» по текущему списку нельзя.

| Отказ | Где | Вердикт | Задача |
|---|---|---|---|
| `unknown option: sidebarTabs` в `bin/pt/pretty.toml` | Fedora | наш, коммит `6ca8fcf2` обновил `bin/st/shitty.toml` и забыл конфиг `pt` | `G7` |
| `VulkanBlitSmokeTest` ×2 — `differ by 255` и `by 179` | UBSan, ASan, Coverage | **наш**, регрессия волны «панели/шов/альфа» | `G8` |
| `SplitGpuParityTest` — `splits are not supported here yet` | все Linux-джобы | наш, но не регрессия: Vulkan многопанельных кадров не умел никогда | `G9` |
| `test_wide_ligature_overflow` | UBSan, ASan, Coverage | **средовой, и уже починен апстримом** | не ставится |

### Вердикты `G6` и почему средовой отказ не требует задачи

`G6` (`docs/reports/G6-triage-2026-09-03.md`) установил происхождение двух отказов сравнением с чистым `origin/master` в отдельном дереве.

**Опорное доказательство, проверенное командиром независимо:** прогон апстрима `33579959738` на `1b8279f0`, на том же образе Nix, **выполняет оба теста и оба проходит** (`Tests UBSan` = `success`). Значит унаследованными они не являются — гипотеза «за десять дней красного гейта натекло чужое» опровергнута.

`test_wide_ligature_overflow` — гвард «глиф-бомбы» во FreeType ≥ 2.14.2 (в Nix стоит 2.14.3): `U+FDFD` при `-fontsize 20` упирается в него, `FT_Render_Glyph` отказывает, наш `strikeFor()` молча сдаётся. **Апстрим этот гвард уже обошёл** — коммит `3c91f408`, «Render oversize glyphs through the outline path, link simdutf into embed». Командир проверил: он в `origin/master`, к нам не смержен. Отказ уйдёт сам с мержем; своей задачи не требует.

Заголовок того же коммита продолжается словами «link simdutf into embed» — **апстрим независимо от нас чинил ту же связку simdutf, что и `G3`.** Это место будет конфликтным, и разрешать его придётся зная обе истории. Записано сюда, чтобы не всплыло внезапно посреди волны.

**`G6` поправил постановку в двух местах, оба раза по делу.** Отказов на Nix не два, а четыре: у каждого есть близнец. И вердикт «наш» несёт именно близнец (`test_resize_during_retained_update_rebuilds_frame`) — его тело совпадает с апстримным посимвольно, тогда как у названного мной теста тело у нас правлено. Список в брифе был неполон.

### Происшествие со средой, и чем оно поучительно

`G6` подвесил docker-сборкой демон OrbStack, **и вместе с ним лёг системный резолвер имён на хосте**: `getaddrinfo` падал на любом имени через 30 секунд, при этом сырой TCP до `140.82.121.4:443` открывался, а `dig` резолвил нормально. То есть сеть была цела, сломан был только резолвер macOS.

Обход на время: `git -c http.curloptResolve=github.com:443:<IP> push` и `GODEBUG=netdns=go` для `gh` — обе команды ходят мимо `getaddrinfo`. Пуш `3c6b8440` ушёл именно так.

Починка оказалась не в системе: демон ожил сам, контейнер `g6ours` висел пятый час, и **после `docker rm -f g6ours` резолвер заработал немедленно**. Sudo не понадобился — командир успел попросить его у человека и снял просьбу.

**В бриф каждого агента, которому нужен docker:** не собирать под эмуляцией amd64 (машина arm64), давать контейнерам имена, снимать за собой. Подвешенный демон ломает не только своего агента, но и сеть всей машины.

### `G7` и `G9` закрыты

Смержены в `master`: `4d76e05c` (`G7`), `fc4dc233` (`G9`). Отчёты — `docs/reports/G7-pretty-config-2026-09-03.md`, `docs/reports/G9-split-parity-2026-09-03.md`.

**`G7` нашёл вторую половину той же рассинхронизации, о которой его не спрашивали.** Коммит `f805e538` («R9-qa: the shipped sample config had gone stale») чинил ровно эту пару — `sidebarTabs`→`tabBar` и документацию `paneDividerWidth`/`paneDividerColor` — и **тоже применил правку только к `bin/st/shitty.toml`**. То есть `bin/pt/pretty.toml` забыли дважды подряд, включая задачу, специально занимавшуюся устареванием конфигов. Внесено в `CLAUDE.md`.

Проверка командиром: на `master` `tst/production_surface.py` даёт `FAILED (failures=1)`, на ветке `G7` — `OK`, 5 тестов. С полным окружением (`SHITTY_PRODUCTION_BINARY`, `SHITTY_PRETTY_BINARY`, `SHITTY_TEST_VERSION`); без него — 3 ложных отказа на обеих ветках.

**`G9` решил задачу через способность бэкенда, а не через платформу.** Тест строит двухпанельный кадр и читает ответ: на точную фразу отказа Vulkan отходит в сторону, повторяя её как причину, на любой другой отказ падает. Довод против платформенного признака — не теоретический: пропуск по платформе `test_gpu_smoke.py` **уже отменял двумя коммитами**.

Проверка командиром на живом прогоне: `test_a_split_frame_is_the_same_picture_on_both_backends ... ok` при `SHITTY_TEST_VULKAN_REQUIRED=1` — **выполнен**, не пропущен. Сторож `F3` цел.

### Три находки `G9`, которые меняют метод

1. **Отказ приходит на `write()`, а не на `present()`.** Обёртка вокруг презентации — очевидное место и не сработала бы; выяснилось бы это только следующим прогоном CI.
2. **Команды протокола, называющей бэкенд, нет.** `VULKAN_SHADOW` отвечает 0/1. Вопрос возникал уже дважды (`G9` и `VulkanBlitSmokeTest`); на третий раз дешевле завести `GPU_BACKEND` в `test_mode.cpp`.
3. **Критерий `./build st --clear` доказывает не то, что командир думал.** Питоновские тесты идут от `st_test` через цель `test`; продуктовый `./st` на `--test-fd` отвечает `unknown option`. Критерий стоял в каждом сегодняшнем брифе и проверял сборку не того бинарника.

### Недостающая поддержка сплитов в Vulkan — теперь именованная работа

`G9` §5: восемь полей состояния в `render_vk.cpp` (с номерами строк) заведены на поверхность вместо панели; два пути прицелить dispatch в срез буфера ячеек (рекомендовано динамическое смещение дескриптора — остаётся в одном бэкенде, против нового поля в общем блоке push-констант); `present()` (2084-2249) требует разложения на «кадр» и «панель»; в `recordCommands()` цикл, где заливка и швы уже написаны наперёд. **350-500 строк из 2417, один-два дня**, эталон для сверки — Metal.

Пропуск устроен так, что **сам себя снимет**: когда работа сядет, сравнение начнёт выполняться на Vulkan без единой правки в тесте.

### Заведён `CLAUDE.md`

Его в проекте не было; отсутствие числилось отложенным ещё с прошлого плана. Семь разделов, каждый оплачен отдельной задачей: окружение для ручного прогона (четыре ложных прогона командира), `./build st` не строит тесты и не запускает проверок (три дефекта, дошедшие до CI), локальная сборка на macOS не свидетельство, два признака одной зависимости, два бренда из одного дерева, забываемый `pretty.toml`, две ловушки кеша сборки, docker и резолвер.

**Записать оказалось легче, чем применять:** уже после коммита `CLAUDE.md` командир пятый раз за сессию вызвал тест без нужного окружения.

### `G8` и `G10` закрыты — CI сведён к одному классу отказов

Смержены: `ebfc36a2` (`G8`), `0651a01f` (`G10`). Отчёты — `docs/reports/G8-vulkan-blit-2026-09-03.md`, `docs/reports/G10-root-writable-2026-09-03.md`.

**`G8`: коммит-источник найден бисектой, не рассуждением** — `eed01bbb` («WIP RED: F9 divider painting»). Проход заливки панели шёл на **каждом** кадре, а Vulkan перерисовывает только строки из журнала повреждений: заливка красила фоном всю панель, поверх ложились лишь свежие строки, а удержанное с прошлых кадров стиралось проходом, который задуман лежать **под** ячейками.

Почему падал только blit-путь: на прямом свопчейн крутит три картинки, та, в которую пишет кадр, почти всегда устарела, `requiresFull()` истинно и приходят все строки. На blit-пути картинка одна, и уже второй `present` инкрементальный. Metal не видит вовсе — чистит текстуру и перестраивает все ячейки каждый кадр.

`255` и `179` — **один дефект**: яркость самого яркого потерянного пикселя, сплошная заливка глифа против сглаженного штриха.

Правка — одно условие: заливка спрашивает `damage.requiresFull(...)`, тот же вопрос, которым `materializeUpdates()` решает, выдавать ли все строки. Фича не откачена. Подтверждено на живом CI (`33716488615`): оба `VulkanBlitSmokeTest` из отказов исчезли.

**Находка `G8`, которая важнее его задачи: волна F9/R9/T10 не собиралась на Linux ни на одном из своих тринадцати коммитов.** Измерено бисектой. Красный гейт не просто скрывал результат — под ним две недели копилась работа, ни разу не проверенная на второй платформе.

**`G10` поправил гипотезу командира наполовину.** Root — подтвердился: обе контейнерные джобы стартуют без `options: --user`. А «только Alpine» — **нет**: `Tests Fedora` работает от того же root и просто не доходила до C++-юнит-тестов, падая раньше на питоновской группе (`unit-tests/group` в её логе 0 раз против 11 у Alpine). После починки питоновского отказа Fedora упала бы там же.

Решение выбрано то, которого требовал бриф, а не запасное: отказ записи вызывается **висячей symlink** на месте временного файла — `open()` идёт по ней и получает `ENOENT`. Это не право доступа, поэтому держит и под root. **Пропусков не добавлено, инвариант проверяется на всех джобах, включая единственную musl.**

Разборчивость теста доказана подстановкой дефекта в продуктовый код под root: `return false` → `return true` в `catch` внутри `saveQuickFrame()` даёт `ERR: 1`, откат — `OK: 20`.

**Оговорка, названная самим `G10` и принятая командиром:** финальная проверка «временного файла не осталось» стала слабее имени — `access(F_OK)` на висячей symlink не отличает «файла нет» от «symlink на месте». Настоящую уборку проверяет соседний тест `WriteFailureAfterOpenRemovesTheTemporaryFile`, который валит запись через `RLIMIT_FSIZE` (под root действует, проверено). Главный инвариант — прежний кадр цел, `saveQuickFrame` вернул ложь — проверяется полностью.

### Состояние CI: остался один класс отказов, и он не наш

После шести задач (`G3`, `G4`, `G5`, `G7`, `G8`, `G9`, `G10`) на всех джобах остаётся **только** `test_wide_ligature_overflow` — гвард «глиф-бомбы» во FreeType 2.14.3, вердикт `G6`: **средовой, и уже починен апстримом** коммитом `3c91f408`. Своей задачи не требует: уйдёт с мержем.

Это и есть точка, в которой волна 5 становится осмысленной — критерии приёмки волн 5-8 теперь проверяемы, потому что красное в CI означает поломку, а не унаследованный шум.

## Волна 5 — `M5` смержен, из трёх задач осталась одна

`M5` (`5e97735a` в `master`, ветка `wave/upstream-merge-w5`, мерж `892fc8db`). Отчёт — `docs/reports/M5-span-shaper-2026-09-03.md`. `behind` 86 → 85.

**Точка остановки про `CellExtraClient` не сработала: вопрос не встал.** Место нашлось — тот же прокси, который апстрим пересоздал в `span_shaper.cpp`; наша правка (`Listener` → `CellExtraClient`, `onListen` → `extrasCollected`) переехала туда вместе с кодом. Замысел апстрима не менялся, `T5.3` не усложняется.

**Мина, которую взятие апстримной стороны дословно поставило бы молча.** Апстрим кладёт в `composer.cellExtrasChangedListeners` объект `Listener`, а наши `collect()` и `setCellExtras()` делают `static_cast<CellExtraClient*>`. Это **компилируется и линкуется**, и попадает в чужой vtable-слот. Ни один из пяти гвардов такого не ловит.

**Апстрим сам портировал эталонный рендерер и Vulkan.** Оценка плана относилась к состоянию до мержа; после него:

| Задача | Было по плану | Стало |
|---|---|---|
| `T4.1` Metal | перевод на `SpanShaper` | **сузилась** до снятия `PaneArenaMirror` — Metal апстрим не трогал, `M5` перевёл механически, иначе `./build st` красный |
| `T4.2` Vulkan | «наша работа, апстрим этого не делал» | шейперная половина **исчезла**; оставшаяся сплит-поддержка — предмет `G9`, уже разобранный |
| `T4.3` эталонный | перевод на `SpanShaper` | **работы ноль** |

Это ровно тот механизм, который записан в плане после `M4`: конфликтные хунки разрешает сам мерж, поэтому остаточная работа систематически меньше оценки. Оценивать её надо после мерж-шага, а не до.

**Гвард нашёл настоящее нарушение `A1`**, принесённое апстримом в новом файле `span_shaper_ut.cpp`. Новый файл конфликта не создаёт, и наша правка до него не дотягивается — то есть поймал именно гвард, а не человек. Исправлен код, не разрешение конфликта.

Проверка командиром: `./build st --clear` — **225 узлов**, `unit_tests` — `OK: 955`. Питоновский набор `M5` прогнал **на самой предмержевой голове** в отдельном дереве: `6398 / 6 / 14 / 17 / 549` с обеих сторон, двадцать красных совпали поимённо, `diff` пуст.

### Четвёртый вид «зелёного, который ничего не проверяет»

`span_shaper_ut.cpp` на macOS компилируется в **336 байт** — весь файл под выключенным `#if HAVE_FREETYPE`. Ни сборка, ни прогон тестов об этом не сообщают. Проверено командиром независимо: объектник ровно 336 байт.

Предыдущие три: штамп гварда из CAS (пустой вывод и `exit 0` не доказывают исполнения); `build.includes` вне ключа узла (сборка не компилирует ничего и молчит); тест, зелёный 30 раз из 30 при снятой сути задачи (`T5` прошлого плана).

## `G11` закрыт — и постановка командира была неверна дважды

`d372089c`. Отчёт — `docs/reports/G11-split-chord-musl-2026-09-03.md`.

**Причина не в musl и не только в Alpine.** Сплит-чорды существуют только в `__APPLE__`-половине `defaultBindings[]`; в `__linux__`-половине (строки 119-131) их **ноль** — проверено командиром. Отказ есть на ASan, UBSan и Coverage, все три glibc; воспроизведён локально на Fedora. Fedora выглядела невиновной только потому, что вставала раньше — на питоновской группе — и до юнит-тестов не доходила.

**Это второй раз подряд, когда командир приписал отказ конкретной джобе по тому, что видно в логе.** Первый — `G10`: «только Alpine», а Fedora тоже от root и просто не дошла. Ошибка одна и та же: вывод «где падает» делается из того, где **всплыло**, без поправки на джобы, вставшие раньше. Дешёвая защита, применённая `G11`: считать вхождения строки `unit-tests/group` в логе прежде, чем говорить «только там».

Решение — не `#if` вокруг всего теста, а **утверждение отсутствия** в Linux-ветке: ни одна из трёх форм чорда не делит вкладку и не порождает второй оболочки. «Привязки нет» становится записанным решением; добавить туда сплит нельзя, не приняв решения об этом тесте. Разборчивость показана тремя подстановками, включая ровно тот дефект, который видел пользователь.

**Факт для человека:** на Linux сплит с клавиатуры недоступен вообще. По плану панели — macOS-овская работа, так что это не поломка, но и не случайность, о которой стоит узнать позже.

### Две находки о среде, не чинились

- **GCC на Linux/aarch64 ломает `swapContext`** в `ext/libstd` (naked-функция, чей asm не упоминает параметры): любой тест с волокнами падает сегфолтом и на musl, и на glibc. Clang работает. CI не задет — там x86_64.
- `fedora:44` в контейнере не ставится по инструкции из `ci.yml`: `dnf` валится на `openh264`, обходится `--disablerepo="*openh264*"`.

## Волна 5 закрыта: `PASS_WITH_FINDINGS`

`R5-test` — `393f2c4e`. Отчёт: `docs/plans/reviews/upstream-merge-R5-test.md`.

**Оракул, объявленный неисполнимым, построен.** `T4.1` §7.1 писал, что путь «сбор арены посреди многопанельного кадра» локально не воспроизводится. Проверяющий заметил, что бюджет арены — функция размера окна, которое тест строит сам: тысячи глифов не нужны, хватает двух десятков полос. `MetalPanes::APaneKeepsItsInkWhenALaterPaneCollectsTheArena` — три панели, одна молчит, её прямоугольник с первого кадра эталон; инструментацией подтверждено `collected during pane 1 of 3`.

**До ревью `assignFrameStrips` не покрывался ничем:** мутация «вернуть панельный цикл» проходила все 949 юнитов и 20 групп питона молча. То самое расширение цикла с панели на кадр, которое `T4.1` назвал условием корректности снятия зеркала, было единственной непроверяемой частью волны.

Сторож `rowIdentityCounter` подтверждён подстановкой независимо: перенос счётчика в `ScreenBase` → `OK: 948, ERR: 1`, красный ровно он. У `T4.1` в том же измерении было пять лишних красных — забытая `SHITTY_PTY_TEST_HELPER`, **шестой случай этой ловушки за сессию**, теперь у исполнителя.

Проверка командиром: списки тестов сверены поимённо — пропали ровно 12 `PaneArenaMirror::*`, добавились 5 `ArenaMirror::*` и сторож; 955 − 12 + 6 = 949, ни один прежний тест не потерян. `T4.1` отказался дописать шесть тестов до круглого числа, и был прав: критерий командира подталкивал ровно к зелёному, которое ничего не проверяет.

### Находки, перенесённые (волна закрыта, но они остаются)

| Уровень | Что |
|---|---|
| важная | Мутации `M5` (плоское смещение полосы) и `M6` (цветная плоскость) не видит **ни один** юнит-тест: `DrawThreeGridsInOneFrame` пишет всюду одну букву `W`, а новый оракул сравнивает Metal с Metal и слеп к дефектам, постоянным между кадрами |
| важная | Отчёт `G9` устарел за три часа: предписывает завести в `render_vk.cpp` два `PaneArenaMirror` «как в Metal». Структуры больше нет; переносить надо не её, а скаляр `stripGeneration` (`render_vk.cpp:252`) — ему для сплитов нужен один `ArenaMirror`. Два комментария (`:247`, `:2295`) ссылаются на снятый тип |
| замечание | Паритет с Vulkan на этой машине **невозможен**, а не «пропущен»: `render_vk.cpp` компилируется только под Linux. Суита `SpanShaper` здесь не исполнялась ни разу (336 байт), `MetalPanes` не компилируется на Linux — **ни одной платформы, где исполняются обе** |

## Darwin-тесты включены на push в master — решение человека

`R5-test` обнаружил, что пять джоб `Tests Darwin` объявлены `if: workflow_dispatch && inputs.darwin_tests` при `default: false`, то есть **не запускались ни разу**. В обычном CI не исполнялись девять тестов `MetalPanes`, оба оракула волны 5 и Metal-овский `test_gpu_parity`; три из семи мутаций `R5-test` прошли бы там зелёными.

Вопрос вынесен человеку, потому что это его минуты macOS-раннеров. **Решение: включить на push в master.** Правка сделана командиром (`.github/workflows/ci.yml`, пять строк `if:`) — файл не принадлежит ни одной задаче, и проверяется правка только прогоном. `on.push.branches` там уже ограничен `master`, поэтому условие — `github.event_name == 'push' || (workflow_dispatch && inputs.darwin_tests)`. Джобы `Sandboxed` не тронуты, остаются ручными.

**Следствие для командира:** каждый пуш борда теперь стоит пяти macOS-шардов. Пуши борда надо объединять с пушами кода, а не отправлять отдельно.

### Первый в истории форка прогон Darwin-тестов — все пять шардов красные

Прогон `33774317579`, вручную, на `393f2c4e`. Шесть отказов на шарде 0/5:

```
ERROR: test_box_drawing_spans_the_whole_cell_at_the_font_stem_width   tst/test_bitmap_font_render.py
ERROR: test_descenders_hang_below_the_strike_baseline                 tst/test_bitmap_font_render.py
ERROR: test_strike_metrics_shape_the_cell                             tst/test_bitmap_font_render.py
ERROR: test_color_zwj_grapheme_renders_to_image                       tst/test_color_font_render.py
FAIL:  test_sheared_tail_lands_in_the_captured_blank                  tst/test_italic_overhang.py
FAIL:  test_soft_zero_departs_from_the_hinted_grid                    tst/test_soft_render.py
```

**Локальный прогон оракулом не является, и это проверено, а не предположено:** те же четыре файла локально дают `Ran 13 tests, FAILED (failures=3, errors=10)` — падают все тринадцать, потому что на macOS у нас сборка без Homebrew (`build.py:177`, коммит `62cef373`), FreeType и fontconfig выключены. В Nix Darwin они есть, и там падают только шесть. **Локальное окружение беднее CI-шного**, поэтому «локально тоже красное» не доказывает ничего.

Разбор происхождения — задача `G12`.

### Linux сведён к одному средовому отказу

После `G11` на Alpine остаётся только `test_wide_ligature_overflow` — тот самый, что чинится апстримным `3c91f408`. Все прочие классы закрыты семью задачами.

## Darwin разобран до конца: одиннадцать наших закрыты, пять ждут мержа

`G12` (триаж, `6a7f47fe`), `G13` (правка, `f6da2068`), `G14` (`SHITTY_TEST_FONTCONFIG`). Отчёты — `docs/reports/G12-…`, `G13-…`, `G14-darwin…`.

**Постановка командира была неверна трижды, и каждый раз по-разному.**

1. **Отказов не шесть, а шестнадцать.** Шесть — доля шарда `0/5`. Двадцать групп собираются как сорок целей (обычная и `prod_parser`) и раскладываются по пяти шардам хешем имени, поэтому одна группа исполняется дважды и её отказы видны дважды. Одиннадцать методов исполнились по два раза с одинаковым результатом — детерминированы, не мигания.
2. **Гипотеза про `3c91f408` не подтвердилась.** На Darwin тесты широкой лигатуры зелёные в обоих деревьях: рисует CoreText, глиф-бомбы FreeType там нет. Командир перенёс объяснение с Linux на Darwin без проверки.
3. **Апстрим `Tests Darwin` гоняет, и они зелёные** (`33579959738`). Более того, апстрим прошёл ровно этот путь: 24 августа впервые дёрнул матрицу руками, она посыпалась, он починил и 25-го снял гейт. Мы повторили его на девять дней позже.

**Средовых отказов нет ни одного.** Два источника:

| Сколько | Источник | Что делать |
|---|---|---|
| 11 | наш `62cef373` — условие на `darwin`, а не на «из Homebrew»; гасит FreeType и fontconfig и в Nix | `G13`, сделано |
| 5 | апстрим **добавил** платформенные гварды `c049a672`, `ee9a576a`, `18fc9a20` (все 24 августа), у нас их нет | уйдут с мержем |

Улика по первой группе, проверенная командиром независимо: в нашем Darwin-логе `font_freetype` компилируется **ноль раз**, при том что `freetype-2.14.3` в nix-путях того же лога присутствует. У апстрима — 15 раз, при дословно совпадающих `buildInputs`.

### `G13`: признак — неизменяемость, а не платформа

Пакет остаётся включённым на macOS, только если **все** пути, которые pkg-config отдаёт компилятору и линковщику, лежат под корнем стора (`$NIX_STORE`). Это спрашивает ровно то свойство, о котором рассуждал `62cef373` — «библиотека, до которой `brew upgrade` не дотягивается», — а не платформу, с которой оно совпадало. `flake.nix` не понадобился: договариваться двум файлам не о чем, второй признак не заводится.

Проверка — подставной стор (сторовые имена, свои `.pc`, симлинки на настоящие библиотеки): без признака 225 узлов и `font_freetype` ноль раз, с признаком 226 и объектник собран; все одиннадцать тестов воспроизведены как отказы в off и проходят в on, имя в имя.

**Названы семь условий, при которых признак ошибётся.** Главное: `nix profile install` на машине разработчика проверку пройдёт, но `nix store gc` путь удалит — признак доказывает неизменяемость, не живучесть.

**Подтверждено на живом CI** (`33780219855`): `Tests Darwin 1/5` — первый зелёный Darwin-шард в истории форка; на остальных четырёх остались ровно пять унаследованных, поимённо совпавших с триажем `G12`.

### `G14`: три находки, объясняющие шум всей сессии

- **`SHITTY_TEST_FONTCONFIG` врал.** Ветка «шрифтового бэкенда нет» переприсваивает `fontconfig = dependency()` — **включённую** пустую зависимость, а `Target.__bool__` возвращает `enabled`. Локально без fontconfig флаг был `"1"`, то есть тесты под `skipUnless` исполнялись там, где обязаны пропускаться. Проверено командиром: теперь `"0"`.
- **Утверждение `G13` «разъехаться не могут» неточно.** Глобальные `build.cppflags` доносили три `HAVE_*` до **27 объектов чистого C** (`vttest`, `pty_test_helper`, `wraptest`), которые с этими библиотеками не линкуются. Безвредно лишь потому, что ни один их не читает — проверено `grep`. Приведены к форме `G3`: define на объекте зависимости.
- **`tst/test_font_fallback.py:10` импортирует класс, а не модуль** — девять тестов `FontResolverTest` исполняются в наборе **дважды**. Отсюда 6399 при 6390 уникальных и `failures=8` при семи разных именах. Командир весь день сверял эти числа, не зная про двойников.

Ещё: **пять из шести тестов под `skipUnless(fontconfig)` раньше проходили** через CoreText и теперь честно пропускаются — декоратор шире, чем нужно (`tst/**`, не трогали). И ловушка `dependency()` осталась в трёх местах (`darwin_backend`, `vulkan`, `wayland_backend`) — сегодня безвредна, никто не спрашивает их как булево.

## Состояние CI: всё красное чинится мержем

| Платформа | Что осталось | Чем закроется |
|---|---|---|
| Linux | `test_wide_ligature_overflow` | апстримный `3c91f408` |
| Darwin | пять тестов, поимённо выше | апстримные `c049a672`, `ee9a576a`, `18fc9a20` |

**Ни одного отказа, требующего нашей работы, не осталось.** Это и есть условие, при котором волны 6-8 проверяемы: красное в CI снова означает поломку.

## Решение человека, 2026-09-04: Liquid Glass — после мержа

Пользователь спросил, что нужно для «эффекта стекла». **Разбор до планирования показал, что матовое стекло уже есть:** опции `-backgroundOpacity 0..100` и `-backgroundBlur`, реализованы `PltBackdropView: NSVisualEffectView` (`ext/plt/platform_cocoa.mm:1302-1319`), материал `NSVisualEffectMaterialUnderWindowBackground`, режим `BehindWindow`. Хром согласует прозрачность через `ui_window_tint.h` — волны `S10`/`T10`. На Linux/Wayland размытия нет вовсе.

Значит вопрос был про другое стекло. Машина на macOS 26.5.2, SDK 26.5 содержит `NSGlassEffectView.h` (`API_AVAILABLE(macos(26.0))`): `style` (`Regular`/`Clear`), `cornerRadius`, `tintColor`, плюс `NSGlassEffectContainerView`, сливающий соседние стеклянные виды в один проход.

**Решения:**

| Вопрос | Ответ |
|---|---|
| Объём | Liquid Glass **включая поверхность под текстом**, не только хром |
| Порядок | **Сначала домержить апстрим**, потом стекло |

**Риск назван до выбора и принят.** `NSGlassEffectView` оборачивает `contentView`, и заголовок SDK прямо предупреждает, что z-порядок произвольных сабвью относительно стекла не гарантируется — а у нас там `CAMetalLayer`. Поэтому работа обязана начинаться с **read-only разведки**: что вообще возможно с нашей иерархией вью, чего это стоит по кадру, и не ломает ли `A1`/`A8`. Планировать через `/coding-plan` **после** разведки, а не до.

Порядок выбран верно и по той же причине, что и раньше: стекло трогает ровно те файлы — `platform_cocoa.mm`, хром, рендереры, — которые перекраивают волны 6-8. Делать его сейчас значит платить за конфликты дважды.

## Волна 6 начата

Нарезана по одному апстримному коммиту на шаг (перенумерация от `R4-arch`), и блокирующая `T5.1` стоит только на третьем:

| Шаг | Коммит | Статус |
|---|---|---|
| `M6` | `25dbda61` — embedding surface, `vt_state.{h,cpp}`, `ansi_palette` в `lib/vterm` | идёт |
| `M6b` | `f3de9de6` — ядро переезжает в `lib/vterm` | ждёт |
| `M6c` | `bd86ed38` — растворение `VtState`, `vt_geometry.{h,cpp}` | **точка остановки: `T5.1`** |
| `M6d` | `9bf5e497` — вычистка понятий эмбеддера | ждёт |
| `M6e` | `7e2a8e3e` — префикс `vt_`, `vt_headless.*` | ждёт |

Объём `M6` по `git diff --stat`: 88 файлов, +2073/−22496, из них новые `lib/vterm/vt_state.{h,cpp}`.

## Решение человека по `T5.1`: вариант «А»

Документ архитектора — `docs/plans/reviews/upstream-merge-T5.1-decision.md` (609 строк), смержен в `master`.

**Принято:** заменить `borderPixels` четырёхсторонними `VtInsets` и добавить `originX`/`originY`/`width`/`height` прямо в `VtGeometry`, закрыв нашу `PaneGeometry`. Один источник правды.

**Довод §4.3 против варианта «А» на дереве не подтвердился, и это перевернуло рекомендацию.** Он звучал так: «меняет публичное поле апстримного ядра, на которое смотрит C-фасад `lib/embed`». Проверено командиром независимо на `60562f22`:

| Утверждение | Проверка |
|---|---|
| `lib/embed` не публикует `VtGeometry` | `shitty_vt.h` объявляет четыре типа, её среди них нет; наружу геометрия идёт только как `uint16_t columns, rows` |
| структура — приватное поле реализации | одно упоминание, `shitty_vt.cpp:50`; в заголовке и в `.map` — ноль |
| апстрим её почти не трогает | из 82 коммитов после `bd86ed38` файл задел **один** (`9bf5e497`), и только словом в комментарии |
| в нашем ядре `borderPixels` уже нет | **ноль** вхождений в `lib/vterm/` — следствие решения `M6` §3.2 |

Последняя строка меняет смысл варианта «Б»: он считался консервативным («сохранить апстримное»), а на сегодняшнем дереве означает «внести в ядро поле, которого там нет». Консервативным он быть перестал.

**Предельная цена «А» измерена, а не оценена:** 2 файла, 86 строк становятся нашими навсегда.

**Что архитектор добавил сверх §4.3:**

- Гвард проверен пробой в обе стороны: `\bborderPixels\s*\(` считает **форму вызова**, поле `geometry.borderPixels` — нет. Ловушка плана реальна только для *метода*, а решение метода не добавляет.
- `vterm_boundary`: на живом дереве **8 строк в 3 файлах**, а не «четыре нарушения», как писал §4.3. «А» закрывает 2 выкладкой и делает возможной третью; 4 уходят с `T5.9`, последняя с `T5.4`. **Обещать зелёный гвард в приёмке `T5.1` нельзя** — это войдёт в критерии задачи.
- Третий вариант «Г» (держать всё в `PaneGeometry`, `vt_geometry` побайтово апстримный) назван и отвергнут: две структуры с полями `columns`/`rows` разного смысла — ровно тот тихий сдвиг текста, от которого защищаемся.
- Две ловушки, названные до кода: `origin` меняет базу отсчёта (`originX_new = chromeInsets().left + originX_old`), и четыре окновых места в `vterm.cpp` (`columnsForPixelWidth`, XTWINOPS) нельзя механически перевести на инсеты панели в `T5.5`.
- Побочно: `grid_geometry.h` расщеплять не нужно — `T5.2` сокращается, и открытый пункт §3.3 про раздвоение ключа в `border_pixels_allowance` закрывается сам.

**Оговорка архитектора, принята:** дерево он не собирал и тесты не гонял; список из семи краснеющих тестов получен чтением. Исполнял только `check_includes.py` и вытащенную из `build.py` программу гварда.

## `M6c` оборван лимитом аккаунта

Агент умер посреди мержа `bd86ed38` — 45 файлов, конфликты не разрешены, работа по разрешению не начиналась (последняя фраза: «Now I'll build the substitution-driven resolution»). `git merge --abort` выполнен командиром, дерево `wt-M6c` чистое. Терять было нечего.

### Дефект в брифах командира, найденный этим обрывом

Весь день агентам предписывалась проверка перед `git add`:

```
ls .git/MERGE_HEAD
```

**В связанном worktree `.git` — файл, а не каталог**, поэтому команда всегда отвечает `Not a directory`, и проверка «мержа нет» проходит **всегда**, в том числе посреди чужого мержа. Измерено на `wt-M6c`: `git rev-parse -q --verify MERGE_HEAD` печатает `bd86ed38…`, а `ls .git/MERGE_HEAD` — `Not a directory`.

То есть защита, введённая после трёх коллизий деревьев, во всех worktree не срабатывала ни разу. Верная форма записана в `CLAUDE.md`:

```
git rev-parse -q --verify MERGE_HEAD && echo "МЕРЖ ИДЁТ" || echo "мержа нет"
```

## `M6c` смержен — `a9cc139b`, `behind` 82

Отчёт — `docs/reports/M6c-vtstate-dissolved-2026-09-04.md`. Проверка командиром: 226 узлов, `OK: 950`, `vterm_boundary` — **те же восемь строк**, список не вырос.

**Граница с `T5.1` соблюдена и подготовлена.** Реализовывать вариант «А» не пришлось: ни один конфликт этого не потребовал. Ближайшая развилка — откуда `Composer::borderPixels()` берёт число после исчезновения `VtState::baseBorder` — разрешена **в пользу «А»**: берётся `opts->border`, апстримное поле `geometry.borderPixels` оставлено незаполненным. `T5.1` придёт на готовое место, а не будет переделывать.

### Пятый вид «зелёного, которое ничего не проверяет» — теперь в сборке

Дерево собиралось, `st` линковался, `./build st pt --clear` был зелёным — и **все 65 тестов `SessionSet` падали с SIGSEGV** на нулевом `composer.host`. Продуктовый путь вызов `installVtHost()` содержал (приехал автомержем), девятнадцати тестовым фикстурам его не хватало. Поймал только прогон `unit_tests`.

**Собралось ≠ работает**, и это уже не риторика: зелёная сборка не свидетельствовала даже о том, что программа переживёт первый тест. Внесено в брифы следующих шагов как ловушка №1, с явным указанием, что критерий про `unit_tests` важнее критерия про сборку.

Предыдущие четыре: штамп гварда из CAS; `build.includes` вне ключа узла; тест, зелёный 30 из 30 при снятой сути задачи; `span_shaper_ut.cpp` в 336 пустых байт.

### Шестой — и он про сами пробы

**Проба-комментарий зеленит все четыре гварда сразу:** `blanked()` вычищает тела комментариев перед сканированием. На первом заходе `M6c` это выглядело как «гварды ослепли от растворения `VtState`». То есть **неверный способ пробы имитирует ровно тот отказ, который пробой и ищут**. Пробу ставить кодом, не комментарием — в брифы внесено.

### Отказ, который стоит отметить как правильный

**48 апстримных строк в трёх рендерерах отброшены**, потому что взять их физически нельзя: `pane_grid_guard` краснеет на каждой. Это `A9` в работе — охрана не позволила молча принять чужое решение о том, кто знает сетку окна.

## Решение командира: порядок остатка волны 6

Задачи `T5.1`–`T5.11` **не запускаются между мерж-шагами**. Сначала `M6d` и `M6e`, потом все `T5.x` против осевшего дерева.

Основание — дважды измеренное на этой волне: конфликтные хунки разрешает сам мерж, поэтому остаточная работа систематически меньше плановой оценки. На `M5` из трёх задач волны так исчезли две; на `M6b` **`T5.7` закрылась мержем целиком**, а `T5.3` сократилась до переименования. Запускать переводчиков раньше мержей значит платить за их работу, которую мерж сделает сам, и потом ещё раз за конфликты.

Блокирующей эта перестановка не была: блокировало **решение** `T5.1`, а оно принято.

## Мерж-часть волны 6 закончена: `M6d` и `M6e` смержены, `behind` 80

| Шаг | Коммит | В `master` | Узлов | `unit_tests` |
|---|---|---|---|---|
| `M6d` | `9bf5e497` | `fa6a5f4f` | 228 (+2, две новые TU) | `OK: 950` |
| `M6e` | `7e2a8e3e` | `68d04998` | 228 | `OK: 950` |

Отчёты — `docs/reports/M6d-embedder-scrub-2026-09-04.md`, `M6e-vt-prefix-2026-09-04.md`. Оба проверены командиром независимо; `vterm_boundary` на обоих — те же 8 строк.

**`uriScheme*` отданы полностью,** как решил план: trie и решение живут в `Options::uriSchemeTrie`/`uriSchemeAllowed()`, ядро спрашивает через `VtHost::uriSchemeAllowed()`, `darts.*` переехали в `lib/shitty`. Для пользователя не изменилось ничего; побочно стало лучше — неразобранный `Options` вместо разыменования нулевого trie честно отвечает «нельзя».

**`M6e`: дифф с апстримом сошёлся в 0 и 0.** Оба конфликта — блоки включений, разрешены одинаково: наша сторона с применённым апстримным переименованием, ни одна апстримная строка не отброшена.

### Седьмой вид «зелёного, которое ничего не проверяет» — и он мой

`bin/main_fuzz/main.cpp` и `bin/core_perf/main.cpp` **не компилируются с `M6c`**. Ни одна цель `./build test` их не строит, поэтому CI молчит, а мои критерии для мерж-шагов (`./build st --clear` + `unit_tests`) их не трогают.

**Я принял `M6c`, оставивший две программы несобирающимися, и узнал об этом только потому, что следующий агент споткнулся.** Список программ в `build.py` открывается одной командой; я его не открыл.

Починено измерением, а не оговоркой: собраны **все** программы разом, получен точный список — ровно эти две, больше ничего. Заодно выяснилось, что `st_memprofile` в такой критерий брать нельзя (требует gperftools, которого нет ни локально, ни в CI).

`core_perf` был **ничей** — в плане не значился ни у одной задачи. Дописан во владение `T5.11` вместе с готовой командой; та же команда стоит критерием у `M6e`, `T5.1` и `T5.9` как «список сломанного не вырос».

### Восьмой — и он целится в метод проверки

**`./build -k` при сломанной цели не публикует символические ссылки** `.build/st_test`, `pt_test`, `toml_dump`. Они остаются предмержевыми, прекрасно запускаются и дают правдоподобные, но **старые** числа.

Проверки командира это не задело — они идут без `-k` и падают жёстко. Но критерий «список сломанных программ» использует `-k`, и после него запускать тесты нельзя. Внесено в брифы: гонять тесты только после сборки без `-k`.

### Проба на ловушку с гвардами, сделанная строже задания

`M6e` — массовое переименование с префиксом `vt_`, то есть ровно тот случай, на котором `pane_grid_guard` уже слепнул. Исполнитель временно переименовал **файл, стоящий ключом в разрешении**, по шаблону этого коммита — и гвард покраснел **с двух сторон сразу**: узаконенное обращение всплыло как неузаконенное, а осиротевший ключ поймала самозащита.

Значит переименование ключа не даёт зелёной пустоты. Это сильнее, чем требовал критерий, и снимает опасение, ради которого он писался.

## Волна 6 перешла к задачам-переводчикам

Запущены две независимые головы цепочек:

| Задача | Владеет | Цепочка |
|---|---|---|
| `T5.1` | `vt_geometry.{h,cpp}`, `mouse_frontend.{h,cpp}` | → `T5.5` → `T5.6` → `T5.3` → `T5.2`; плюс `T5.4` |
| `T5.9` | `vt_headless.{h,cpp}` | → `T5.10`, `T5.11` |

**Главный критерий `T5.1` — не про сборку.** От исполнителя требуется показать тестом или пробой, что «сколько занято слева» знает **ровно одно место**: искусственно рассогласовать `Composer` и `VtGeometry` и убедиться, что это ловится. Без такого сторожа задача не закрыта — именно этот дефект не падает, а тихо сдвигает текст в правой панели.

**`T5.9` может закрыть `T5.11` попутно.** `M6e` установил, что `bin/core_perf/main.cpp` после мержа побайтово апстримный и вызов в `main_fuzz` тоже апстримный: ломает их единственно наша сигнатура, которую `T5.9` и возвращает к апстримной. От `T5.9` потребована проверка сборкой, а не рассуждением.

## `T5.1` и `T5.9` закрыты — граница включений 8 → 6

Смержены: `de35963a` (`T5.9`), `T5.1` следом. Отчёты — `docs/reports/T5.9-vt-headless-2026-09-04.md`, `T5.1-vtgeometry-2026-09-04.md`.

### `T5.9`: задача в плановой формулировке оказалась неисполнима

Исполнитель отказался её выполнять, и отказ верный. Три довода, **проверены командиром независимо**:

| Утверждение | Факт |
|---|---|
| наш `Vterm::create` шире апстримного | **11 параметров** (`lib/vterm/vterm.h:329`) против **9** (`7e2a8e3e:vterm.h:226`) |
| `windowResized()` у нас нет | единственное вхождение — комментарий, объясняющий замену на `paneResized(const PaneGeometry&)` по `A8` |
| возврат к апстримной сигнатуре ломает `st` | `lib/shitty/main.cpp:104` зовёт нашу форму |

Расхождение, оценённое планом в «17 строк», на деле `+84/−147` — и это **цена `A1`/`A8`**, а не небрежность. Плюс план противоречил себе в одной таблице: `T5.9` требовала девятиаргументного вызова, соседняя строка фиксировала другое.

**План исправлен** (`b9affec6`): в строках `T5.9` и `T5.10` записано, что именно неисполнимо и почему, со ссылками на файлы и строки.

Сделано то, что можно честно: снят мёртвый `#include "options.h"`, исправлены три комментария, обещавшие замену, которой не будет.

**Размен назван вслух вместо тихого выбора:** заголовок разошёлся с апстримом на 7 строк **сильнее** — это комментарий, фиксирующий блокер; предложен к снятию, если ревью решит, что отчёта достаточно.

### `T5.1`: главный критерий выполнен предметно

Сторож единственного источника правды — `MouseFrontend::TheChromeReserveIsCountedOnceOnTheWayIntoAPanesGeometry` — **доказан двумя мутациями, а не заявлен**: `paneInsets()→contentInsets()` даёт `ERR: 4`, «origin забыл хром» — `ERR: 2`, и в обоих случаях он среди поймавших. Гоняет продуктовые `contentBox()`/`paneGeometry()` на вертикальном сплите с 30-пиксельным резервом слева, подметает все 200 пикселей поверхности.

Проверка командиром: 228 узлов, **`OK: 951`** (было 950: одно переименование, один новый тест), тест в списке пройденных, питоновский набор побайтово совпал с прогоном на предзадачной голове.

**Три расхождения с утверждённым документом, все названы:**

1. `mouseGeometry()` берёт **две** `VtGeometry` — панели и окна, — а не одну. Односоставная форма потребовала бы **расширить разрешение гварда**, что запрещено, а окновые `pixelWidth` в экземпляре панели дали бы N копий одного числа, устаревающих между сменой шрифта и ближайшим `resize()`. Отказ от буквы документа сделан ради свойства, ради которого документ писался.
2. `width`/`height` — протяжённость **текста**, а не прямоугольника (сегодняшний контракт `PaneGeometry::width`).
3. **Противоречие внутри самого документа решения:** §7 записывает `vterm.cpp:27` в «закрывает `T5.1`», §2.7 запрещает единственную правку, которая её закрывает. Исполнитель выбрал §2.7 и не стал добавлять мёртвые методы ради числа в отчёте.

### Граница включений: где мы и куда упирается

| | Было | Стало | Кто снял |
|---|---|---|---|
| всего | 8 строк, 3 файла | **6 строк, 2 файла** | `T5.9` — одну, `T5.1` — одну |

Остаток поровну: три в `vt_headless.cpp` (`composer.h`, `pane_layout.h`, `grid_geometry.h`) и три в `vterm.cpp` (`session.h`, `composer.h`, `grid_geometry.h`).

**Зелёного гварда в этой волне не будет, и это надо сказать заранее.** Три строки `vt_headless.cpp` — структурные: они держатся на том же расхождении `A1`/`A8`, из-за которого `T5.9` неисполнима. Три строки `vterm.cpp` расписаны на `T5.2`, `T5.4`, `T5.5`, но §2.7 решения `T5.1` прямо запрещает правку, закрывающую `vterm.cpp:27`.

### Находка, требующая решения: провисающее разрешение

После `T5.1` **`mouse_geometry_allowance` разрешает по одному односоставному `mouseGeometry(` в `mouse_frontend.h`/`.cpp`, где фактических попаданий ноль.** Счётчики исполнитель не трогал — бриф запрещает. Сужение до нулей — правка на одну строку, кандидат для `T5.8` или для арх-ревью волны.

Сегодня не опасно, но это ровно та щель, через которую однажды пройдёт настоящее нарушение: разрешение, не обеспеченное предметом, — то же самое, что ослепший гвард.

## `T5.4` и `T5.11` закрыты — ошибок компиляции ноль впервые с `M6c`

Проверка командиром на собранном `master`: 228 узлов, **`OK: 952`**, **0 ошибок компиляции** среди двенадцати программ, 1 сломанная цель (см. ниже).

### `T5.4`: ноль строк продуктового кода, и это её главный результат

`T5.1` уже передала `Vterm::create` геометрию панели, перевела `openSession()`, `ptySize()`, `applyLayout()` на `VtGeometry` и вынесла `contentBox()`/`paneGeometry()` в `pane_layout`. `grep` по четырём файлам задачи на `PaneGeometry`/`borderPixels` — ноль совпадений. **Единственной невыполненной работой оказался сторож `A5`, и его отсутствие измерено мутацией, а не осмотром.**

**Число, ради которого волна и делалась:** мутация «воспроизведение состояния окна приделано к `show()`, то есть адресуется всем панелям вкладки» на входном дереве давала `OK: 951` — **ни одного красного из 951**. Дефект, о котором план предупреждал ещё в разведке (`T0.3`, «Обнаружено»), не ловился **ничем**. С новым сторожем `SessionSet::ActivationReplaysTheWindowsFocusToTheFocusedPaneAndNotToEveryPaneOfTheTab` та же мутация даёт `ERR: 1`, и единственный красный — он.

Проверено командиром: 66 тестов `SessionSet` (было 65), сторож в списке пройденных.

**Три находки:**
- **`vterm.cpp:22` (`session.h`) из файлов `T5.4` закрыть нельзя.** Включение держится на единственном вызове `composer.sessions->cellCapacityExcept(this)` (`vterm.cpp:3308`, инвариант `A11`). Развязка требует интерфейса в `lib/vterm` плюс правок в `vterm.cpp` и `composer.h` — оба файла исполнителю запрещены. Предложение записано для `T6.1`.
- **Старый тест ловит мутацию не за то, чем называется:** `MovingTheFocusTellsBothPanesAndMovesNeitherOffScreen` падает на утверждении про сплит, диагностика уводит от `A5`. Тест, краснеющий по неверной причине, немногим лучше молчащего.
- **Половина `pointerPresence` не наблюдаема снаружи терминала** — мутация, двигающая только её адресата, не ловится ничем. Отдано `T5.10`.

### `T5.11`: критерий командира был недостижим по построению

Я потребовал «ноль сломанных программ». **`main_fuzz` не линкуется и не линковался никогда:** он определяет `LLVMFuzzerTestOneInput` и берёт `main` из рантайма libFuzzer, которого нет ни в Apple-тулчейне, ни в Homebrew-llvm.

Проверено командиром независимо: `git log -S'int main(' -- bin/main_fuzz/main.cpp` **пуст**, `grep -c "fsanitize=fuzzer" build.py` — **0**. Состояние записано в трёх отчётах предыдущего плана (`panes-status.md`, `panes-R3-test.md`, `panes-R5-qa.md`) — я их не прочитал и поставил критерий по своей арифметике.

Ошибка компиляции **заслоняла вторую, независимую и куда более старую поломку**. Критерий в плане исправлен (`3177fc71`): «без ошибок компиляции», а не «ноль сломанных целей». Настоящий итог: `4 упавших / 2 сломанных` → `1 / 1`, ошибок компиляции **2 → 0**.

**Критерий «не просто собирается, а запускается» закрыт по-настоящему:** фаззер слинкован со standalone-драйвером через штатный `LDFLAGS`, 300 направленных seed'ов приняты; **что тело работает, доказано пробой** `check(false, …)` в `comparePty` — `EXIT=134`, SIGABRT с сообщением инварианта. Без второй половины «300 seed'ов приняты» означало бы только, что программа не падает.

**Две честные оговорки исполнителя:**
- **Дыра в покрытии фаззера.** `validateRig()` — весь блок проверок целостности — зовётся единственный раз (`main.cpp:652`) и только для «диких» терминалов. Проба внутри него на 300 seed'ах не сработала **ни разу**, тогда как та же проба в `comparePty` падает на первом seed'е. Направленная кампания проверяет дифференциальное равенство пары, но не инварианты.
- **Добавленная им цель сборки пока инертна.** `add_test(parser_perf, core_perf, instrumented=False)` начнёт давать сигнал только после того, как `vterm_boundary` позеленеет: сейчас `./build test` без `-k` останавливается на границе раньше. А в этой волне граница не позеленеет — записано выше.

## Закономерность волны 6, которую стоит назвать

| Задача | Плановая оценка | Фактически |
|---|---|---|
| `T5.7` | перенос `childPid()` | **закрылась мержем целиком** |
| `T5.3` | `CellExtraClient` на новый API | сократилась до переименования |
| `T5.4` | `Vterm::create` на панель | **ноль строк** продуктового кода |
| `T5.11` | новые сигнатуры | по одной строке на файл |
| `T5.10` | перенести +858 строк тестов | вероятно ноль |

Разница каждый раз уходила в мерж-шаги, разрешившие конфликты сами. **Но освободившееся время не пропало: во всех случаях настоящей работой оказалось завести сторожа, которого не было** — и мутации показывают, что это не формальность.

## Волна 6: десять задач из одиннадцати закрыты, `unit_tests` 950 → 961

Смержены `T5.1`, `T5.3` (двумя заходами), `T5.4`, `T5.5`, `T5.6`, `T5.8`, `T5.9`, `T5.10`, `T5.11`. Осталась `T5.2`.

### Главный итог волны — не код, а семь незакрытых инвариантов

**Плановая работа почти вся оказалась уже сделанной мерж-шагами**, и это измерено, а не оценено:

| Задача | По плану | Фактически |
|---|---|---|
| `T5.7` | перенос `childPid()` | закрылась мержем целиком |
| `T5.4` | `Vterm::create` на панель | **ноль строк** продуктового кода |
| `T5.6` | взять четыре пункта апстрима | **полный `NO-OP`**; два пункта адресованы волне 8 |
| `T5.3` | `CellExtraClient` на новый API | переносить было **не из чего** — апстрим этих файлов не трогал вовсе |
| `T5.10` | перенести +858 строк тестов | ноль |
| `T5.11` | новые сигнатуры | по одной строке на файл |
| `T5.2` | расщепить `grid_geometry.h` | **ноль** — файла нет в merge-base, он создан внутри волны |

**Настоящей работой каждый раз оказывалось завести сторожа, которого не было.** Найдено мутациями, ни один не падал и ни один не был виден в выводе тестов:

| Инвариант | Ловило до волны |
|---|---|
| `A5` — воспроизведение фокуса адресуется панели, а не вкладке | **0 из 951** |
| XTWINOPS в `unit_tests` | недостижим: `allowWindowOps` по умолчанию `false`, ни один тест его не включал |
| окновая граница против границы панели | тест был **ложно-зелёным**: в юнит-тестовом `Composer` нет `Options`, границы нулевые с обеих сторон |
| половина `pointerPresence` | не наблюдаема снаружи терминала |
| дедуп корней в `collect()` | **0 из 957** |
| alt-половина `cellCapacity()` | **0 из 957** |
| якорь IME и начальный размер окна (`T5.2`, в работе) | **0 из 959** на обеих мутациях |

### Вырожденная фикстура — самый частый механизм слепоты

Три случая за волну, и все три одинаковы по устройству: тест написан честно, но выбрана точка, в которой проверяемое ни на что не влияет.

- `T5.5`: в юнит-тестовом `Composer` нет `Options`, `borderPixels()` даёт 0 с обеих сторон — запрещённая §2.7 подстановка проходила тест. Панель в её коммите несёт границу 7 пикселей ровно поэтому.
- `T5.3`: аргумент `roots` у `collect()` не проходился **ни одним из восьми** существующих вызовов — все зовут `(cells, nullptr, 0)`; без негативного контроля красный не отличал бы сломанный дедуп от неработающего аргумента.
- `T5.2`: все три теста якоря IME снимают его на **домашней клетке**, где оба слагаемых нулевые — транспозиция координат курсора и перестановка метрик глифа невидимы. Комментарий `application_ut.cpp:764` это фиксирует прямым текстом.

**Отсюда требование, вошедшее во все брифы волны:** заводишь тест — сначала докажи, что фикстура **различает** проверяемое. `T5.3` сделала это обратной пробой (сломала фикстуру при целом коде и получила красный), `T5.2` получила то же требование явно.

### Отзыв собственного утверждения — `T5.3`

В первой версии отчёта она доказывала неизменность питоновских чисел совпадением CAS-хешей. Перепроверив, **отозвала**: `st_test` не побайтово воспроизводим через `--clear`, хеш до и после очистки кеша разный при том же дереве. Настоящее доказательство привела другое — откат тестовых файлов пересобирает **ноль узлов**, то есть `_ut.cpp` в эти цели не входят.

Командир принимал довод «CAS-хеши совпали» сегодня трижды от разных агентов и **ни разу не проверил**. Впредь принимать только вместе с указанием, что именно доказывается.

### Правки плана по ходу волны

| Что | Почему |
|---|---|
| `T5.9`, `T5.10` | «взять апстримное целиком» неисполнимо: наш `Vterm::create` — 11 параметров против 9, `windowResized()` у нас нет, возврат ломает точку входа `st` |
| `T5.11` | «ноль сломанных целей» недостижимо: `main_fuzz` не линковался **никогда**, ему нужен рантайм libFuzzer |
| `T5.6`, `T5.3` | два пункта адресованы коммиту `7e2a3c15`, который **вне диапазона волны**; `CellExtraClient` у апстрима не существует вовсе |
| `M8` | добавлен пробел: `font_optical.{h,cpp}` и опция `-optical` (`1cc50043`) у нас отсутствуют целиком и в §7 не значились |

**Общая причина трёх из четырёх:** план писался диффом против `origin/master` целиком, поэтому в задачи волны попадали строки, которых в её диапазоне физически нет. Метод, найденный `T5.6` и вошедший в брифы: сравнивать с **merge-base**, а не с `origin/master`.

### Что остаётся открытым

- **`vterm_boundary` не позеленеет в этой волне** — 6 строк в 2 файлах. Три в `vt_headless.cpp` структурные (то же расхождение `A1`/`A8`); три в `vterm.cpp`, и §2.7 решения `T5.1` прямо запрещает правку, закрывающую одну из них. Четыре исполнителя отказались её делать, отказ принят.
- **Обе половины `A11` охраняются врозь** — `cellCapacityExcept()` в `session_ut.cpp`, `cellCapacity()` в `vt_headless_ut.cpp`, связаны только комментариями. `T5.10` работала в этом файле и alt-половину не закрыла.
- **`SessionSetImpl::pointerPresence()` адресует `activeTerminal()`**, тогда как `pointerMotion`/`scroll`/`pointerButton` ходят через `pointerTarget(x, y)`. Панель, над которой указатель реально находится, об его уходе не узнаёт. Проверено командиром: `session.cpp:732` против `session.cpp:1474`. Заявка на волну 7.
- **`M7` не приносит `lib/embed` целиком** — `shitty_vt_memory_usage` и `shitty_vt_set_save_lines` появляются позже. Если `T6.1` зафиксирует список экспортов по состоянию `M7`, волна 8 его сломает.
- **`saveLines` нельзя изменить перезагрузкой конфига** — `ScreenInfo` не несёт потолок, `resizeGrid` сравнивает только колонки и строки. Молчаливое игнорирование опции до перезапуска; закроется с `M8`.

## Остановка 2026-09-04 ~11:26: лимит аккаунта, агенты не запускаются до 15:00 MSK

`T5.2` и `G15` умерли одновременно с «You've hit your session limit · resets 3pm (Europe/Moscow)». Оба worktree проверены командиром — **чистые, незакоммиченных изменений ноль**, терять нечего. Но обе задачи требуют кода, а командир кода не пишет.

### Найденное `T5.2` до обрыва — существовало только в сообщении, записано сюда

**Расщеплять нечего, измерено против merge-base `7e2a8e3e`:** `git diff --numstat` по `lib/shitty/grid_geometry.h` даёт `74 0` — файла в merge-base **нет вовсе**, он целиком создан внутри диапазона волны. Это не апстримный код, который надо разделять, а наш собственный. `Composer` в нём — 0 вхождений. Единственная зависимость от `lib/shitty` — тип `Insets` (`composer.h:71`); вынести арифметику в `lib/vterm` = вынести `Insets` из `composer.h`, что запрещает §2.7 решения `T5.1`. **Остаток расщепления = 0.**

**Сама арифметика `grid_geometry.h` охраняется хорошо** — `grid_geometry_ut.cpp` бьёт по функциям напрямую асимметричной фикстурой (`top=25, right=21, bottom=45, left=11`).

**Но места вызова в `application.cpp` — нет.** База `OK: 959`:

| Мутация | Что сломано | Поймало |
|---|---|---|
| C1 | `application.cpp:758` — `cellOrigin(cursor.posX, cursor.posY, …)`, оси переставлены | **0 из 959** |
| C2 | там же — `cellPixelWidth`/`cellPixelHeight` переставлены | **0 из 959** |
| C3 (контроль) | там же — `contentInsets()` → `Insets{}` | 3 ✓ |
| C4 | `application.cpp:361` — `gridPixelSize(columns, rows, …)` переставлен | 2 ✓ |
| C5 | `application.cpp:349` — `requestMinimumSize(width, height)` переставлен | 1 ✓ |
| C10 | `application.cpp:350` — `reserve.width`/`reserve.height` переставлены | 1 ✓ |
| C11 (контроль) | `application.cpp:759` — `requestTextInputRect(x,y)` переставлен | 3 ✓ |
| C6 | `application.cpp:836` — `showWindow()`: `nCols` → `nRows` | **0 из 959** |
| C6b | `application.cpp:837` — `showWindow()`: `nRows` → `nCols` | **0 из 959** |

**Дыра №1 (C1/C2) — третий за волну случай вырожденной фикстуры.** Все три теста якоря IME (`application_ut.cpp:773`, `:893`, `:981`) снимают якорь на **домашней клетке**: курсор в (0,0), оба слагаемых `column*glyphWidth` и `row*glyphHeight` — нули, и транспозиция координат курсора вместе с перестановкой метрик глифа невидима. Комментарий `application_ut.cpp:764` фиксирует это прямым текстом: «the cursor is the home cell». Сама `cellOrigin(3, 1, …)` в `grid_geometry_ut.cpp:160` проверена — незакрыт именно **вызов**.

**Дыра №2 (C6/C6b).** Начальный размер окна, который просит `showWindow()`, не наблюдает никто. `application_ut.cpp:1433` проверяет результат `requestResize()` из `fontChanged()` (строка 361, её ловит C4), а не то, что запрошено при показе.

**Владение решено командиром:** `lib/shitty/application_ut.cpp` в плане волны 6 не закреплён ни за кем (`grep` по плану пуст) — отдан `T5.2` с двумя условиями: взять точку, где C1 и C2 дают **разные** неверные ответы (иначе одна мутация замаскирует другую), и доказать, что на домашней клетке новый тест зеленел бы при обеих — это объясняет молчание старых трёх и не даёт вернуть их фикстуру.

### Ошибка командира, найденная в тот же час: `vterm_boundary` блокирует CI

Когда `M6b` оставил гвард красным (6 строк в 2 файлах), командир принял это со словами «красный здесь аудит, а не сборка, дерево собирается». Дерево действительно собирается.

**Но гвард входит в `add_test(...)` (`build.py:4360`), и в CI `./build test` останавливается на нём.** Проверено на прогоне `33862433804`: `Tests Alpine` и `Tests Fedora` падают на `FAIL $(B)/vterm-boundary.stamp` и **не исполняют ни одного теста**.

Значит **две джобы слепы с `eb016aec`** — четыре мержа подряд. Всё это время в борде писалось «Alpine красный из-за унаследованного `test_wide_ligature_overflow`», которого там уже не видно, потому что до него не доходит.

**`T5.11` предупреждал об этом прямым текстом**, и предупреждение записано в борд как оговорка к его задаче — вывода про CI командир не сделал. Причина ошибки: гвард проверялся **локально одной командой**, где он и правда аудит, и ни разу — внутри `./build test`.

**Задача `G15` поставлена и не выполнена:** завести в `check_includes.py` механизм разрешений той же формы, что у четырёх остальных гвардов (у него его нет — 69 строк, всё или ничего). Требования: поимённо по файлу и включению; седьмое нарушение **в том же файле** обязано краснеть; самозащита от устаревшего ключа. Закрывать сами шесть строк не надо — установлено четырьмя исполнителями независимо, что три структурны (`A1`/`A8`), а одну §2.7 прямо запрещает трогать.

### Что ждёт возобновления

| Задача | Ветка | Что делать |
|---|---|---|
| `G15` | `fix/G15-boundary-allowance` | разрешения для `vterm_boundary`; **срочно — CI слеп на двух платформах** |
| `T5.2` | `feat/T5.2-grid-geometry` | два сторожа в `application_ut.cpp` по мутациям C1/C2 и C6/C6b |

Обе ветки от `master`, деревья чистые. Брифы обеих задач полностью восстановимы из этого раздела.

## Волна 6 закрыта целиком: 11 задач, 4 проверяющих, `F6`. `unit_tests` 950 → 966

Возобновление после лимита прошло без потерь: `T5.2` и `G15` перезапущены с сохранёнными измерениями, `G16` снял барьер `TMPDIR`.

### Вердикты проверочной волны

| Проверяющий | Вердикт |
|---|---|
| `R6-arch` | `PASS_WITH_FINDINGS` — 11 инвариантов из 11 соблюдены и доказанно охраняются (18 мутаций + 5 проб гвардов) |
| `R6-test` | `PASS_WITH_FINDINGS` — все семь сторожей волны перепроверены девятью мутациями с нуля, расхождений с отчётами ноль |
| `R6-sec` | блокирующих нет; одна важная (`V1`), пять замечаний |
| `R6-qa` | готово с замечаниями — четыре мутации и все четыре правки плана воспроизведены независимо |

Красных нет. **Волна закрыта, находки перенесены в `F6` и закрыты там же.**

### Что нашла проверочная волна сверх подтверждения

**Источник правды один, но дорог от него три, а охранялась одна** (`R6-arch`). Два других конца, оба `0 из 963`: `SessionSetImpl::toContentBox()` — обратное преобразование хит-теста, единственный тест с `border = 0`; `ApplicationImpl::buildFrameUpdates()` — **полное изъятие хрома переживало весь набор**, потому что `application_ut.cpp` ни разу не ставил ненулевую границу.

**Пятая и шестая вырожденные фикстуры**, устройство то же: параметр, обнуляющий проверяемое, стоит по умолчанию и им пользуются все.

**Четвёртая — в стороже, который эта же волна завела** (`R6-test`). `T5.3` поставила сторожа alt-половины на `saveLines = 0`, где **оба слагаемых суммы равны**: подстановка «считать основной экран дважды» проходила зелёной. Автор **измерял канал перед тем, как на него опереться**, и всё равно попался: проверил, что величина реагирует, но не что её слагаемые различимы между собой.

**`V1` оказалась вдвое больше** (`F6`): кроме ветки нулевого trie не охранялась и мутация «`uriSchemeAllowed()` отказывает всему» — всё покрытие политики схем питоновское, через уже разобранную конфигурацию.

**Незаказанная находка `R6-sec`:** волна **молча починила use-after-free** на пути `-dump` — `OutBuf`/`FDRegular` жили в пуле окна, а `ScopedFD` в арене сессии; закрытие вкладки освобождало дескриптор из-под буфера. Ни один отчёт волны этого не называл. Показано чтением двух ревизий, не воспроизведением.

**Поверхность ядра сократилась вчетверо:** `composer.opts` 68 → 0, `composer.window` 23 → 1. Конечное состояние проверено командиром.

### Опровергнутые утверждения командира

| Что я записал | Как на самом деле |
|---|---|
| три строки `vt_headless.cpp` структурны | **не структурны** — следствие того, где лежит файл; перенос в `lib/shitty` закрывает все три ценой расхождения в двух бинарниках (`R6-arch`) |
| дефект указателя на `session.cpp:732` | `:732` верна и охраняется; дефект на **`:1586`** — ошибка ушла в бриф (`R6-qa`, проверено командиром) |
| «список красных не изменился» доказывает непойманность | **не доказывает**: `C8b` поймал бы `test_bitmap_font_render.py:59`, но он уже красный по своей причине (`R6-test`) |
| критерий «`Ran 6400`, 20 красных» | числа режима `./build`; одним процессом — **22**. Режимы несравнимы, `R6-test` предупредила, до брифа не дошло (`F6`) |
| «ноль сломанных целей» достижимо | `main_fuzz` не линковался **никогда** |

### Новое в `CLAUDE.md` за волну — семь разделов

Цель `test` принципиально не зелёная (24 узла всегда); временные корни выбираются, а не наследуются от `TMPDIR`; «список красных не изменился» — не доказательство; регрессия может дать `SIGSEGV` без итоговой строки; числа зависят от режима прогона; `vterm_boundary` имеет разрешение и оно теснее прочих; `./build <цель> --clear` снимает симлинки остальных целей.

### Открытое, перенесённое в волну 7

- **`vterm.cpp:6586`** (`composer.resize` рядом с `host->requestResize`) — **строка без владельца**: решение отдало её `M6d`, тот опроверг это в своём же отчёте, дальше передавали `T5.1` и `T5.5`, не досталась никому.
- **`SessionSetImpl::pointerPresence()` (`session.cpp:1586`)** адресует `activeTerminal()`, соседи — `pointerTarget(x, y)`. Наблюдаемое следствие, найденное `R6-test`: **подсветка OSC 8 на несфокусированной панели остаётся гореть** после ухода указателя из окна.
- **Два ключа разрешения границы не имеют выхода вообще** — сказано в самих ключах после `F6`.
- **`A11` охраняется двумя тестами без запаса** — каждая мутация даёт ровно один поймавший; `R6-arch` предложил третий тест на тождество.
- **`A3` и `A7` — «документ устарел»**: `A7` неожиданно, `C11` сделал резерв нулевым навсегда. `T0.3` ошибается в числе тестов `pane_layout_ut` — 27, а не 28.

## Решение человека 2026-09-04: вариант «А» по `VtHost`

Документ — `docs/plans/reviews/upstream-merge-vthost-decision.md` (829 строк), смержен в `master`.

**Принято: полный вариант «А».** Убрать `Composer&` из `Vterm::create`, заменив пять его употреблений в ядре тремя методами `VtHost` — `contentInsets()`, `surfaceResized()`, `cellCapacityExcept()`; арифметику сетки перенести в `lib/vterm/vt_geometry.h` над уже существующим `VtInsets`; **плюс перенести `vt_headless.cpp` в `lib/shitty`**.

### Почему это возникло

Апстримный `lib/embed` (пришёл шагом `M7`) у нас **не компилируется**: `Vterm::create` берёт `Composer&`, а `libshitty_vt_core` — это буквально `glob("lib/vterm/*.cpp")` плюс фасад, с зависимостями только `libstd` и headless-`libplt`. `lib/shitty` там нет по построению.

**Разрыв — два символа, а не фонт-стек.** Первая оценка `M7` (сорок неопределённых) была верна только для варианта «фасад сам строит `Composer`», которого делать не надо. Померено: `nm -u` по `vterm.o` с флагами embed-цели даёт `Composer::contentInsets()` и `Composer::resize()`, и всё; `cellCapacityExcept` символа не порождает — вызов виртуальный (`session.h:114`, `virtual … = 0`, проверено командиром).

### Прецедент, сделавший решение очевидным

**За 89 коммитов апстрим тронул `vt_host.h` один раз** — `9bf5e497` — и сделал там ровно то же: перенёс политику схем URI в `VtHost` («detection stays in the core, but whether a detected scheme is actionable is the embedder's call»). **Наш `vt_host.h` расходится с апстримным на ноль строк** — проверено командиром.

То есть предлагаемое — не изобретение форка, а тот же ход, который апстрим уже сделал для соседнего случая.

**И это первое изменение за весь мерж, которое расхождение уменьшает:** `Vterm::create` станет десятипараметрическим против апстримных девяти. Все предыдущие решения его углубляли.

### Что закрывается попутно

- **Четыре из шести** нарушений `vterm_boundary` (`vterm.cpp` → `composer.h`, `grid_geometry.h`; `mouse_frontend.h` → `composer.h` — тот же `Insets`).
- **Строка без владельца** `vterm.cpp:6598`: `composer.resize()` стоит **в одной лямбде подряд** с `window->requestResize()` на тех же величинах с клампом — проверено командиром. Скорее всего дубль; тогда закрывается удалением, а не переездом.
- **`./build so`** — но только если `vt_headless.cpp` уедет в `lib/shitty`: он подходит под тот же glob, а `link_shared.py:26-32` линкует с `--whole-archive --no-undefined`. Архитектор называет перенос **условием работоспособности**, а не опрятностью; в `check_includes.py` и в находке №6 `R6-arch` он записан как «размен ради опрятности» — формулировку надо поправить.

### Поправки к постановке командира

| Что я написал | Как есть |
|---|---|
| `Vterm::create` на `vterm.h:329` | **`:303`** — я дал доволновой номер |
| «сорок символов тянет фонт-стек» | верно только для варианта, который делать не надо; настоящий разрыв — два символа |
| `bin/core_perf` и `bin/main_fuzz` сломаются | **не сломаются** — они зовут `VtermHeadless::create`, чья сигнатура не меняется; `grep -rn 'Vterm::create' bin/` пуст |
| `vt_headless.cpp` строит `Composer` | **принимает** (`:218`) |

### Седьмая вырожденная фикстура за мерж

`AnswersTheWindowReportsAboutTheWindowAndTheTextAreaAboutThePane` (`vt_headless_ut.cpp:287`) — лучший сторож окновых отчётов — **не отличит корректный `contentInsets()` от возвращающего нули**: `Options::border` по умолчанию `0` (`options.h:74`, проверено командиром), резерв хрома в фикстуре не ставится. Вынесено в приёмочные критерии задачи с тремя обязательными мутациями.

### Цена и оговорка

13 файлов плюс один новый тест, порядка `+200/−125`, **22 места правки**: 5 в ядре, 15 вызовов `Vterm::create`, 2 арифметики в `vt_headless.cpp`.

**Оговорка архитектора, принята:** дерево он не собирал и тестов не гонял; вывод про красный `./build so` — рассуждение из двух аргументов линкера, а не наблюдение, и его надо проверить одной командой на Linux до утверждения.
