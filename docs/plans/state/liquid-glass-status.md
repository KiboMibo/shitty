# Статус: Liquid Glass в терминале

- **План:** `docs/plans/2026-09-05-liquid-glass.md`
- **Разведка:** `docs/research/2026-09-05-liquid-glass-reconnaissance.md` + снимки `docs/research/glass-2026-09-05/`
- **Архитектура:** `docs/architecture/2026-08-18-panes-and-window-chrome.md` — инварианты `A1`–`A11`
- **Репозиторий:** форк `KiboMibo/shitty` (remote `fork`), рабочая ветка `master`
- **Начато:** 2026-09-05, сразу после закрытия мержа апстрима (`behind` 0)
- **Обновлено:** 2026-09-05, релиз **14** опубликован; после осмотра пользователем — две правки по его замечаниям (`T5`, `T6`) влиты

## Решения пользователя

| Вопрос | Решение |
|---|---|
| Объём | Стекло **включая поверхность под текстом**, не только хром |
| Порядок | После мержа апстрима — выполнено, мерж закрыт 2026-09-05 |
| Опция | `-backgroundBlur` со значением `off` / `blur` / `glass` |
| Стиль | `Regular` |
| Охват | Подложка окна **плюс** своё стекло у сайдбара и вкладок, слитое контейнером |

## Решения командира

| Что решено | На основании |
|---|---|
| В TOML `backgroundBlur = true`/`false` остаются рабочими как псевдонимы `blur`/`off` | Иначе ломаются существующие конфиги. Цена нулевая — правка в разборе значения |
| В командной строке голое `-backgroundBlur` и `+backgroundBlur` ломаются | `SepArg` безусловно требует значение и запрещает `+` (`options.cpp:1112-1116`). Отказ **громкий**: разбор отвергает неизвестное значение, тихой подмены поведения нет |
| Сторож чётности конфигов (`T2`) включён в волну 1 | `T1` — первая правка обоих конфигов с тех пор, как `M8c` измерила, что у их расхождения **нет ни одного наблюдателя**. Десять строк против мины, дважды дожившей до продакшена |
| Волна 3 отделена от волны 2 | Сайдбар и вкладки живут в файлах с тремя цветовыми строками, не защищёнными ничем. Одной задачей было бы неясно, что сломалось |

## Волны

| Волна | Задачи | Статус |
|---|---|---|
| 1 | `T1` (опция), `T2` (сторож конфигов) | **закрыта и влита в `master`** (`03b88012`) |
| 1-ревью | `F1`, `R1-test`, `R1-qa`, `F1b` | **закрыта**, оба вердикта жёлтые, находки устранены |
| 2 | `T3` (`PltGlassView`, подложка окна) | **закрыта и влита** (`4f508582`) |
| 2-ревью | `R2-test`, `R2-arch`, `R2-qa`, `F2` | не начата |
| 3 | `T4` (стекло сайдбара и вкладок) | **закрыта и влита** (`abef9f77`) |
| 3-ревью | `R3-test`, `R3-qa`, `F3` | не начата |

## Задачи

| ID | Что | Статус | Ветка | Отчёт | Вердикт |
|---|---|---|---|---|---|
| `T1` | Опция `backgroundBlur` со значением, девять мест | **готово** | `feat/T1-backdrop-mode` | `docs/reports/T1-backdrop-mode-2026-09-05.md` | принято командиром |
| `T2` | Сторож чётности `shitty.toml` и `pretty.toml` | **готово** | `feat/T2-config-parity` | `docs/reports/T2-config-parity-2026-09-05.md` | принято, мутация проверена командиром |
| `F1` | Сведение волны: четыре теста, `-vulkanBlit`, снятие разрешения | **готово** | `wave/liquid-glass-w1` | `docs/reports/F1-wave1-reconcile-2026-09-05.md` | принято, прогон сверен командиром |

Статусы: `ждёт` · `идёт` · `готово` · `блокирована` · `отменена`

## Что известно до начала, и чем это оплачено

**Вариант A подтверждён пиксельно, и командир смотрел снимки сам.** `docs/research/glass-2026-09-05/glassBelowSibling-1-initial.png`: волосяные линии заднего плана внутри окна исчезают, цвет проходит насквозь, белые полосы «текста» лежат поверх и не приглушены. Отказной случай `glassOpaqueMetal-1-initial.png`: при `CAMetalLayer.opaque = YES` окно сплошь чёрное.

**Гарантированная SDK раскладка — негодная.** Отдать `PltView` в `glass.contentView` кладёт стекло поверх текста и навязывает окну размер через Auto Layout. Нужный путь документацией не покрыт — ровно поэтому разведка шла до кода.

**Времени кадра в числах нет, и это сказано честно.** Ни fps, ни `%cpu` WindowServer не различают раскладки: первый упирается в 120 Гц у всех включая эталон, второй не шелохнулся при росте площади окна в 5,7 раза. Есть только верхняя граница риска — ни одна стеклянная раскладка не опустилась ниже частоты дисплея.

**Поправка досъёмки к первой редакции разведки.** Довод против варианта C «измеренно неверный порядок слоёв» **вычеркнут**: пиксельно `glassContent` неотличим от `glassBelowSibling`. Отказ от C остаётся по другой причине. Урок шире: **дамп дерева слоёв не доказывает видимый порядок там, где есть портальные слои** — четыре раскладки из пяти он описал верно, одну нет, и различить их по дереву нельзя.

## Происшествие, о котором пользователь извещён

Пробник восемь раз снял **настоящий экран пользователя** (открытую страницу в браузере) вместо своего окна: шаг `3-unfocused` делает `[NSApp hide:nil]`, и вместе с окном исчезла полноэкранная подложка пробника. Агент опознал снимки, удалил и в коммит не пустил, затем завёл постоянную проверку, отбраковывающую такие кадры.

Следствие для плана: поведение стекла у неактивного окна **осталось неизмеренным** и вынесено в `R2-qa` на осмотр глазами.

## Поправка плана 2026-09-05: путь опции длиннее, чем я написал

`T1` остановился по правилу «тронуть что-то ещё» и назвал три недостающих файла. Все три подтверждены; план поправлен, раздел «Поправки по ходу».

| Находка | Решение командира |
|---|---|
| `platform_cocoa.mm` читает **не** `Options`, а `WindowOptions::backgroundBlur` (`ext/plt/window.h:170`); мост — `application.cpp:1171` | Правка идёт **в `application.cpp`**, слой платформы не трогается вовсе. Вариант исполнителя лучше моего: граница `lib/shitty` → `ext/plt` сохраняется. Я писал бриф по потребителю, не проследив мост |
| `options_ut.cpp:480,512` не собирается с `enum class`; один тест строит argv с запрещённой формой `+backgroundBlur` | Отдано `T1`. Несобирающееся дерево не проверить ничем. `T1` чинит компилируемость, чувствительность добавляет `R1-test` |
| `tst/harness.py:324` делает `int(value)` для **каждого** поля дампа | Дефект **контракта**, не правки: «дамп печатает имя» уронил бы каждый тест, зовущий `terminal.options()`. Контракт сохранён — имя читаемее числа, — `harness.py` расширен: нечисловое кладётся строкой, числовые поля не меняются |
| Пять утверждений в `tst/test_config.py` ломаются, а файл принадлежит `T2` | **`F1`**, и она перестаёт быть пустой. Правило «две задачи одной волны не владеют одним файлом» не обходится по причине «правка выглядит механической» |

### Модель веток уточнена

Волна 1 собирается на **`wave/liquid-glass-w1`**, а не мержится в `master` по частям: между `T1` и `F1` дерево несёт пять красных, и `master` этого видеть не должен. В `master` уходит зелёная волна целиком.

Это возврат к штатной модели плана (ветки задач → ветка волны → рабочая ветка); в мерже апстрима порции шли прямо в `master`, потому что каждая была самодостаточной.

## `T2` готова — сторож заведён, и он сразу нашёл расхождение

Ветка `feat/T2-config-parity`, коммиты `ac845994` (сторож) и `6dd02dd8` (отчёт). Тронуты **ровно два файла**: `tst/test_config.py` и свой отчёт.

Командир проверил независимо: `test_config.py` — `Ran 31, OK`; мутация «ключ `saveLines` убран из `pretty.toml`» краснит **ровно один** тест и называет ключ (`only in bin/st/shitty.toml: ['saveLines']`).

Сторож вышел шире задачи и лучше её: три теста вместо одного — **ключи**, **значения** и строки **`# CLI:`**, потому что расходиться они могут по разным причинам и должны называться раздельно. Отсюда `+3` теста, а не `+1`.

**Значения сравниваются по правилу, а не по списку.** Значение из `shitty.toml` прогоняется через `Shitty→Pretty` / `shitty→pretty` / `SHITTY→PRETTY` и обязано совпасть. Списку исключений понадобилось бы ручное дополнение при следующей опции — то есть ровно тот шаг, который забывают. Ложная зелень при таком правиле невыразима.

Премиссы по приёму `F7` проверены отдельно: опустошённый `pretty.toml`, ослеплённый разбор и пустой `BRAND_WORDS` краснеют **на премиссе**, а не проходят на пустом месте.

### Расхождение, уже жившее в дереве

`bin/st/shitty.toml:84` несёт `# CLI: -vulkanBlit …`, `bin/pt/pretty.toml` — нет. Опция не брендозависима: `options.cpp:96` держит её в общей таблице, `pt -vulkanBlit` принимается. Пришла коммитом `92657e71` и в `pretty.toml` не приходила никогда. **Подтверждено командиром.**

`T2` не стала чинить — `pretty.toml` принадлежит идущей `T1` — и записала имя в `DOCUMENTATION_ALLOWANCE` по образцу `lib/vterm/check_includes.py`: ключ «файл + имя», плюс проверка на осиротевший ключ.

**Решение командира: чинит `F1`, и обе половины идут вместе.** Добавить строку в `pretty.toml` и убрать ключ из разрешения — одна правка: строка без снятия ключа краснит сторожа осиротевшим ключом, что и задумано. Отдавать это `T1` нельзя — разрешение живёт в файле `T2`.

### Уточнение в `CLAUDE.md`

Контроль `M8c` «удаление опции из `shitty.toml` краснит `test_config`» верен для удаления опции **целиком**. Удаление одного присваивания оставляет строку `# CLI:`, которую сверяет с `-help` другой тест, и он остаётся зелёным. Два случая надо различать; внесено.

## Волна 1 сведена и зелёная

Ветка `wave/liquid-glass-w1`, вершина `f2062151`. Собрана из `feat/T1-backdrop-mode` и `feat/T2-config-parity` одним мержем `d0e2218e`, затем `F1` (`a46fd7ed`).

**Проверено командиром независимо, полным прогоном:**

| | Эталон `master` | Ветка волны |
|---|---|---|
| `unit_tests` | `OK: 982` | **`OK: 982`**, `EXIT=0` |
| питоновский набор | `Ran 6604`, `errors=14` | **`Ran 6607`** (+3 теста сторожа), `errors=14` |
| красные **поимённо** | — | **`diff` пуст** |
| `failures` / `skipped` | 0 / 23 | **0 / 23** |
| `DOCUMENTATION_ALLOWANCE` | — | **пусто** |
| строк `# CLI:` в конфигах | 77 / 76 | **77 / 77** |

### `T1`: предсказала свои красные до правки

Десять критериев из одиннадцати сошлись; одиннадцатый дал **ровно предсказанную четвёрку**, все в `tst/test_config.py`, все разряда «ожидание переписано». Потеряно 0, добавлено 0 за пределами этого файла.

**Поправила командира:** красных четыре, а не пять. Пять — число *строк*; лежат они в четырёх тестах, а пятая (`:228`) переписки не потребовала. Командир это предполагал, исполнитель **проверил прогоном**.

**Наши сообщения разошлись в пути:** она приняла все четыре решения сама, до ответа командира, и приняла ровно те же — включая замену `platform_cocoa.mm` на `application.cpp`. Значит решение было выводимо из кода, а не вкусовым.

**Оговорка брифа разрешилась в нужную сторону, и это измерено.** Командир велел остановиться, если не все поля дампа окажутся числовыми. Выяснилось обратное: до правки числовыми были все, `background_blur` стал первым нечисловым, расширение `harness.py` строго аддитивно.

### Три места без наблюдателя, названные исполнителем самостоятельно

Переданы в `R1-test` как её основная работа. Все три сегодня переживут любую мутацию:

1. **Подсказка формы** `; -NAME takes a value, as in -NAME off` — `tst/test_options.py:32-33` смотрит только на `missing value` и `'+' is invalid here`; обе уцелеют, если хвост выбросить.
2. **Смысловая часть предупреждения** — переписано с «has nothing **to blur**» на «has nothing **to show**», потому что «размывать» неверно для стекла. Проверяются только два имени опций в сообщении; возврат к «to blur» не покраснит ничего.
3. **Хвост `(default: off)` в справке** — появился сам, из-за смены вида опции: `printUsage()` печатает умолчание только для не-`NoArg`. Текста справки набор не проверяет вовсе.

### `F1`: три разные поломки различаются по имени

Четыре мутации показали, что сторож не просто краснеет, а называет причину: убранный ключ → тест **чётности** (`only in bin/st/shitty.toml: ['login']`); осиротевшее разрешение → тест **документации** именно как осиротевший ключ; убранная строка → тот же тест с текстом `undocumented there`.

**Решение исполнителя, которое стоит сохранить.** В `test_the_command_line_beats_a_configured_opacity` он оставил в конфиге **старое** написание `backgroundBlur = true`, поменяв только командную строку. Псевдоним уцелел, и пара теперь заодно закрепляет, что конфиг, написанный до волны 1, стартует. Причина вписана комментарием **в сам тест** — без неё следующий читатель принял бы старое написание за недосмотр, «починил» и молча потерял покрытие псевдонима.

## Ревью волны 1 запущено

| Задача | Что делает | Отчёт |
|---|---|---|
| `R1-test` | Закрывает три места без наблюдателя, каждое мутацией; проверяет сторожа `T2` как посторонний | `docs/plans/reviews/liquid-glass-R1-test.md` |
| `R1-qa` | То, что тестом не закрыть: стартует ли живой конфиг со старым написанием; внятны ли три новых текста ошибок человеку; оба бренда | `docs/plans/reviews/liquid-glass-R1-qa.md` |

`R2-sec` не запускается: волна 1 не добавила ни внешнего входа, ни работы с данными или секретами — она меняет разбор собственной опции. Основание записано здесь по правилу соразмерности.

## Волна 1 закрыта и влита в `master`

`master` = `03b88012`. Ветка волны `wave/liquid-glass-w1` собрала `T1`, `T2`, `F1`, оба ревью и второй круг `F1b`.

**Проверено командиром на каждом шаге, последний прогон:** `unit_tests` `OK: 985`, `EXIT=0`; набор `Ran 6609`, `errors=14` **поимённо тем же списком**, `failures=0`, `skipped=23`.

### Вердикты ревью

| Задача | Вердикт | Числа |
|---|---|---|
| `R1-test` | `PASS_WITH_FINDINGS` | 5 наблюдателей, **14 мутаций, 13 пойманы поимённо, 1 нет** |
| `R1-qa` | готово с замечаниями | незакрытых критериев **0** из 21 |

`R1-sec` не запускалась: волна не добавила ни внешнего входа, ни работы с данными или секретами. Основание записано по правилу соразмерности.

### Что дало ревью сверх галочек

**`R1-qa` подкрепила совместимость доводом, а не только пробами:** `getBackdropMode()` принимает **строгое надмножество** того, что принимал `getBool()`, поэтому «стартовал вчера, не стартует сегодня» невыразимо. Шесть живых конфигов на два бренда — подтверждение, а не доказательство.

**Она же добавила отрицательный контроль к бренд-тесту:** `pretty_binary_branding.py` зелен на `pt` **и краснеет на `st`**. Без второй половины зелёный ответ не доказывает ничего.

**`R1-test` доказала отсутствие наблюдателя тем, что мутация оставила весь набор зелёным** — сильнейшая форма такого доказательства.

### Второй круг: подсказка называла единственное бесполезное значение

Голое `-backgroundBlur` **вчера включало** размытие, а сегодня человеку советовали `-backgroundBlur off` — значение, которое выключает. Ни `blur`, ни `glass` не названы, на `-help` не отправлен.

`F1b` завела седьмое поле `valueNames` в описании опции, заполненное **у одной**; при `nullptr` механизм прежний — измерено на пяти опциях. Проверено командиром: `-bg`, `-saveLines`, `-fontsize`, `-backgroundOpacity` сохранили форму `as in -bg #000`, а три отбивки `backgroundBlur` теперь называют один набор в одном порядке.

Довод против «другого примера» стоит запомнить: `blur` перекосил бы отбивку на форму `+`, где `off` как раз **правилен** — `+backgroundBlur` значило «выключить».

**Наблюдатель починен, а не обойдён**, и мутация `M-C` доказала, зачем: переписанный на равнозначный текст набор оставляет тест на *неизвестное* значение зелёным — непротиворечие двух сообщений держит только новый наблюдатель.

**Недостижимая ветка заменена, а не удалена:** на её месте `(void)(get(...))`, чтобы будущая смена умолчания на `nullptr` дала громкий отказ, а не тихий `Off`.

### Ошибка командира, исправленная до пуша

Из-за `||` в команде мерж ревью лёг **в `master`**, а не в ветку волны — волна попала в рабочую ветку раньше второго круга, вопреки объявленной за час до того модели. Не запушено, `reset --hard` к предыдущему состоянию, мерж переложен в ветку волны. Ветки ревью и волны целы, ничего не потеряно.

## В отложенное: дыра, закрывать которую дороже, чем оставить

**Ключ, удалённый из обоих примерных конфигов сразу, наблюдателя не имеет.** Сторож `T2` сравнивает файлы друг с другом, поэтому симметричное удаление ему невидимо. Измерено `R1-test` мутацией `M14`.

**Командир проверил независимо, можно ли вывести правило из кода** — собрал ожидаемый состав конфига из **обеих** таблиц `options.cpp` (опции не-`cliOnly` плюс видимые ресурсы). Остаётся **десять** законных отсутствий: `shell`, `remap`, `uriScheme`, `colorScheme`, `dump`, `paneDividerColor`, `sidebarColor`, `soft`, `quickCompanion`, `quickFullscreenHotkey`.

Значит правило потребовало бы ручного списка — ровно того, против чего `T2` привела довод и что в этом репозитории забывают. Асимметричный случай, который бил дважды, закрыт; симметричный — сознательное действие, а не описка. **Оставлено.**

Ещё три мелких находки `R1-test`: приём `true`/`false` работает и с командной строки (шире, чем говорит раздел 4B плана — текст плана поправить); обрыв `unit_tests` с `EXIT=255` вместо красного при регрессии псевдонима.

## Волна 2 дана в работу — стекло появляется в продукте

`T3`, ветка `feat/T3-glass-backdrop` от `03b88012`. `PltGlassView` встаёт туда, где сегодня `PltBackdropView`.

В бриф вынесено сверх плана:

- **довесок `T1`** — слой платформы читает `WindowOptions`, а не `Options`; расширение `ext/plt/window.h` придётся решать, и решение про границу между слоями;
- **обе ловушки разведки** — снимок экрана может оказаться экраном пользователя (готовая проверка `ringcheck.py` лежит рядом с пробником), и дамп дерева слоёв не доказывает видимый порядок;
- **три точки остановки** — расхождение с разведкой пиксельно, необходимость двигать `window.contentView`, отсутствие `NSGlassEffectView` в SDK.

## Волна 2: стекло в продукте

`master` = `4f508582`. Ветка `feat/T3-glass-backdrop`, коммиты `6b472eea` (код) и `11304b8a` (отчёт и снимки).

**163 строки продуктового кода:** `platform_cocoa.mm` `+136` (класс `PltGlassView: NSGlassEffectView`, выбор подложки по режиму, `@available(macOS 26.0, *)` с откатом), `window.h` `+29`, `application.cpp` `+22`.

**Командир видел снимок сам:** `docs/research/glass-t3-2026-09-05/glass-under-text.png` — текст чёткий и белый, рабочий стол за окном проходит размытыми цветными полосами. Стекло **под** текстом, как заказано.

| Критерий | Свидетельство |
|---|---|
| `blur` неотличим от сегодняшнего | против сборки `master` **max разница 1 из 255**, **0.000%** пикселей; контроль `off` даёт 0.090% — прибор не слеп |
| стекло отличимо от размытия | при opacity 0 `glass` отличается от `blur` на **98.02%** пикселей |
| `contentView` = `PltView` | во всех четырёх прогонах |
| пять гвардов | зелёные прямым запуском **и каждый покраснел своей пробой** |
| `unit_tests` | `OK: 985` — эталон в точности |
| набор | `Ran 6609`, 14 красных поимённо |

**Решение про границу слоёв.** Платформенный двойник `plt::Backdrop {None, Blur, Glass}`, мост `platformBackdrop()` в `application.cpp`. Не общий тип — потому что два перечисления **уже расходятся**: знает ли система про стекло, известно только бэкенду, значит откат `Glass`→`Blur` обязан жить внутри `platform_cocoa.mm`. Общий тип обещал бы, что прошедший через мост режим исполнен буквально.

**Оговорка, которую нельзя закрыть кодом.** Свойство «подложка держится активной независимо от фокуса» для стекла **сохранить нечем**: у `NSGlassEffectView` весь API это `contentView`, `cornerRadius`, `tintColor`, `style`. У матовой ветки для этого стоит `NSVisualEffectStateActive`. Что стекло делает при уходе фокуса — не измерено ни разведкой, ни `T3`. Пункт ручного осмотра.

## Волна 3: хром получил своё стекло, и слияние отвергнуто измерением

`master` = `abef9f77`. Ветка `feat/T4-chrome-glass`, коммиты `80b42395` и `3b885744`.

**`NSGlassEffectContainerView` работает — и именно поэтому не применён.** Он действительно сливает стекло хрома с подложкой окна, но подложка накрывает окно **целиком**, объединение фигур даёт её же, и стекло хрома исчезает без остатка: профиль яркости с контейнером **совпал с профилем без стекла хрома вовсе**.

Единственное непроверенное API плана отработало как задумано, а задуманное оказалось не тем, что нужно. Хром получил собственные стеклянные вью **над** метал-слоем, стиля `Clear`. Шов принят, показан снимком и обоснован числами — это развилка, предусмотренная планом и разрешённая измерением.

| Критерий | Итог |
|---|---|
| `blur`/`off` как сегодня | попиксельно **`max=0`** на обоих; контроль: `glass` даёт `max=106` |
| автоскрытие по ховеру | 6 переходов, сетка `157x49` на каждом, **ни одной строки пересчёта** — `A7` цел |
| `cmd+b` | три перехода, иерархия пуста в закрытом состоянии |
| брендирование | `pt` `EXIT=0`, `st` `EXIT=1` — проверено командиром обеими сторонами; **новых классов ObjC не заведено вовсе** |
| `unit_tests` / набор | `OK: 985` / `Ran 6609`, 14 красных поимённо |

**Две из трёх незащищённых цветовых строк тронуты** — фон сайдбара (был тинт-слой 6% от цвета текста, стал собственный лист стекла `Clear`) и тинт титульной полосы. Снимки «до/после» в отчёте. Это видно только глазами, тестом не ловится; **решение пользователя**, откат — одна строка.

`ext/plt` не тронут: хром узнаёт про стекло из живой иерархии вью, а не из опции.

## Третий инцидент со снимками — и вторая дыра в правиле командира

Первый снимок `T4` содержал размытые виджеты Центра уведомлений пользователя. Они лежали **выше подложки и ниже окна** — там, где приём `T3` со списком перекрывающих окон их не видит: он ловит то, что **впереди**, а стекло самплит то, что **позади**. Снимок удалён, в репозиторий не попал.

Правило в `CLAUDE.md` переписано **в третий раз за сессию** и наконец в общей форме:

> Снимок программы, которая показывает сквозь себя, содержит чужое по умолчанию. Доказывать надо не «моё окно сверху», а **«между моей подложкой и моим окном нет ничего чужого»**.

Полная форма: подложка на уровень выше рабочего стола, и к каждому снимку — весь экранный стек.

## Что осталось

| Что | Кому |
|---|---|
| `R2-*` и `R3-*` — ревью волн 2 и 3 | агентам, не запускались |
| Осмотр стекла глазами, включая **неактивное окно** | человеку |
| Решение по двум цветовым строкам | человеку |
| `T7.2` — ручная приёмка мержа апстрима | человеку |

## Релиз 14

`https://github.com/KiboMibo/shitty/releases/tag/14`, семь артефактов. Все семь джобов зелёные — включая сборку Linux с нуля и тесты релизного артефакта на Fedora, Alpine и Ubuntu, которых локальные прогоны не давали никогда.

**Поправка командира к собственным словам:** до запуска назван тег **10**; вышел **14**. `git tag | tail -5` сортировал лексически, и `10`…`13` встали перед `5`.

## Пользователь посмотрел — две правки по замечаниям

### `T5`: «сделать одинаковым сайдбар и экран, 1px линия, активный таб — парящее стекло»

`master` `ce96337e`. Один файл, `ui_sidebar_tabs.mm`.

Первые две части оказались **снятием, а не добавлением**: лист стекла на весь сайдбар (`applyGlass`, добавлен `T4`) убран, и подложка окна прошла насквозь одинаково; линия в 1px **уже была** (`:1090`, видима намеренно по прежней жалобе «где начинается терминал»).

| | Было | Стало |
|---|---|---|
| сайдбар ↔ терминал | 21.30 | **0.95** при шуме полосы 1.13 |
| ступень на шве | 21.42 | **0.11** |
| перепад на линии | — | **47.93** против 0.43 у прочего |

Пилюля активной вкладки стала `NSGlassEffectView`, стиль **`Clear` по числам**: `Regular` — на 12.6 темнее поверхности, `Clear` — на 66.7 светлее, кромка 103 против 18. `NSGlassEffectContainerView` проверен тремя формами и **снова гасит пилюлю** (0.00 / 0.05 / 0.76); вывод `T4` устоял, но основание теперь измерение, а не одна проба. Заголовок SDK при этом противоречит формулировке `T4`: контейнер сливает потомков `contentView`, `addSubview:` в их число не входит.

Три самообмана, пойманных исполнителем: `git checkout <файл>` стёр правку посреди работы — восстановлено, точность доказана побайтовым совпадением пересобранного бинарника; первая редакция измерения прочитала 29.94 единицы разницы там, где пилюли нет — полосатая подложка; `cmd+1..9` через `osascript` **до терминала не доходят и молчат** — восемь снимков «смены вкладки» вышли одинаковыми, поймано попиксельно. Последние два — в `CLAUDE.md`.

### `T6`: «стекло таба темнее — текст не читается; разделитель темнее»

`master` `6c79a9d0`. Один файл, `+68/−2`.

Довод `T5` про «язык списка» (активная строка всегда светлее) пользователь не принял — читаемость важнее. Критерий поставлен как контраст текста по WCAG, цель ≥ 4.5:1.

| | тёмный стол | светлый стол |
|---|---|---|
| контраст текста на пилюле | 3.48 → **6.38** | 2.66 → **5.72** |
| яркость линии при поверхности | 0.157 при 0.038 → **0.005** | 0.157 при 0.064 → **0.008** |

Способ для пилюли — `tintColor` цветом `bg` темы с альфой **0.65** поверх `Clear`; выбор из четырёх по числам: `Regular` 11.17:1, но яма −15.6 и кромка 18 против 103; чёрный тинт 7.00:1, но не переносится на светлую тему; `0.80` на светлом столе кладёт пилюлю на 5.7 ниже поверхности.

Разделитель меняется **только под стеклом**: там поверхность — чужой рабочий стол, и `shade(0.30)` на столе яркости 0.157 пропал бы целиком. В `blur`/`off` не тронут ни пикселем — `max=0` на всём кадре.

**Честная цена, вынесена пользователю:** на тёмном столе тёмная линия слабее светлой — **1.58:1 против 2.37:1**, потолок арифметический (ниже нуля яркости места нет, на поверхности 0.038 предел 1.75:1). Взамен снято слепое пятно, а на светлом столе тёмная контрастнее: 1.97 против 1.82. Пользователь просил темнее прямо — сделано это, а не оптимум прибора.

**Методическое, в `CLAUDE.md`:** одно поле подложки — половина измерения; полупрозрачный элемент мерить минимум на двух полях, разнесённых по яркости. Плюс три бесшумных отказа `yabai`: неверный `frame.x`, меняющийся ответ сервера, съеденный первый клик на свежем space.

## Что осталось

| Что | Кому |
|---|---|
| Осмотр `T6` на своих обоях: линия на тёмном столе, пилюля | человеку |
| `R2-*`, `R3-*` — ревью волн 2–4 | агентам, не запускались |
| Неактивное окно — единственное неизмеренное | человеку |
| `T7.2` — ручная приёмка мержа апстрима | человеку |

## `T7`: `sidebarTabTint` — прозрачность пилюли из конфига

`master` = `3782dfb2`. Опция `sidebarTabTint`, `0..100`, умолчание **65** — в конфиге `sidebarTabTint = 65`. Оттенок по-прежнему из `bg`.

Девять мест вместо шести — как и предсказала `T1`: `options.{h,cpp}`, `test_mode.cpp`, оба конфига, `ui_sidebar_tabs.mm`, `README.md`. Сторож чётности сработал на живом случае: краснел двумя тестами, пока ключ был в одном файле.

**Проверено командиром целиком:** дифф — константа `0.65` заменена на `opts->sidebarTabTint / 100.0` в том же выражении; `configChanged()` → `project()` → `apply()` → `applyPill()` → `tintColor` переставляется безусловно; отбивки `101`/`abc`/без значения внятные; `toml_dump` оба `ok`; `OK: 985`; набор поимённо; брендирование в обе стороны.

### Снимков нет — решение командира после пятого инцидента

Две попытки съёмки подряд захватили рабочий стол пользователя; 137 и 23 кадра удалены командиром не глядя. Критерии 1, 3, 4 закрыты без пикселей: диффом, дампом (`0`/`42`/`65`/`97`/`100` доходят до `tintColor`), живым reload `65 → 12 → 97` по `SIGUSR1`.

**Честная цена, названа исполнителем:** критерий 3 потерял в силе — вместо `max=0` довод о равенстве `65/100.0` литералу `0.65`; критерий 1 — визуальную часть на концах шкалы. Для одного числа в выражении, доказанном `T6`, этого довольно.

### Причина всех пяти инцидентов найдена

**Правило `CLAUDE.md` было неверным:** подложка на уровне `-1` в стекло **не композитится** — стек чист, кадр чужой, и проверка «что между» это пропускает. На `0` — композитится. Правило переписано. Плюс три отказа оснастки: список окклюдеров не гейтил, `pkill -f` не убивал, липкое окно ломало «пустой space».

**Без наблюдателя, в отложенное:** `ui_sidebar_tabs.mm:993` — мутации «делить на 255», «`fg` вместо `bg`», «вернуть константу» не покраснят ничего; строка дампа `sidebar_tab_tint=` тоже.

## Конфиги пользователя вычищены; `T8` и `F-panes` идут параллельно

### Что сделал командир руками — это конфиги пользователя, не код

`~/.config/shitty/shitty.toml` и `quick.toml`: 158 + 82 строки → **65 на оба**, бэкапы `*.bak-2026-09-06` рядом. Убрано равное умолчаниям (двадцать ключей), дубль `quickCornerRadius`, неверный комментарий у `quickGeometry`. **Убраны `bg`/`fg`/`cr`** — цвета Nord, перебивавшие `colorScheme = "Catppuccin Mocha"`: разбор ставит палитру схемы и накладывает явные цвета сверху, «плохо видимый тёмно-синий» шёл именно отсюда. Оба файла приняты `st` и валидны по `toml_dump`.

`quick.toml` — **не слой поверх** `shitty.toml`, а самостоятельный конфиг процесса-компаньона (`quickCompanion`); повторы шрифта и схемы в нём намеренны.

**Флага печати конфига нет** — `-help` проверен целиком.

### `T8` — пятнадцать умолчаний и флаг печати

Ветка `feat/T8-defaults-and-print`. Часть 1 закрыта: `OK: 986`, `Ran 6611`, новых красных ноль. Идёт часть 2.

Не в умолчания намеренно: `font` (личный набор Nerf-шрифтов), `quickCompanion` (путь в домашнем каталоге), `quick`/`no-decorations`/`showWraps` (про компаньон).

Промежуточные находки `T8`, вне её файлов:

- **`tabBar = sidebar` съедает `cmd+B`** — штатно (`V3`, чорд переключает сайдбар), но с новым умолчанием он не доходит до pty по умолчанию. `tst/test_keyboard.py:250` пришпилен `-tabBar top`. Это цена умолчания, не дефект.
- **`plt::QuickGeometry` в `ext/plt/window.h:97`** несёт инициализатор `100%x40%+0+0` — дубликат **прежнего** умолчания; продукт его не читает. Не тронут: граница `ext/plt`. В отложенное.

### `F-panes` — дефект, который новое умолчание выводит на путь по умолчанию

**Воспроизведён командиром на `master`:** нажатие → движение → уход указателя из окна → отпускание. Без флага и с `+panes` — `b"abcde"`, с **`-panes` — пусто**. Дефект не новый: жил в панелях всегда.

**Механизм прочитан:** `session.cpp:1587` — `pointerPresence(false)` зовёт `dropPointerGrab()` (`S2`, «жест, конец которого окно не увидит»). Но уход указателя при зажатой кнопке — не потеря окна: Cocoa и Wayland доставят отпускание туда, где было нажатие. Грэб сброшен раньше времени, отпускание приходит с `pressedPane_ == 0` → `held == nullptr` → уходит в никуда (`:1518-1520`).

**Связь с отложенным:** в борде мержа апстрима с 2026-09-05 лежит находка про ту же функцию — `activeTerminal()->pointerPresence()` на `:1589` адресует не ту панель. Один и тот же метод, две разные неточности; `F-panes` чинит первую, вторая остаётся в отложенном.

Ветка `fix/F-panes-release-after-leave`; файлы `session.cpp` и `session_ut.cpp` — не пересекаются с `T8`. Требование: юнит-тест с премиссой, мутация поимённо, и обратный тест, что `focus(false)` грэб **по-прежнему** роняет.

После обоих: `tst/test_mouse_frontend_pointer.py`, пришпиленный `T8` на `+panes`, отпускается отдельной правкой.
