# Статус: зависание кадров при сплите панелей

- **План:** `docs/plans/2026-08-29-pane-frame-stall.md`
- **Архитектура:** `docs/architecture/2026-08-18-panes-and-window-chrome.md`
- **Диагноз:** `docs/research/2026-08-29-tui-in-split-pane-diagnosis.md`
- **Репозиторий:** форк `KiboMibo/shitty` (remote `fork`), апстрим `pg83/shitty` (remote `origin`) · рабочая ветка `master`
- **Начато:** 2026-08-29
- **Обновлено:** 2026-08-29, волна 1 запущена
- **Общий статус:** волна 1 из 3, идёт реализация

## Задача

Пользователь: «при сплите панелей в новой панели нельзя запустить приложение с TUI, оно просто не запускается». Разведка показала другое: TUI запускается и работает, а окно навсегда перестаёт презентовать кадры при переходе любой панели на альтернативный экран. Дефект не специфичен для новой панели.

Второй план — `docs/plans/2026-08-29-upstream-merge.md`, исполняется после этого.

## Решения пользователя

| Вопрос | Решение |
|---|---|
| Порядок работ | Сначала фикс бага на текущем master, потом мерж апстрима |
| Вариант фикса | Вариант 2 сейчас (ответ на отказ рендерера), вариант 1 — условно, после замера |
| Тестовый режим | Чинить отдельной задачей **до** фикса: без этого регрессию нечем поймать в `tst/*.py` |
| Находка Т2 (начальный `winsize`) | Отдельной задачей в части 1 |
| Апстримный `lib/embed` | Просто втащить и собирать, своей работы поверх не делать |
| Объём мержа | Весь апстрим, 8 волн |
| Режим мержа | Автономно до волны 9 (по нумерации плана мержа — до волны 6, задача `T5.1`) |
| Структура | Два отдельных плана, а не один общий |

## Волны

| Волна | Задачи | Статус | Ветка волны | Смержено в master |
|-------|--------|--------|-------------|--------------------|
| 1 | `T1`, `T5` | собрана, идёт доделка `F1a` | `wave/pane-frame-stall-w1` | — |
| 1-ревью | `R1a-test`, `R1a-qa`, `F1a` | не начата | — | — |
| 2 | `T2` | не начата | — | — |
| 3 | `T3`, `T4` | не начата | — | — |
| 3-ревью | `R3-test`, `R3-arch`, `R3-sec`, `R3-qa`, `F3` | не начата | — | — |
| 4 (условная) | `T6` + `R4-*`, `F4` | не начата, ждёт гейта `T4` | — | — |

## Задачи

| ID | Что | Статус | Ветка | Отчёт | Вердикт |
|----|-----|--------|-------|-------|---------|
| `T1` | Панели в тестовом режиме | код есть, не проверен | `feat/T1-test-mode-panes` | не написан, делает `F1a` | — |
| `T5` | Начальный `winsize` до `fork` | код есть, не проверен | `feat/T5-initial-winsize` | `docs/reports/T5-initial-winsize-2026-08-29.md`, неполон | — |
| `F1a` | Доделка и проверка волны 1 | готово | `wave/pane-frame-stall-w1` | `docs/reports/F1a-wave1-completion-2026-08-29.md` | — |
| `R1a-test` | Тесты волны 1 | готово | `review/R1a-test` | `docs/plans/reviews/pane-frame-stall-R1a-test.md` | `PASS_WITH_FINDINGS` |
| `R1a-qa` | Приёмка волны 1 | идёт | — | — | — |
| `T2` | Падающий тест регрессии | ждёт | — | — | — |
| `T3` | Фикс: `exposeAll()` + ответ на отказ | ждёт | — | — | — |
| `T4` | Замер стоимости лишнего кадра | ждёт | — | — | — |
| `T6` | Условно: расщепить `shapeChanged` | ждёт гейта | — | — | — |

Статусы: `ждёт` · `идёт` · `готово` · `блокирована` · `отменена`

## Решения командира

| Когда | Что решено | На основании |
|-------|------------|--------------|
| 2026-08-29 | Перед планированием запущена разведка двумя read-only агентами вместо прямого вызова `/coding-plan` | План по багу с неизвестной корневой причиной получил бы непроверяемые критерии приёмки |
| 2026-08-29 | Диагноз переопределил дефект: не «TUI не запускается», а «окно перестаёт презентовать кадры» | Три независимых измерения на живом бинаре (CPU, `sample`, снятие цикла повреждением соседней панели) |
| 2026-08-29 | `T1` не трогает `session.cpp`: кит ловится существующим `composer.sessionsChangedListeners` | Иначе конфликт владения с `T5` в той же волне — п. 2 проверки плана |
| 2026-08-29 | Гигиена: `.omc/`, `.repowise/`, `.DS_Store` убраны из индекса и закрыты в `.gitignore` (коммит `36466b21`) | 17 файлов операционного мусора тянулись в каждую ветку задачи; территория командира, не задача плана |
| 2026-08-29 | Модель веток адаптирована: рабочая ветка `master` форка, мерж-коммиты `merge <ID>: …` | Конвенция репозитория (`merge T10-followup`, `merge C10`), а не дефолт скилла |
| 2026-08-29 | Убраны 7 висячих worktree от прошлых волн (`/private/tmp/t10`, `/private/tmp/c10` и др.) | `git worktree prune`; гигиена |
| 2026-08-29 | **Дефект плана, найден в работе.** Три строки `test_mode.cpp` (`:400`, `:676`, тело `:676`) переданы из владения `T1` в `T5`. `T1` уведомлён | Контракт К3 меняет **виртуальную** сигнатуру `Pty::spawn()` — она неотделима от всех реализаций, включая фальшивый `TestPtyFactory` в чужом файле. Отказано в варианте «временная правка с откатом»: ветка, не собирающаяся сама по себе, не является сданной задачей, и доказательства расходились бы с коммитом |
| 2026-08-29 | `T5` дополнительно получил `session_ut.cpp` и `ui_sidebar_tabs_ut.cpp` | Механическое следствие смены сигнатуры, по одному токену; ими не владела ни одна задача, без них не собирается |
| 2026-08-29 | **Возобновление после обрыва.** Обе сессии исполнителей волны 1 умерли на лимите аккаунта, не на ошибке в работе. Расхождения борда с git на момент возобновления описаны ниже | Протокол возобновления: git правее борда |
| 2026-08-29 | Незакоммиченная работа `T1` (164 строки `test_mode.cpp`, 54 строки `harness.py`) закоммичена командиром как **чекпойнт, а не сданная задача** — коммит `f915f355`, в сообщении это сказано прямо | Иначе работа терялась. Приёмка остаётся открытой |
| 2026-08-29 | Мой коммит `4404e5d6` по ошибке лёг на ветку `feat/T1-test-mode-panes` вместо ветки волны: `T1` успел переключить основное дерево на свою ветку раньше, чем я коммитил | Исправлено `git branch -f wave/pane-frame-stall-w1 4404e5d6`. Урок: перед коммитом командира проверять, на какой ветке стоит дерево, когда в нём работает агент |
| 2026-08-29 | **Заявление `T5` о зелёной сборке не подтвердилось.** Его последнее сообщение — «`./build st` и `./build unit_tests` зелёные»; проверка его ветки командиром дала две ошибки компиляции в `test_mode.cpp` | Агент откатил `test_mode.cpp` перед коммитом, как предлагал изначально, и заявленное состояние в ветке не сохранилось. Отчёт исполнителя без вывода прогона — не доказательство; это правило внесено в бриф `F1a` |
| 2026-08-29 | Волна собрана мержами `merge T1` и `merge T5` — **без конфликтов**, `T5` чужой файл действительно не трогал | Передача владения тремя строками сработала как задумано, но ни один из двоих их не применил |
| 2026-08-29 | Доделка отдана свежему исполнителю `F1a`, а не сделана командиром | Механическую правку сигнатуры командир вправе сделать сам как разбор сборочной поломки, но `openpty()` с геометрией — это поведение, и прогон тестов — тоже работа исполнителя |
| 2026-08-29 | `openpty()` в фальшивом pty обязан получать геометрию панели, а не `nullptr` — переведено из «желательно» в критерий приёмки `T5` | Иначе панель, рождённая сплитом, в тестовом режиме имеет `0×0`, и `T2` следующей волны напишет тест на неверной геометрии |

## Отложенные находки

| Откуда | Находка | Уровень | Решение |
|--------|---------|---------|---------|
| Диагноз §8, Т3 | `-dump` при нескольких сессиях затирает сам себя | замечание | Не чиним; в итоговый отчёт |
| Диагноз §1 | Сплит через клавиатуру вживую не проверен (`osascript` без Accessibility), проверялся через lldb | важная | Закрывается ручной приёмкой `R3-qa` |
| Скилл `coding-plan` | `references/plan-template.md`, на который ссылается SKILL.md, в установке отсутствует — в каталоге только `SKILL.md` | замечание | Про процесс, не про проект. В итоговый отчёт, предложить правку скилла человеку |
| Оценка мержа §8 | В историю форка был закоммичен операционный мусор тулинга | важная | Устранено `36466b21` |
| `T5` в работе | `build.py:705` собирает `all_libshitty_sources` глобом `lib/shitty/*.cpp`, и `libshitty_test` (`:747`) — из того же списка. Один несобирающийся файл в `lib/shitty` блокирует и `st`, и `unit_tests`, то есть проверку **любой** задачи | важная | Ограничивает параллелизм сильнее, чем видно из карты владения. Учтено в плане; перенести вывод в план мержа перед волной 4 |
| `R1a-test`, мутация M1 | **`EveryPanesChildIsBornWithThatPanesSize` не сторожит то, ради чего написан.** Снятие `resizePty()` до `fork` не роняет его ни разу из 30: `applyLayout()` (`session.cpp:844-857`) ресайзит все панели сразу после `openSession()` в `splitFocused()` (`:743`), и потомок читает `TIOCGWINSZ` уже после этого. Решение Р5 для панели **от сплита** не покрыто ничем; настоящий страж — `TheChildIsBornWithTheSizeSpawnWasGiven`. Комментарий `session.cpp:742-743` и отчёт `T5` коду противоречат | важная | В `F1b`: переименовать тест под то, что он проверяет (`pty_ut.cpp:525`), поправить комментарий и отчёт |
| `R1a-test`, мутация M3 | **Правка `F1a` с `&born` полезна, но не там, где он думал.** У панели от сплита геометрия верна и без неё; `0×0` получает **новая вкладка** (`WINSIZE (0,0)` при окне 11×7). Закрыто новым тестом | важная | Обоснование правки в отчёте `F1a` переписать под реальную причину |
| `R1a-test` | Чётность ширины: 40 колонок → `[18, 18]` (ширины **совпадают**), 41 → `[18, 19]`. Формулировка критерия `T1` «ширина панели 1 отличается от панели 0» негодна | важная | `T2` волны 2 обязана опираться на «каждая панель уже окна», а не на различие ширин. Внести в бриф `T2` |
| `F1a` в работе | **В профиле пользователя `tr` — алиас на `task add project:rt`.** Конвейер вида `... \| tr '\n' ' '` создал 20 пустых задач в taskwarrior (id 730–749). Bash-инструмент читает профиль пользователя, поэтому алиас перехватывает любую команду `tr` | важная | Устранено: пользователь разрешил, 20 задач удалены, 727 рабочих задач проекта целы. **В брифы агентов: не использовать `tr`, брать `sed`/`awk`.** Кандидат в `CLAUDE.md` проекта |
| Отчёт `T5` | **`dev/style.py` на этой машине опасен.** clang-format 23.1.0, а скрипт рассчитан на 21 (`STYLE.md` это оговаривает). Прогон переформатировал 130 файлов, из них 120 чужих (`ext/libstd`, `ext/plt`). Откачено | важная | В `CLAUDE.md` проекта при завершении плана. В брифы агентов внесено уже сейчас |
| Отчёт `T5` | Основное дерево репозитория занято тем агентом, который в нём стоит; параллельная задача обязана работать в отдельном worktree, иначе переключит ветку под соседом | важная | Внести в брифы всех параллельных волн. `T5` сделал это сам и правильно |
| `T5` в работе | Первый fd приходит снаружи через `firstFd` мимо `openpty()` — применить к нему `size` нечем без отдельного `ioctl` | замечание | Не чиним в `T5`; ждёт решения по отчёту |

## Точки остановки и вопросы человеку

| Когда | Вопрос | Ответ |
|-------|--------|-------|
| 2026-08-29 | Порядок: баг или мерж первым | Сначала баг |
| 2026-08-29 | Вариант фикса | 2 сейчас, 1 после замера |
| 2026-08-29 | Чинить ли тестовый режим | Да, отдельной задачей до фикса |
| 2026-08-29 | Находка Т2 (`winsize`) | Отдельной задачей в части 1 |
| 2026-08-29 | Объём и режим мержа | Весь апстрим, автономно до `T5.1` |
| 2026-08-29 | Один план или два | Два |
| — | Мерж `wave/pane-frame-stall-w1` в `master` | ожидается, когда волна 1 зелёная |
| — | Пуш в `fork` | не делался; ожидается решение человека |
