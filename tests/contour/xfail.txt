# Imported baseline: each entry completed every upstream checkpoint but has at
# least one semantic dump mismatch. Infrastructure errors never count as XFAIL.
# VT100 User Guide chapter 3 requires DEL to be ignored and VT52 graphics
# code 137 octal to image Blank; step02 instead retains DEL and '_'.  VT220
# Programmer Reference 4.5 requires S8C1T to be ignored in VT100 mode, while
# step04 is a prematurely recorded empty checkpoint rather than the two
# DECRQSS/S8C1T results printed by vttest.  DECARM is a user-preference mode,
# so the recorded `on` state is not a portable oracle either.
vttest.07.vt52-mode
vttest.09.known-bugs
vttest.11.5.iso6429-cursor
vttest.11.6.6.vt102-with-bce
vttest.11.6.7.ecma48-misc-with-bce
# The scenario's `*` enables ERM with SM 6 before SPA.  ECMA-48:1991 table 6
# and 7.2.4 define set-state as ALL: ED, EL and ECH erase protected as well as
# unprotected areas.  The Contour golden instead retains the guarded box.
vttest.11.6.7.1.protected-area-with-bce
vttest.11.7.iso6429-other
# As above, the wildcard enables ERM before this checkpoint.  ECMA-48:1991
# 7.2.4 requires the box to be erased; the imported golden records Contour's
# non-conforming retention of it.
vttest.11.7.1.protected-area
